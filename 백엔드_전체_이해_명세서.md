# BACK 백엔드 전체 이해 명세서

상태: `현재 기준 문서` (코드 + 함수/역할 분석 문서 기반 사용자/개발자 이해용 재구성)

- 작성일: 2026-02-24
- 수정일: 2026-02-24
- 대상 프로젝트: `madcamp-Screening-Humanity-BACK`
- 기준 문서:
  - `백엔드_함수_분석_및_역할_분석_명세서.md` (함수/파일/역할 분석 상세)
  - `백엔드_API_통합_명세서.md` (HTTP 계약 상세)
- 이 문서의 목표:
  - 신규 개발자/프론트/기획/운영 담당자가 “이 백엔드가 무엇을 하는 시스템인지”를 빠르게 이해
  - 주요 사용자 여정, 데이터 흐름, 외부 서비스 의존성, fallback 정책, 운영 포인트를 한 번에 정리
  - 코드 리팩토링 이후에도 변하면 안 되는 사용자 경험/도메인 계약을 설명

## 최근 작업 메모 (2026-02-26)

- PostgreSQL + C(gRPC) 데이터서비스 전환 스캐폴드 위에 `history` API 1차 실구현이 추가되었다.
  - 목록/상세/삭제는 Python 브리지 테이블(`conversations`, `conversation_audio_assets`) 기반으로 동작
  - 오디오 다운로드(개별/전체 zip) + `tts/regenerate`까지 1차 구현 완료 (기존 chat TTS 내부 경로 재사용)
- 프론트 `/history` 페이지는 상단 진입 버튼 + 목록 카드 + 상세 조회 + 삭제/다운로드 1차 UI가 연결되었고, 신규 대화부터 서버 저장 history 목록에 점진적으로 나타난다.
- 프론트 `landing`의 "내 기록" 패널도 서버 `/api/history` 목록/상세를 우선 사용하도록 전환되었으며,
  - Google 계정은 서버 원본을 우선 표시하고 로컬 기록 fallback 의존을 줄였다.
  - guest 계정은 서버 오류 시에만 현재 탭의 로컬 임시 기록 fallback을 유지한다.
- 프론트 `chat-room`의 로컬 히스토리 저장은 `saveChatHistoryCompat()` 경로로 축소되어,
  - Google 계정은 서버 history를 원본으로 쓰고 브라우저 메모리에는 현재 세션 임시 캐시만 남긴다.
  - guest/anonymous는 호환성 때문에 기존 로컬 저장 경로를 당분간 유지한다.
- guest→Google 병합 API도 1차(move-only) 구현되어 ownership transfer가 가능해졌다.
- 프론트 guest→Google 병합 모달은 공통 hook/dialog로 정리되어 `landing`과 `/history`에서 동일 정책으로 동작하며, 기본 CTA는 `나중에 결정(보류)`를 따른다.
- guest→Google 병합 UX를 위해 Google 로그인 후 guest 쿠키를 즉시 삭제하지 않는 준비 플래그는 계속 유지된다.
- `USE_C_DATA_SERVICE=true` 실리허설을 위해 `history/guest merge` 일부 백엔드 경로는 내부적으로
  `DataServiceClient`의 최소 gRPC 호출 시도를 먼저 경유하고, 현재 단계에서는 Python `history_service`로 fallback한다.
  즉, 사용자 기능은 유지하면서 C(gRPC) 데이터계층 전환 경로를 실제 호출 시도까지 포함해 먼저 고정하는 상태다.
- 운영용 상세 헬스체크(`/api/system/health/detailed`)에는 `data_service` 상태가 포함되어
  C data-service gRPC 리허설 준비 상태(`grpc_ok`/`scaffold_only`/`grpc_unavailable`)를 함께 관찰할 수 있다.
- 리허설 단계에서는 `DATA_SERVICE_REHEARSAL_FALLBACK_ENABLED`로 최소 gRPC 호출 실패 시 Python fallback 허용/차단 정책을 제어할 수 있다.
- `c-data-service` 컨테이너는 `--healthcheck` one-shot 모드로 PostgreSQL 연결 점검을 수행하며 compose healthcheck에 연결되어 있다.
- `c-data-service`는 `--check-schema` one-shot 모드도 제공하여 리허설 전에 핵심 테이블/뷰 존재 여부를 점검할 수 있다.
- `scripts/smoke_data_service_rehearsal.py`로 `grpcurl`/`docker`/`protoc`/컴파일러 준비 상태와 `HealthService.Check` best-effort 스모크를 JSON으로 점검할 수 있다.
- `DataServiceClient`는 리허설 단계에서 `DATA_SERVICE_CLI_BRIDGE_ENABLED=true`면 `c-data-service` one-shot JSON 명령을 통해
  최소 `history/guest merge` 경로를 C data-service 바이너리로 먼저 경유할 수 있다. (gRPC transport 완성 전 중간 단계)
- 같은 CLI bridge 범위가 확장되어 `history detail/delete`도 `c-data-service` one-shot JSON 경유를 먼저 시도하고,
  실패 시 기존 Python `history_service` fallback으로 내려가는 리허설 경로를 사용한다.
- 같은 CLI bridge 범위가 guest session 조회/상태변경 및 `auth_events` 생성까지 확장되었고,
  인증 감사 로그는 data-service 우선 / SQLAlchemy fallback(best-effort) 구조로 전환되기 시작했다.
- 같은 리허설 패턴이 `auth/deps`의 user principal 조회 일부와 `story.py`의 시나리오 저장까지 확장되기 시작해,
  인증/시나리오 저장 경로도 점진적으로 C data-service 우선 구조로 이동 중이다.
- 같은 리허설 패턴(`gateway -> data_service_client -> fallback`)이 `GET /api/characters/presets`, `GET/PUT /api/users/me/settings`에도 확장되기 시작해,
  점진적으로 SQLAlchemy 직접 접근을 줄이는 방향으로 정리 중이다.
- PostgreSQL canonical schema와 Python 브리지 ORM 차이를 줄이기 위한 `004/005` 브리지 호환 migration이 추가되어,
  history 브리지 컬럼(`legacy_session_id`, `mode`, `preview_text`, `audio_count`, bridge audio columns)과 관련 view 컬럼이 보강되었다.
- `/history` 상세 화면은 1차 확장으로 기록 기반 `대화 평가`, `스토리 분석`, `TTS 재생성` 액션을 제공하여
  백엔드에 존재하던 일부 미노출 기능 재연결(Phase F)을 시작했다.
- Phase E 브리지로 캐릭터 preset 공급원 전환이 시작되었다.
  `GET /api/characters/presets`는 DB `character_catalog`를 우선 사용하고, 초기 단계에서는 `FRONT/public/characters` 파일 로더를 fallback으로 유지한다.
- 관리자용 캐릭터 카탈로그(`/admin/character-catalog`) 1차 화면/백엔드 API가 추가되어,
  seed import와 기본 메타 수정/삭제 흐름을 먼저 검증할 수 있는 상태다.
- 관리자 캐릭터 카탈로그는 1차 확장으로 페르소나 AI 생성(실패 시 mock fallback 저장)과 이미지 링크/파일 업로드(media_assets 연계) 흐름까지 지원한다.
- 관리자 캐릭터 카탈로그는 변경 이력(revision) 조회 패널과 DB 원본 이미지 프리뷰까지 연결되어 seed import 이후 운영 검수/보정이 쉬운 상태다.
- seed import 결과에는 `image_stats`가 포함되어 이미지 import 후보/누락/오류 및 BLOB/volume 저장 분포를 운영자가 바로 확인할 수 있다.
- DB 원본 캐릭터 카탈로그 이미지는 `GET /api/characters/catalog-image/{catalog_id}`로 서빙되며,
  내부적으로 `media_assets`의 blob/volume 저장 형태를 숨긴다.
- 프론트 preset 로딩은 backend `/api/characters/presets`를 우선 사용하도록 전환되었고,
  `FRONT/app/api/characters/route.ts`는 개발/비상 fallback용 로컬 프록시 역할로 축소되는 단계다.
  로컬 fallback은 환경 플래그(`NEXT_PUBLIC_LOCAL_PRESET_FALLBACK_ENABLED`, `CHARACTER_LOCAL_PRESET_FALLBACK_ENABLED`)로 단계적으로 차단할 수 있다. (기본값: dev만 허용)

## 목차

1. [문서 목적과 대상 독자](#문서-목적과-대상-독자)
2. [한눈에 보는 시스템 요약](#한눈에-보는-시스템-요약)
3. [핵심 사용자/역할 모델](#핵심-사용자역할-모델)
4. [핵심 도메인 개념 (무엇을 다루는 서비스인가)](#핵심-도메인-개념-무엇을-다루는-서비스인가)
5. [전체 아키텍처 이해 (컴포넌트/통신/저장소)](#전체-아키텍처-이해-컴포넌트통신저장소)
6. [주요 사용자 여정 (시나리오별 end-to-end 흐름)](#주요-사용자-여정-시나리오별-end-to-end-흐름)
7. [데이터 저장/수명주기 이해](#데이터-저장수명주기-이해)
8. [Fallback/장애 대응 정책 이해](#fallback장애-대응-정책-이해)
9. [보안/설정 플래그가 사용자 경험에 미치는 영향](#보안설정-플래그가-사용자-경험에-미치는-영향)
10. [운영/배포 관점 이해 (로컬/도커/외부 GPU 서버)](#운영배포-관점-이해-로컬도커외부-gpu-서버)
11. [신규 개발자 온보딩 가이드 (읽는 순서/핫스팟)](#신규-개발자-온보딩-가이드-읽는-순서핫스팟)
12. [문제 해결 가이드 (증상별 트러블슈팅)](#문제-해결-가이드-증상별-트러블슈팅)
13. [용어 사전 / FAQ](#용어-사전--faq)
14. [변경 이력 / 후속 과제](#변경-이력--후속-과제)

## 문서 목적과 대상 독자

이 문서는 API 함수 시그니처를 나열하는 문서가 아니라, **서비스 전체를 이해하기 위한 운영/도메인 설명서**다.

### 대상 독자

- 프론트엔드 개발자
  - 어떤 화면/기능이 어떤 백엔드 도메인과 연결되는지 이해
  - 실패 시 어떤 fallback UX가 나오는지 예측
- 백엔드 신규 개발자
  - 코드 읽기 전에 전체 맥락(도메인/데이터/외부 연동/보안 플래그)을 파악
- 기획/PM
  - 현재 시스템이 실제로 제공하는 기능 범위/한계/운영 리스크를 이해
- 운영/DevOps
  - 서비스 의존성(LLM, GPT-SoVITS, Files/Train API, Redis, DB)과 헬스체크/환경변수 영향도를 파악
- QA
  - 정상/오류/fallback 동작을 기능 단위로 묶어 테스트 관점을 세울 수 있음

### 이 문서와 다른 문서의 역할 구분

- `백엔드_함수_분석_및_역할_분석_명세서.md`
  - 함수 단위/파일 단위/역할 분석 + 보수적 개선 관점의 기준서
- `백엔드_API_통합_명세서.md`
  - HTTP path/method/body/response 계약 중심 명세서
- `백엔드_전체_이해_명세서.md` (이 문서)
  - 사용자 관점 흐름 + 시스템 관점 이해 + 운영 관점 설명서

## 한눈에 보는 시스템 요약

이 백엔드는 단순한 채팅 서버가 아니라, 다음 기능을 한 시스템 안에서 묶어 제공한다.

### 제공 기능 축

1. 인증/세션
- Google OAuth 기반 로그인
- 백엔드 guest 로그인 (HttpOnly guest 쿠키 + 서버 guest 세션 기록)
- HttpOnly 쿠키 기반 세션 유지
- 관리자 이메일 기반 권한 분기

2. 캐릭터(페르소나) 관리
- preset 캐릭터(파일 기반)
- 사용자 캐릭터(DB 기반)
- 관리자 캐릭터-음성 연결 관리
- AI 기반 캐릭터 설정 자동 생성

3. 채팅/대화 경험
- 일반 모드(한 캐릭터와 대화)
- 감독 모드(두 캐릭터를 교대 발화)
- 스트리밍 응답(SSE)
- 대화 요약/컨텍스트 관리

4. TTS (GPT-SoVITS)
- 공개 TTS API
- 채팅 응답에 `audio_url` 첨부
- 음성 목록 조회 및 미리듣기
- 로그인 사용자 TTS 캐시(오디오 메타 저장)

5. 음성(Voice) 관리/학습 파이프라인
- 관리자용 Server A 파일 탐색/업로드/트리밍
- 사용자용 model-make 업로드/학습 시작/상태 조회/등록/정리
- 관리자용 학습 프록시(voices/train/*)

6. 운영성/관측성
- 상세 헬스체크 (DB + 외부 서비스)
- startup 보안 플래그 경고
- 설정 중앙화 (`Settings`)

### 시스템의 핵심 특징 (사용자 경험 관점)

- 외부 AI/음성 서버가 불안정해도 **일부 경로는 success 형태 fallback**을 유지한다.
  - 예: `/chat`, `/story`, `/evaluation`, `/characters/generate` 일부
- 레거시 경로를 남겨 프론트/운영 도구 호환성을 우선한다.
  - 예: `/api/generate/story`, `/api/story/analyze`, `/api/generate/character-details`
- 라우터마다 응답 스타일이 완전히 통일돼 있지는 않다.
  - 이는 레거시/프론트 계약 유지 우선 전략의 결과이며, 문서로 명확히 인지하고 사용해야 한다.

## 핵심 사용자/역할 모델

### 1) 일반 사용자 (로그인 사용자)

할 수 있는 일:
- 로그인 후 내 설정(TTS 옵션) 저장
- 내 캐릭터 생성/조회/수정/삭제
- 채팅/스트리밍 채팅 사용
- model-make로 내 음성 모델 제작(업로드/학습 시작/등록/삭제)
- 일부 AI 생성 기능 사용

중요 제약:
- 관리자 전용 Voice CRUD, Server A 파일 관리 경로 접근 불가
- 본인 소유가 아닌 캐릭터/음성 삭제/수정 제한

### 2) 게스트 사용자 (프론트 탭세션 + 백엔드 guest 쿠키)

가능한 기능(현재 정책 기준):
- 랜딩에서 게스트 시작
- `/api/auth/guest/login`으로 서버 guest 세션 생성 + guest 쿠키 발급
- `/api/chat`, `/api/chat/stream`, `/api/tts` 등 공개/선택적 인증 경로 사용
- 프론트 탭 세션(`sessionStorage`) 기반 대화 기록 유지 (탭 종료 시 삭제)

중요한 특징:
- 백엔드는 `guest_sessions`, `auth_events`로 guest 로그인/로그아웃/만료를 기록
- `/api/auth/me`는 `provider="guest"`, `auth_mode="guest"`, `is_guest=true` 반환 가능
- 관리자/사용자 전용 경로(`/api/users/me/settings`, admin API)는 접근 불가

### 3) 비로그인 사용자 (익명/개발 모드 사용자 포함)

가능한 기능(현재 정책 기준):
- 일부 공개 API 접근 (`/chat`, `/tts`, `/voices`, 헬스체크, 일부 AI 생성)
- 개발용 캐릭터 경로(`/api/characters`, `/api/characters` POST) 사용 가능 (운영 비활성화 후보)

주의:
- `DEV_AUTH_FALLBACK_ENABLED=true`면 토큰이 없어도 `dev-user` 폴백이 일어나 일부 인증 경로가 개발 환경에서 동작 가능
- 운영에서는 이 플래그를 끄는 것이 권장됨

### 4) 관리자

가능한 기능:
- Voice CRUD
- Server A 파일 브라우징/업로드/삭제/mkdir/trim
- 관리자용 캐릭터-voice 연결 관리
- voices/train 프록시로 학습 직접 제어

판정 기준:
- `settings.is_admin(email)` -> `ADMIN_EMAILS` 환경변수 기반
- 프론트 `/admin/voices` 페이지에도 UX 선차단 가드가 추가되었지만, 최종 판정은 백엔드 `require_admin`

### 5) 시스템/운영 도구 (비인간 사용자)

대표 사용 경로:
- `/api/system/health/detailed`
- `/api/system/health`, `/api/health`
- `/api/system/status`
- `/api/test`

역할:
- 서비스 가용성 점검
- 외부 의존성(LLM/TTS/Server A) 연결 가능 여부 모니터링
- 로컬/프론트 연결 점검

## 핵심 도메인 개념 (무엇을 다루는 서비스인가)

### 1) 캐릭터 (Character)

이 서비스에서 캐릭터는 단순 프로필이 아니라 **대화 행동 규칙(페르소나)** 을 담는 핵심 도메인이다.

캐릭터의 구성 요소:
- 이름/설명/카테고리/태그
- 페르소나 텍스트 (`persona`)
- 연결된 음성 (`voice_id`)
- 이미지/모델 URL(부가 자산)

캐릭터의 출처가 두 종류인 점이 중요하다.

- preset 캐릭터: 파일(JSON) 기반
- 사용자 캐릭터: DB 기반

이 혼합 구조 때문에 `/api/characters/{id}` 같은 경로는 내부에서 “preset 우선 -> DB fallback” 순서로 처리된다.

### 2) 음성 (Voice)

Voice는 TTS 품질과 학습 파이프라인을 연결하는 메타데이터 레이어다.

포함 정보:
- 참조 오디오 경로 (`ref_audio_path`)
- 참조 텍스트/언어 (`prompt_text`, `prompt_lang`)
- 파인튜닝 모델 가중치 경로 (`gpt_weights_path`, `sovits_weights_path`)
- 학습 파이프라인 연결 정보 (`train_input_dir`, `training_model_name`)

중요한 점:
- 경로는 백엔드 로컬 경로가 아니라 **Server A 기준 경로 문자열** 일 수 있다.
- 관리자 Voice 관리와 사용자 model-make Voice 등록이 같은 `voices` 테이블로 합류한다.

### 3) 시나리오 / 대화 / 요약

서비스는 채팅 자체뿐 아니라 **채팅 전 배경 설정과 채팅 후 요약** 을 함께 관리한다.

- `Scenario`: 대화 시작 전에 만든 상황/배경 스냅샷
- `ChatMessage`: 실제 대화 로그 (session_id 기준)
- `ChatSummary`: 세션별 최신 요약 1개

이 구조 덕분에 사용자 경험은 다음처럼 분리된다.
- 화면 상단: `Scenario.summary`/`background` 기반 맥락
- 실제 대화 지속성: `ChatMessage` + `ChatSummary`

### 4) 생성 작업 (GenerationJob)

`/api/generate/*` 라우터는 현재 mock 성격이 강하지만, 구조적으로는 다음을 위한 자리다.

- 장시간 비동기 생성 작업 상태 추적
- 진행률/결과 URL 제공
- 실패 메시지 기록

즉, “AI 채팅/TTS” 외에 3D/자산 생성 같은 확장형 워크로드를 수용하는 발판 역할을 한다.

### 5) Server A (GPT-SoVITS 측)와의 관계

이 백엔드는 GPT-SoVITS 본체를 직접 구현하지 않고, **Server A를 orchestration/proxy 형태로 활용**한다.

- TTS 합성 요청 전달
- 학습 요청 전달
- 파일 인덱스/삭제/트리밍 준비 API 호출
- 결과 메타를 백엔드 DB에 연결

즉 이 시스템은 사용자 경험과 데이터 일관성을 담당하고, GPU 연산/학습/TTS 본체는 외부 서비스에 위임한다.

## 전체 아키텍처 이해 (컴포넌트/통신/저장소)

### 1) 컴포넌트 구성 (개념도)

```text
[Frontend (React/Next)]
        |
        | HTTP(S) / SSE / Cookie
        v
[BACK FastAPI]
   |      |      |        |
   |      |      |        +--> [Redis] (queue/stream, context fallback)
   |      |      +-----------> [LLM] (Gemini / Ollama)
   |      +------------------> [Server A Files/Train APIs]
   +-------------------------> [GPT-SoVITS TTS API]
        |
        +--> [DB (SQLite/Postgres 설정 가능)]
        +--> [Local assets dir (/assets)]
```

### 2) 레이어 구조 (코드 관점)

- `app/api/*`
  - 외부 계약(HTTP path/method/body/response)을 담당하는 라우터 레이어
  - 일부 레거시/오케스트레이션 로직이 남아 있음
- `app/services/*`
  - 라우터에서 반복되는 비즈니스/helper/외부 연동 로직 분리
  - `server_a_client`, `chat_service`, `character_service`, `tts_service`가 핵심
- `app/core/*`
  - 설정, DB, 보안, LLM, Redis 등 공통 인프라
- `app/models/*`
  - SQLAlchemy ORM 모델
- `app/workers/*`
  - Redis 큐 기반 worker (`tts_worker`)

### 3) 저장소 종류별 역할 분담

#### DB (메타데이터/관계/상태)

저장하는 것:
- 사용자 계정
- 캐릭터/음성 메타
- 시나리오/대화 메시지/요약
- 생성 job 상태
- 사용자 설정
- TTS 오디오 메타(캐시 정보 포함)

DB에 저장하지 않는 것:
- 대용량 오디오 바이너리 본문
- GPT-SoVITS 학습 가중치 실제 파일
- 훈련용 WAV 파일 실데이터

#### 파일시스템 / 정적 자산 디렉터리

- `/assets`로 서빙되는 `USER_ASSETS_DIR`
- TTS 생성 결과 파일 경로/URL의 실제 저장 위치
- mock 생성 결과(예: `.glb`) 저장 위치

#### Server A 파일 공간 (외부 서비스 측)

- `sample_train_voice`, `logs`, `TEMP`, `GPT_weights*`, `SoVITS_weights*`
- 백엔드는 경로 문자열과 API 호출로 관리/참조

#### Redis

- TTS worker 큐/스트림
- context_manager fallback 경로 일부
- 실시간 처리 보조 (DB 대체가 아니라 보조/임시 상태 성격)

## 주요 사용자 여정 (시나리오별 end-to-end 흐름)

### 여정 1. 처음 접속해서 인증 상태를 만들고 내 정보를 확인하는 흐름

#### 사용자 관점

1. 랜딩에서 `Google 로그인` 또는 `게스트로 시작` 선택
2. (Google) OAuth 로그인 완료 후 프론트 콜백 페이지 이동
3. (Guest) 서버 guest 세션 생성 + 프론트 탭세션 시작
4. 프론트는 Google/Guest 모두 `/auth/me`를 호출해 현재 인증 주체(user/guest)를 최종 확정
5. guest는 탭 우선 정책: 탭세션(`sessionStorage`)이 없으면 서버 guest 쿠키가 남아 있어도 자동복원하지 않고 guest 로그아웃 처리 후 익명 시작

#### 백엔드 내부 흐름

1. Google 경로
   - `GET /api/auth/google/login`
   - `GET /api/auth/google/callback`
   - `users` 조회/생성 -> JWT 발급 -> user 쿠키 설정
   - guest 쿠키가 남아 있으면 정리
   - `auth_events(login, google_user)` 기록
2. Guest 경로
   - `POST /api/auth/guest/login`
   - `guest_sessions` 생성 -> guest JWT 쿠키(`guest_access_token`) 발급
   - `auth_events(login, guest)` 기록
3. `GET /api/auth/me`
   - user 쿠키 우선, 없으면 guest 쿠키 해석
   - guest 응답이면 `last_seen_at` 갱신(베스트 에포트)
   - `auth_mode`, `is_guest` 포함 응답 반환
   - 프론트는 이 응답을 단일 부트스트랩 기준으로 사용 (callback/app-init 공통)

#### 실패/예외 체감 포인트

- Google 응답 지연 -> `408` (타임아웃)
- 쿠키 secure/samesite 설정 불일치 -> 프론트에서 로그인 상태 유지 실패
- guest 쿠키가 있어도 user 전용 경로에서는 인증 실패(의도된 동작)
- `DEV_AUTH_FALLBACK_ENABLED`가 켜져 있으면 개발 환경에서 “로그인 안 했는데 되는 것처럼” 보일 수 있음

### 여정 2. 시나리오 생성 후 캐릭터와 대화하는 흐름

#### 사용자 관점

1. 상황 키워드 입력 -> 스토리 생성
2. 배경/요약 확인
3. 채팅 시작
4. 대화 진행 중 가끔 요약 압축
5. (옵션) TTS 음성 재생

#### 백엔드 흐름 (축약)

1. `POST /api/story/generate/story`
   - LLM 호출로 `summary/background` 생성
   - `scenarios` 저장
   - 실패 시 fallback summary/background 반환
2. `POST /api/chat`
   - 요청 정규화 / 캐릭터 식별 / 시나리오 필드 추출
   - 컨텍스트 요약 필요 시 `context_manager` 수행
   - LLM 호출 (Gemini/Ollama)
   - `chat_messages` 저장
   - TTS 옵션 켜져 있으면 내부 TTS 경유 `audio_url` 첨부 시도
3. 프론트는 `content`, `usage`, `session_id`, `context_summarized`, `audio_url?` 사용

#### 사용자 경험에 중요한 계약

- `/chat` 실패 시에도 `success=true` mock 대사가 올 수 있음 (연결 문제 시 UX 유지)
- TTS 실패는 채팅 전체 실패로 전파되지 않고 `audio_url`만 빠질 수 있음
- `session_id`를 프론트가 유지해야 대화 컨텍스트/요약이 이어짐

### 여정 3. 스트리밍 채팅 (SSE)

#### 사용자 관점

- 응답이 한 번에 오지 않고 타자치는 것처럼 조금씩 보임

#### 백엔드 흐름

1. `POST /api/chat/stream`
2. 시스템 프롬프트 구성 + 메시지 정규화
3. `call_llm_stream` 호출
4. 청크마다 SSE 프레임 전송
5. 완료 시 `done=true`, `full_content`, `session_id` 전송

#### 설계 의도 (중요)

- `/chat`와 완전히 같은 로직을 쓰지 않는다.
- 이유: SSE 연결/프레임 호환성이 핵심이고, DB 저장/TTS 후처리는 상대적으로 우선순위가 다르기 때문

### 여정 4. 캐릭터를 직접 만들거나 AI로 초안 생성하는 흐름

#### A) 수동 생성

- 비로그인 개발용: `/api/characters` (dev-user 기반)
- 로그인 사용자: `/api/characters/my`

#### B) AI 초안 생성 (간단형)

- `POST /api/generate` (characters 라우터)
- 입력: 이름/설명/카테고리
- 출력: `persona`, `description`, `tags` 중심 초안
- 실패 시 mock fallback 유지

#### C) AI 상세 생성 (정밀형)

- `POST /api/ai/generate/character-details` (또는 legacy alias)
- 더 많은 필드(세계관/말투/가이드라인 등) 생성
- 인증 필요
- 실패 시 500 (mock fallback이 아니라 strict한 생성형 경로)

#### 사용자 입장에서 헷갈릴 수 있는 점

- `/api/generate` 와 `/api/generate/character-details` 는 이름이 비슷하지만 출력 스키마/의도가 다르다.
- 하나는 빠른 초안, 다른 하나는 상세 설정 생성에 가깝다.

### 여정 5. 관리자 Voice 관리 + 미리듣기 + 학습 프록시

#### 관리자 작업 흐름 (전형)

1. Server A 파일 확인 (`/api/voices/server-files`)
2. train voice 업로드 (`/api/voices/server-files/upload`)
3. ref audio trim/prepare (`/api/voices/server-files/trim`)
4. Voice DB 등록/수정 (`/api/voices`, `/api/voices/{id}`)
5. 미리듣기 (`/api/voices/{id}/test`)
6. 필요 시 Server A 학습 프록시 (`/api/voices/train/*`)

#### 설계상 특징

- 관리자 경로는 사용자-facing UX보다 운영 편의/프록시 기능 비중이 큼
- raw proxy 응답이 많아서 프론트 관리도구는 Server A 응답 구조를 일정 부분 알아야 함

### 여정 6. 일반 사용자의 model-make 음성 제작 파이프라인

#### 사용자 관점

1. WAV 여러 개 업로드
2. 학습 시작
3. 상태/로그 확인
4. 완료 후 Voice 등록
5. 내 Voice를 캐릭터에 연결해서 대화 TTS에 사용

#### 백엔드 흐름

1. `POST /api/model-make/upload`
   - 용량/개수/길이 검증
   - Server A 경로에 업로드
   - `train_input_dir` 반환
2. `POST /api/model-make/start`
   - `train_input_dir`를 실제 Server A `upload_path`로 조합
   - 학습 시작
3. `GET /api/model-make/status/{model_name}`, `/log/{model_name}`
   - 상태/로그 polling
4. `POST /api/model-make/register`
   - ref audio 보정 시도
   - weights 자동 추론 시도
   - `voices` 테이블에 user-owned voice 등록
5. `DELETE /api/model-make/my/{voice_id}`
   - Voice + 연결 캐릭터 + Server A 잔여 자원 정리

#### 사용자 경험상 중요한 정책

- `abort`, `delete_my_model_voice`는 외부 삭제 일부 실패를 흡수하고 UX를 우선함
- `register`에서 weights 자동 추론 실패해도 Voice 등록 자체는 진행 가능 (이후 관리자 보정 가능)

## 데이터 저장/수명주기 이해

### 1) 채팅 데이터 수명주기

입력 -> 저장 -> 요약 -> 재사용의 흐름으로 이해하면 쉽다.

1. 프론트가 `messages` + `session_id`(또는 없음) 전송
2. `/chat`가 `session_id`를 확정
3. user/assistant 메시지를 `chat_messages`에 저장
4. 긴 대화면 `context_manager`가 요약 생성/업데이트 (`chat_summaries`)
5. 다음 요청에서 `session_id`로 이어짐

의미:
- `ChatMessage`는 상세 로그
- `ChatSummary`는 압축 컨텍스트
- 둘 다 있어야 긴 대화에서 성능/품질 균형을 잡기 쉬움

### 2) TTS 데이터 수명주기

1. `/chat` 또는 `/tts` 요청에서 TTS 실행
2. Server A에서 오디오 바이너리 생성
3. 파일시스템에 저장 (내부 TTS 경로)
4. 로그인 사용자면 `AudioFile` 메타 저장 + `text_hash` 캐시 가능
5. 이후 동일 텍스트/voice/format이면 캐시 재사용 가능

주의:
- 공개 `/api/tts`는 base64/binary 중심이고, 내부 `_synthesize_tts_internal`은 `audio_url` 중심 계약
- 둘은 의도적으로 역할이 다르다

### 3) 캐릭터/Voice 연결 수명주기

- 캐릭터 생성 시 `voice_id`는 없어도 됨
- 나중에 관리자 또는 사용자 흐름에서 연결 가능
- Voice 삭제 시 연결 캐릭터 처리 정책이 경로마다 다를 수 있음
  - `model-make` 삭제 경로는 관련 캐릭터 `voice_id=NULL` 처리 수행
  - 관리자 Voice 삭제 정책은 `permanent` 여부에 따라 분기

### 4) model-make 자원 수명주기

- 업로드 산출물: `sample_train_voice/user_{id}/run_{ts}`
- 학습 산출물/로그: `logs/{model_name}`, `TEMP/{model_name}`
- 등록 후 DB 링크: `Voice.train_input_dir`, `Voice.training_model_name`
- 삭제/중단 시 이 메타정보를 사용해 외부 자원 정리

### 5) 게스트 인증/감사 데이터 수명주기

1. `POST /api/auth/guest/login`
2. `guest_sessions(status=active)` 생성 + `expires_at` 설정
3. `auth_events(login)` 기록
4. `/api/auth/me`, `/api/chat*` 호출 중 guest actor 식별/로그 컨텍스트 사용
5. `POST /api/auth/logout` 시 `guest_sessions.status=logged_out`, `ended_at` 갱신 + `auth_events(logout)` 기록
6. 프론트는 guest 탭세션(`sessionStorage`)을 별도로 삭제해 탭 종료/게스트 종료 시 기록을 제거

의미:
- 프론트 탭세션 데이터와 별개로, 서버는 guest 세션의 로그인/로그아웃 이력을 추적 가능
- abuse 대응/rate-limit/운영 감사의 기반 데이터로 활용 가능
- 따라서 guest는 "인증 세션(서버 soft close)"와 "대화/UX 데이터(프론트 hard delete)"가 분리된 2층 수명주기 구조로 이해해야 한다.

## Fallback/장애 대응 정책 이해

이 백엔드는 사용자 경험을 살리기 위해 일부 경로에서 **엄격 실패 대신 성공 형태 fallback**을 택한다. 이 특성을 이해하지 못하면 QA/프론트에서 “버그인가?”로 보일 수 있다.

### 1) 성공 형태 fallback을 유지하는 경로 (대표)

- `/api/chat`
  - LLM 연결 실패 시 mock 응답 대사 반환 (`success=true`)
- `/api/story/generate/story`
  - 스토리 생성 실패 시 기본 summary/background 반환 (`success=true`)
- `/api/evaluation/evaluate`
  - 평가 실패 시 기본 점수/피드백/summary 반환 (`success=true`)
- `/api/generate` (characters 라우터의 간단 생성)
  - LLM 체인 실패 시 mock 데이터 반환 (`success=true`)
- `/api/model-make/abort`, `/api/model-make/my/{voice_id}`
  - 외부 삭제 실패 일부 흡수 후 전체 요청 성공 유지 정책

### 2) 실패를 그대로 전파하는 경로 (대표)

- `/api/ai/generate/character-details`
  - Gemini/Ollama fallback 체인 실패 시 `500`
- `/api/model-make/start`
  - 입력 버전 검증 실패 `400`
  - Server A 프록시 실패 `HTTPException`
- `/api/voices/*` 관리자/관리성 경로 상당수
  - 운영도구 경로 특성상 실패를 명시적으로 보여주는 편이 유리

### 3) 왜 이런 혼합 정책을 쓰는가

- 사용자 대화/콘텐츠 생성 UX는 “완전 중단”보다 “품질 저하 fallback”이 체감상 낫다.
- 반면 관리자/운영/학습 경로는 실패를 숨기면 실제 운영 문제가 누적된다.
- 따라서 도메인별로 실패 노출 전략이 다르며, 이는 의도된 설계다.

## 보안/설정 플래그가 사용자 경험에 미치는 영향

### 1) `DEV_AUTH_FALLBACK_ENABLED`

사용자 체감 영향:
- `true`인 개발 환경에서는 로그인 없이도 일부 인증 경로가 동작할 수 있음
- 프론트 개발 속도는 빨라지지만, 운영에서 켜져 있으면 보안 위험

운영 권장:
- 프로덕션 `false`
- `APP_ENV=production` + `STRICT_STARTUP_VALIDATION=true` 조합에서는 `true`일 경우 startup fail-fast

### 2) `GOOGLE_SSO_ALLOW_INSECURE_HTTP`

사용자 체감 영향:
- 로컬 HTTP 환경에서 Google OAuth 테스트 가능 여부에 영향
- 운영 HTTPS에서는 `false`로 두는 것이 바람직

### 3) `AUTH_COOKIE_SECURE`, `AUTH_COOKIE_SAMESITE`

사용자 체감 영향:
- 로그인 후 세션 유지/쿠키 전달 여부에 직접 영향
- 특히 프론트와 백엔드 도메인이 다르거나 HTTPS/HTTP가 섞이면 체감 버그처럼 보일 수 있음

### 4) `STRICT_STARTUP_VALIDATION`

사용자 체감 영향:
- 기본 정책은 warn-only
- 단, `APP_ENV=production`에서 일부 위험 조합(`DEV_AUTH_FALLBACK_ENABLED=true`)은 fail-fast 적용
- 운영자가 위험 설정을 조기에 발견/차단하는 데 도움

### 5) Guest 인증 관련 플래그 (`GUEST_*`)

사용자 체감 영향:
- 게스트 로그인 가능 여부 (`GUEST_AUTH_ENABLED`)
- guest 쿠키 이름/수명 (`GUEST_AUTH_COOKIE_NAME`, `GUEST_ACCESS_TOKEN_EXPIRE_MINUTES`, `GUEST_COOKIE_SESSION_ONLY`)

주의:
- 프론트 탭세션(`sessionStorage`)과 서버 guest 쿠키는 역할이 다르다.
  - 프론트 탭세션: 대화 기록/UX 상태
  - 서버 guest 쿠키: 서버 식별/감사 로그
- 권장 프론트 정책(탭 우선):
  - `/auth/me`가 guest를 반환해도 현재 탭 `sessionStorage` payload가 없으면 guest 자동복원 대신 `/auth/logout` 후 익명 처리
  - 목적: "탭 종료 시 guest 데이터 삭제" UX를 유지하면서 서버 감사 로그는 보존

### 6) `STARTUP_SCHEMA_PATCH_ENABLED`

사용자 체감 영향:
- 서버 기동 시 레거시 DB 호환 패치 적용 여부
- 개발/레거시 DB 환경에서 부팅 성공 여부에 영향 가능

## 운영/배포 관점 이해 (로컬/도커/외부 GPU 서버)

### 1) 로컬 개발 vs 외부 GPU 서버 연동

이 시스템은 백엔드 자체에서 GPU TTS/학습을 수행하지 않고, Server A를 호출하는 구조다.

따라서 로컬 개발 시 가능한 형태가 여러 가지다.

- 백엔드만 로컬 + Server A는 외부 도메인 사용
- Docker Compose로 백엔드/프론트 + GPT-SoVITS/sidecar까지 로컬 실행
- 일부는 로컬, 일부는 원격(혼합)

핵심은 설정 URL들:
- `SERVER_A_FILES_API_URL`
- `SERVER_A_TRAINING_API_URL`
- `TTS_BASE_URL`
- `OLLAMA_BASE_URL`

### 2) Docker Compose 이해 포인트 (사용자/운영 관점)

루트 `docker-compose.yaml` 기준 주요 컨테이너 역할:
- `backend`: FastAPI (Server B)
- `frontend`: 프론트 서버
- `gpt-sovits-cu128`: GPT-SoVITS 본체 이미지
- `gpt-sovits-files-api`: 파일 인덱스/보조 API 사이드카
- `gpt-sovits-training-api`: 학습 제어 API 사이드카

중요한 운영 포인트:
- GPT-SoVITS 본체는 별도 사전빌드 이미지 사용
- automation 사이드카는 프로젝트 내부 `server-a-scripts-Gpt-sovits_inside` 코드로 빌드
- 볼륨을 공유해서 본체/사이드카가 같은 `sample_train_voice`, `logs`, `weights`를 본다
- 현재 compose는 GPT-SoVITS 관련 데이터 볼륨을 Docker named volume 대신 **Windows D드라이브 bind mount**로 고정
  - 기본 루트: `D:/docker/madcamp03/gpt-sovits`
  - override: `DOCKER_DATA_ROOT`
- 운영자는 Docker Desktop에서 D드라이브 파일 공유 권한과 디렉토리 사전 생성 여부를 확인해야 함

### 3) 헬스체크가 보는 대상과 실제 체감 장애의 차이

`/api/system/health/detailed`는 “연결 가능 여부” 중심이다.

의미:
- 4xx를 `online`으로 보는 경우가 있음 -> 서비스 살아있음 의미
- 실제 기능 성공과 1:1 대응은 아님

예시:
- TTS `/tts` GET 호출이 파라미터 부족으로 422여도 “연결 가능”로 분류 가능
- 사용자 입장에서는 기능 실패지만 운영 대시보드에서는 서버 alive로 보일 수 있음

## 신규 개발자 온보딩 가이드 (읽는 순서/핫스팟)

### 1) 읽는 순서 (권장)

1. `app/main.py`
- 어떤 라우터가 어떤 prefix로 붙는지
- startup/shutdown에서 무엇을 하는지

2. `app/core/config.py`
- URL/보안/쿠키/TTS/LLM/Redis 설정이 어디서 오는지

3. `백엔드_API_통합_명세서.md`
- 외부 계약(경로/응답 형식)을 먼저 고정해서 이해

4. 도메인 핵심 라우터
- `app/api/chat.py`
- `app/api/characters.py`
- `app/api/tts.py`
- `app/api/voices.py`
- `app/api/model_make.py`

5. 서비스 레이어
- `app/services/chat_service.py`
- `app/services/character_service.py`
- `app/services/tts_service.py`
- `app/services/server_a_client.py`

6. 상세 내부 기준서
- `백엔드_함수_분석_및_역할_분석_명세서.md`

### 2) 핫스팟(자주 수정/회귀 위험 높은 곳)

- `chat.py` / `chat_service.py`
  - 응답 shape + fallback + SSE 계약
- `tts.py` / `tts_service.py`
  - 공개 TTS와 내부 채팅 TTS 계약 차이
- `voices.py` / `model_make.py`
  - Server A 프록시 + 운영성 경로 + 파일 경로 정책
- `characters.py`
  - dev/user/admin/preset/AI 생성 혼합 구조
- `app/core/llm.py`
  - Gemini/Ollama 라우팅과 stream/non-stream 차이

### 3) 변경 시 가장 먼저 확인해야 하는 계약

- `/api/chat` 응답 키 (`content`, `usage`, `session_id`, `context_summarized`, `audio_url?`)
- `/api/chat/stream` SSE 프레임 키 (`content`, `done`, `full_content`, `session_id`)
- `/api/tts` JSON/binary 분기
- `/api/voices` 응답이 `success/data` 래퍼형이 아니라는 점
- legacy alias 경로 유지 여부

## 문제 해결 가이드 (증상별 트러블슈팅)

### 증상 1. 로그인은 성공한 것 같은데 `/api/auth/me`가 401

가능 원인:
- 쿠키가 브라우저에 저장되지 않음 (`AUTH_COOKIE_SECURE`, `AUTH_COOKIE_SAMESITE`, 도메인/프로토콜 문제)
- 프론트 요청에 `credentials: include` 누락
- `SECRET_KEY`/JWT 설정 불일치

확인 포인트:
- 브라우저 DevTools 쿠키 저장 여부
- 응답 `Set-Cookie` 헤더
- 백엔드 startup 경고 (`AUTH_COOKIE_SECURE`, `GOOGLE_SSO_ALLOW_INSECURE_HTTP`)
- guest 로그인인 경우 `guest_access_token` 쿠키가 발급되었는지 확인

추가 주의:
- `/api/users/me/settings` 같은 user 전용 경로는 guest 쿠키로는 접근되지 않음(정상)

### 증상 2. `/api/chat`는 되는데 음성이 안 붙음 (`audio_url` 없음)

가능 원인:
- `tts_enabled=false`
- 캐릭터 `voice_id` 미연결 또는 유효하지 않음
- TTS 서버/Server A 장애
- 내부 TTS 실패가 경고로만 흡수됨(채팅 성공은 유지)

확인 포인트:
- `/api/chat` 응답에서 `audio_url` 키 존재 여부
- 백엔드 로그의 `TTS 합성 실패(audio_url 미포함)` 경고
- `/api/tts` 단독 호출 테스트

### 증상 3. `/api/chat/stream`이 JSON 파싱 에러남

가능 원인:
- SSE를 일반 JSON API처럼 호출함

해결:
- `EventSource` 또는 stream reader 사용
- `data:` 프레임 파싱 로직 적용

### 증상 4. model-make upload 후 start가 실패함

가능 원인:
- `version` whitelist 불일치
- `train_input_dir` 잘못 전달
- Server A Training API 연결 실패
- WAV 검증(길이/개수/총용량) 실패

확인 포인트:
- `/api/model-make/upload` 응답의 `train_input_dir` 그대로 사용했는지
- `SERVER_A_TRAINING_API_URL` 설정
- `/api/system/health/detailed`의 `server_a_train` 상태

### 증상 5. `/api/voices/server-files`에서 목록 구조가 예상과 다름

가능 원인:
- Server A 통합 인덱스 API 미지원 -> 백엔드가 `models/train_voices/logs` 개별 조합 fallback 사용

해결/주의:
- 프론트 파일 매니저는 `models`, `train_voices`, `logs` 키 기준으로 파싱하도록 유지

### 증상 6. 개발 환경에서 인증 없이도 일부 경로가 됨

가능 원인:
- `DEV_AUTH_FALLBACK_ENABLED=true`

설명:
- 개발 편의용 정상 동작일 수 있음. 운영 환경과 동일하게 테스트하려면 `false`로 끄고 검증.

### 증상 7. `/admin/voices` 페이지는 열리는데 실제 작업이 안 되거나 바로 차단됨

가능 원인:
- guest 로그인 상태 또는 일반 사용자 상태
- 프론트 페이지 가드가 `/api/auth/me` 결과를 보고 선차단
- 백엔드 `require_admin`가 최종적으로 `403` 반환

설명:
- F12로 프론트 상태를 바꿔도 백엔드 `require_admin`를 우회할 수는 없음
- 관리자 권한은 `ADMIN_EMAILS` 기반이며 서버가 최종 판단

## 용어 사전 / FAQ

### 용어 사전

- **Server A**: GPU 연산 담당 서비스군 (GPT-SoVITS TTS, 파일 스캐너, 학습 API)
- **Server B**: 본 백엔드(FastAPI). 사용자 경험/DB/오케스트레이션 담당
- **preset 캐릭터**: 파일(JSON) 기반 기본 캐릭터
- **DB 캐릭터**: 사용자가 생성/수정하는 데이터베이스 기반 캐릭터
- **director mode(감독 모드)**: 한 턴에 어느 캐릭터가 말할지 제어하는 대화 모드
- **context summary**: 긴 대화를 압축해 유지하는 세션 요약
- **model-make**: 사용자 음성 모델 제작 파이프라인 (업로드~학습~등록)

### FAQ

#### Q1. 왜 비슷한 경로가 여러 개 있나요? (`/api/story/generate/story` vs `/api/generate/story`)

- 레거시 프론트/도구 호환성 유지 때문입니다. 1차 리팩토링 정책상 path 제거보다 호환성이 우선됩니다.

#### Q2. 왜 어떤 API는 `success/data`이고 어떤 API는 바로 객체를 반환하나요?

- 역사적으로 추가된 도메인이 다르고, 프론트 계약 호환을 유지하는 과정에서 형식이 혼합됐습니다. 문서에서 endpoint별로 구분해 사용해야 합니다.

#### Q3. 왜 실패했는데도 `success=true`가 반환되나요?

- 사용자 경험(서비스가 완전히 멈춘 느낌 방지)을 위해 일부 경로는 fallback 데이터를 반환하도록 설계되어 있습니다.

#### Q4. GPT-SoVITS 코드가 백엔드에 있나요?

- 본 백엔드는 GPT-SoVITS 본체를 직접 구현하지 않고 외부(Server A) API를 호출합니다. 단, Server A 자동화 사이드카 스크립트(파일/학습 API)는 같은 레포에 포함될 수 있습니다.

#### Q5. 채팅 스트리밍은 DB에 저장되나요?

- 현재 정책상 `/chat/stream`는 `/chat`와 저장 우선순위가 다르며, SSE 호환성을 우선합니다. 상세 저장 정책은 `chat.py` 구현 및 정밀 분석 문서를 참조하세요.

## 변경 이력 / 후속 과제

### 이번 라운드 작성 메모

- 정밀 분석 문서의 구조/리스크/호환성 메모를 “사용자 경험/시스템 이해” 관점으로 재구성함
- 프론트/기획/운영이 헷갈리기 쉬운 지점(legacy alias, fallback, 응답 형식 혼합)을 별도 섹션으로 분리함
- model-make / voices / TTS / chat 흐름을 사용자 여정 기준으로 설명함
- guest 서버 인증(guest 쿠키 + `guest_sessions`/`auth_events`)과 프론트 탭세션 병행 구조를 반영함
- `/admin/voices` 프론트 가드 + 백엔드 `require_admin` 최종 판정 역할 분리를 명시함
- Docker Compose GPT-SoVITS 데이터 볼륨의 `D:/docker/madcamp03` bind mount 정책을 반영함

### 후속 과제 (이해 문서 관점)

1. 실제 프론트 화면(페이지/컴포넌트)과 API 경로 매핑 표를 추가해 화면 기준 탐색성 강화
2. 운영 배포 토폴로지(로컬 Docker / 외부 GPU 서버 / 혼합 모드)별 시퀀스 다이어그램 보강
3. QA용 “fallback expected behavior” 시나리오를 별도 부록으로 분리
4. 관리자 운영 매뉴얼(Voice/Server A 파일 관리 UI 기준)을 별도 문서로 독립
