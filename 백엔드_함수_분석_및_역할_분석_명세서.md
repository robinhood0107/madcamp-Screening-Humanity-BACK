# BACK 백엔드 함수 분석 및 역할 분석 명세서

상태: `현재 기준 문서` (코드 기준 베이스라인 재구축)

- 작성일: 2026-02-24
- 수정일: 2026-02-24
- 대상 프로젝트: `madcamp-Screening-Humanity-BACK`
- 분석 기준 브랜치: `dev`
- 분석 기준 커밋: `eed4a01`
- 범위: `app/api`, `app/core`, `app/models`, `app/services`, `app/workers`, `run.py`, `test_ollama_connection.py`
- 제외 범위: `__pycache__`, 가상환경(venv/.venv), generated artifact, `task.md`(현재 작업트리 기준 미존재/삭제 상태)

## 목차

1. [문서 목적 / 사용법](#문서-목적--사용법)
2. [전체 구조 요약](#전체-구조-요약)
3. [라우터/엔드포인트 인벤토리](#라우터엔드포인트-인벤토리)
4. [핫스팟(긴 함수/고위험 구간)](#핫스팟긴-함수고위험-구간)
5. [파일별 상세 분석 + 함수 전수 기록](#파일별-상세-분석--함수-전수-기록)
6. [보안 점검 결과 (1차: 경고+플래그화)](#보안-점검-결과-1차-경고플래그화)
7. [함수 분석 기반 개선 실행 순서](#함수-분석-기반-개선-실행-순서)
8. [회귀 테스트/검증 시나리오](#회귀-테스트검증-시나리오)
9. [변경 이력 / TODO / 후속 과제](#변경-이력--todo--후속-과제)

## 문서 목적 / 사용법

- 이 문서는 **함수/메서드 단위 역할과 호출 맥락을 먼저 이해한 뒤** 안전하게 개선하기 위한 기준서다.
- 목표는 외부 계약(path/method/응답 key/DB 의미/Redis 계약)을 유지하면서 함수 역할/책임 경계를 명확히 파악하고 필요한 개선 포인트를 추적 가능하게 만드는 것이다.
- 현재 문서는 손상된 초대형 문서를 코드 기준으로 **베이스라인 재구축**한 버전이며, 수동 Appendix는 마커 구간 기준으로 보존/누적한다.
- 실환경 회귀 검증은 환경/의존성 제약이 있으면 생략될 수 있으며, 이 경우 문법/논리/문서 정합성 중심으로 먼저 관리한다.

## 전체 구조 요약

- 분석 대상 Python 파일 수: **42개**
- 함수/메서드(전수 기록 대상, class 헤딩 제외 기준) 수: **238개**
- 라우터 엔드포인트 데코레이터 수: **54개**
- Phase 7 Canonical 정적 검증 수치/정합성 결과는 하단 `Phase 7 정합성 재검증 결과 (현재 Canonical, 2026-02-24)` 섹션을 단일 참조로 사용한다.

### 레이어 구분 (현재 구조 기준)

- `app/api/*`: FastAPI 라우터 (transport + 일부 비즈니스 로직 혼재)
- `app/core/*`: 설정/DB/보안/LLM/Redis 인프라
- `app/services/*`: 서비스 로직/도메인 helper/외부 연동 보조
- `app/models/*`: SQLAlchemy ORM 모델
- `app/workers/*`: Redis 큐 기반 워커
- 루트 스크립트: 로컬 실행/연결 점검

### 핵심 구조 진단 (짧게 요약)

- 장점: 기능 범위가 넓고, Settings/라우터 구조 기본 틀이 존재한다.
- 문제: 일부 라우터에 오케스트레이션/외부호출/DB/파일/예외처리가 혼재되어 있다.
- 보수적 개선 전략: **라우터 path 유지 + service/helper 단계적 추출 + 보안 플래그화 + 주석 강화**

### 현재 Phase 진행 상태 (Canonical, 2026-02-24)

| Phase | 상태 | 근거(현재 코드 기준) | 다음 정리 포인트 |
|---|---|---|---|
| Phase 0 | 완료 (문서 내 흡수형 기준선) | 인벤토리/정량 지표/정적 검증 기준이 초대형 문서 Canonical 섹션에 흡수되어 재현 가능 | 수동 Appendix 운영 메모는 비차단 후속 관리 |
| Phase 1 | 완료 (전수 커버리지 + 재생성 체계) | 함수/메서드 전수 기록이 코드 기준으로 재생성되며 AST/헤딩 정합성 0 누락 유지 | 수동 Appendix 메모는 운영 지식 누적용 후속 관리 |
| Phase 2 | 완료 (1차 범위) | `config.py`, `main.py`, `auth.py`, `deps.py` 설정화/경고 플래그/보안 가시화 반영 완료 | 2차 보안 기본값 강화안은 별도 정책 단계 |
| Phase 3 | 완료 (1차 범위) | `server_a_client.py` 도입 + `voices/model_make/tts` 공통화 + `ensure_success_response()` 실사용 반영 | 라우터별 사용자-facing 메시지 세분화는 후속 미세개선 가능 |
| Phase 4 | 완료 (1차 범위) | `tts.py` service 추출, `_synthesize_tts_internal` 축소, `tts_worker` 단계 함수 분해, `tts_queue.py` deprecated stub 정리 반영 | 추가 구조 분해는 2차 최적화 후보 |
| Phase 5 | 완료 (보수적 범위) | `chat_service`/`character_service`/`story_service` 도입 + `chat`/`chat_stream`/`characters` 오케스트레이션 축소 및 섹션 정리 반영 | broad exception 추가 축소는 2차 품질개선 후보 |
| Phase 6 | 완료 (1차 범위) | `llm.py` helper 분리, runtime `print` 정리, `STRICT_STARTUP_VALIDATION` warn-only 연결, 잔여 핫스팟 주석/예외 정책 보강 반영 | 추가 로깅/문구 균질화는 지속 개선 과제 |
| Phase 7 | 완료 (정적/문서 정합성 기준) | Canonical 재생성 + AST/헤딩/라인드리프트 0 + 정적 검증 결과 기록 완료 (하단 Canonical 재검증 섹션 수치와 연결) | 실환경 B/C 회귀 결과는 후속 환경/CI에서 누적 기록 |

## 라우터/엔드포인트 인벤토리

> 참고: 아래 `라인` 컬럼은 빠른 탐색용 스냅샷이다. 함수별 정확 라인 기준은 본 문서의 전수 기록 헤딩(`##### 함수명 (path:line)`)을 우선한다.

| 파일 | 함수 | Method | Path | 라인 |
|---|---|---|---|---|
| `app/api/ai.py` | `generate_story` | POST | `/generate/story` | 32 |
| `app/api/ai.py` | `analyze_story_legacy` | POST | `/story/analyze` | 84 |
| `app/api/ai.py` | `generate_character_details` | POST | `/generate/character-details` | 102 |
| `app/api/auth.py` | `google_login` | GET | `/google/login` | 272 |
| `app/api/auth.py` | `guest_login` | POST | `/guest/login` | 280 |
| `app/api/auth.py` | `google_callback` | GET | `/google/callback` | 351 |
| `app/api/auth.py` | `get_current_user_info` | GET | `/me` | 392 |
| `app/api/auth.py` | `logout` | POST | `/logout` | 422 |
| `app/api/characters.py` | `list_preset_characters` | GET | `/characters/presets` | 198 |
| `app/api/characters.py` | `list_user_characters` | GET | `/characters` | 218 |
| `app/api/characters.py` | `create_character` | POST | `/characters` | 260 |
| `app/api/characters.py` | `list_my_characters` | GET | `/characters/my` | 318 |
| `app/api/characters.py` | `create_my_character` | POST | `/characters/my` | 349 |
| `app/api/characters.py` | `list_admin_characters` | GET | `/characters/admin/all` | 401 |
| `app/api/characters.py` | `update_admin_character_voice` | PATCH | `/characters/admin/{character_id}/voice` | 437 |
| `app/api/characters.py` | `get_character` | GET | `/characters/{character_id}` | 512 |
| `app/api/characters.py` | `update_character` | PUT | `/characters/{character_id}` | 548 |
| `app/api/characters.py` | `delete_character` | DELETE | `/characters/{character_id}` | 621 |
| `app/api/characters.py` | `generate_character_details` | POST | `/generate` | 683 |
| `app/api/chat.py` | `chat` | POST | `/chat` | 515 |
| `app/api/chat.py` | `chat_stream` | POST | `/chat/stream` | 605 |
| `app/api/evaluation.py` | `evaluate_chat` | POST | `/evaluate` | 26 |
| `app/api/generate.py` | `create_generation_job` | POST | `/` | 76 |
| `app/api/generate.py` | `get_job_status` | GET | `/status/{job_id}` | 119 |
| `app/api/model_make.py` | `upload_training_files` | POST | `/upload` | 116 |
| `app/api/model_make.py` | `start_training` | POST | `/start` | 215 |
| `app/api/model_make.py` | `abort_training` | POST | `/abort` | 250 |
| `app/api/model_make.py` | `get_training_status` | GET | `/status/{model_name}` | 289 |
| `app/api/model_make.py` | `get_training_log` | GET | `/log/{model_name}` | 304 |
| `app/api/model_make.py` | `register_voice` | POST | `/register` | 321 |
| `app/api/model_make.py` | `list_my_model_voices` | GET | `/my` | 393 |
| `app/api/model_make.py` | `delete_my_model_voice` | DELETE | `/my/{voice_id}` | 422 |
| `app/api/story.py` | `generate_story` | POST | `/generate/story` | 20 |
| `app/api/system.py` | `check_system_health` | GET | `/health/detailed` | 73 |
| `app/api/tts.py` | `prepare_tts_weights` | POST | `/tts/prepare` | 27 |
| `app/api/tts.py` | `synthesize` | POST | `/tts` | 249 |
| `app/api/tts.py` | `list_voices` | GET | `/tts/voices` | 299 |
| `app/api/users.py` | `get_my_settings` | GET | `/me/settings` | 18 |
| `app/api/users.py` | `put_my_settings` | PUT | `/me/settings` | 32 |
| `app/api/voices.py` | `get_server_files` | GET | `/voices/server-files` | 115 |
| `app/api/voices.py` | `upload_train_voice_file` | POST | `/voices/server-files/upload` | 232 |
| `app/api/voices.py` | `delete_server_file` | DELETE | `/voices/server-files` | 269 |
| `app/api/voices.py` | `create_server_folder` | POST | `/voices/server-files/mkdir` | 283 |
| `app/api/voices.py` | `trim_server_audio` | POST | `/voices/server-files/trim` | 297 |
| `app/api/voices.py` | `get_voice_link_options` | GET | `/voices/link-options` | 319 |
| `app/api/voices.py` | `list_voices` | GET | `/voices` | 343 |
| `app/api/voices.py` | `get_voice` | GET | `/voices/{voice_id}` | 388 |
| `app/api/voices.py` | `create_voice` | POST | `/voices` | 429 |
| `app/api/voices.py` | `update_voice` | PUT | `/voices/{voice_id}` | 503 |
| `app/api/voices.py` | `delete_voice` | DELETE | `/voices/{voice_id}` | 574 |
| `app/api/voices.py` | `test_voice` | POST | `/voices/{voice_id}/test` | 610 |
| `app/api/voices.py` | `start_training_proxy` | POST | `/voices/train/start` | 689 |
| `app/api/voices.py` | `get_training_status_proxy` | GET | `/voices/train/status/{model_name}` | 706 |
| `app/api/voices.py` | `get_training_log_proxy` | GET | `/voices/train/log/{model_name}` | 727 |

## 핫스팟(긴 함수/고위험 구간)

| 순위 | 함수 | 파일 | 길이(줄) | 우선순위 | 이유(요약) |
|---|---|---|---|---|---|
| 1 | `_prepare_chat_request_for_llm` | `app/api/chat.py` | 143 | 상 | 일반함수, 호출/예외/외부연동 복합 가능성 |
| 2 | `generate_character_details` | `app/api/ai.py` | 122 | 상 | 엔드포인트, 호출/예외/외부연동 복합 가능성 |
| 3 | `evaluate_chat` | `app/api/evaluation.py` | 97 | 상 | 엔드포인트, 호출/예외/외부연동 복합 가능성 |
| 4 | `generate_story` | `app/api/story.py` | 96 | 상 | 엔드포인트, 호출/예외/외부연동 복합 가능성 |
| 5 | `upload_training_files` | `app/api/model_make.py` | 91 | 상 | 엔드포인트, 호출/예외/외부연동 복합 가능성 |
| 6 | `chat` | `app/api/chat.py` | 87 | 상 | 엔드포인트, 호출/예외/외부연동 복합 가능성 |
| 7 | `AudioAnalyzer.analyze_audio` | `app/services/audio_analyzer.py` | 84 | 상 | 일반함수, 호출/예외/외부연동 복합 가능성 |
| 8 | `chat_stream` | `app/api/chat.py` | 82 | 상 | 엔드포인트, 호출/예외/외부연동 복합 가능성 |
| 9 | `_get_or_create_google_user_or_raise` | `app/api/auth.py` | 78 | 중 | 일반함수, 호출/예외/외부연동 복합 가능성 |
| 10 | `call_llm_json_with_gemini_fallback` | `app/services/character_service.py` | 76 | 중 | 일반함수, 호출/예외/외부연동 복합 가능성 |
| 11 | `process_ref_audio` | `app/api/voices.py` | 74 | 중 | 일반함수, 호출/예외/외부연동 복합 가능성 |
| 12 | `check_system_health` | `app/api/system.py` | 73 | 상 | 엔드포인트, 호출/예외/외부연동 복합 가능성 |
| 13 | `update_admin_character_voice` | `app/api/characters.py` | 72 | 상 | 엔드포인트, 호출/예외/외부연동 복합 가능성 |
| 14 | `update_character` | `app/api/characters.py` | 71 | 상 | 엔드포인트, 호출/예외/외부연동 복합 가능성 |
| 15 | `create_voice` | `app/api/voices.py` | 71 | 상 | 엔드포인트, 호출/예외/외부연동 복합 가능성 |
| 16 | `guest_login` | `app/api/auth.py` | 69 | 상 | 엔드포인트, 호출/예외/외부연동 복합 가능성 |
| 17 | `save_tts_audio_and_optionally_cache` | `app/services/tts_service.py` | 69 | 중 | 일반함수, 호출/예외/외부연동 복합 가능성 |
| 18 | `update_voice` | `app/api/voices.py` | 68 | 상 | 엔드포인트, 호출/예외/외부연동 복합 가능성 |
| 19 | `call_llm_stream` | `app/core/llm.py` | 68 | 중 | 일반함수, 호출/예외/외부연동 복합 가능성 |
| 20 | `get_current_user` | `app/api/deps.py` | 67 | 중 | 일반함수, 호출/예외/외부연동 복합 가능성 |

## 파일별 상세 분석 + 함수 전수 기록

> 아래는 **전수 기록** 섹션이다. 함수/메서드마다 라인/시그니처/부작용/리스크/테스트 포인트를 코드 기준으로 기록한다.

### `app/api/ai.py`

#### 파일 역할

- FastAPI 라우터 모듈

#### 파일 메타

- 총 라인 수: 225
- 클래스 수: 4
- 함수/메서드 수: 4
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 높음
- 전역 상태/캐시/환경변수 사용(상단 할당): `router`, `logger`
- 외부 계약(엔드포인트/서비스/전역 singleton): `POST /generate/story`, `POST /story/analyze`, `POST /generate/character-details`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `_extract_json` | 12 | 6 | - | 하 |
| `generate_story` | 32 | 50 | POST /generate/story | 중 |
| `analyze_story_legacy` | 84 | 3 | POST /story/analyze | 중 |
| `generate_character_details` | 102 | 122 | POST /generate/character-details | 상 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `StoryGenerationRequest` | 20 | `BaseModel` | 0 |
| `StoryGenerationResponse` | 27 | `BaseModel` | 0 |
| `CharacterGenerationRequest` | 88 | `BaseModel` | 0 |
| `CharacterGenerationResponse` | 96 | `BaseModel` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 라우터 오케스트레이션 단계 주석/예외 정책 정리 + service helper 재사용 확대
- 2차 계획 후보:
  - 실환경 회귀 테스트 자산(TestClient/pytest) 확충 후 예외 메시지 외부 노출 최소화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `_extract_json` (`app/api/ai.py:12`)

- 시그니처: `def _extract_json(content: str) -> Dict[str, Any]`
- 라인 범위: `app/api/ai.py:12` ~ `app/api/ai.py:17` (약 6줄)
- 기존 docstring 요약: LLM 응답에서 JSON 객체를 추출합니다.
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `character_service.extract_json_object_from_llm_text`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `generate_story` (`app/api/ai.py:32`)

- 시그니처: `async def generate_story(request: StoryGenerationRequest)`
- 라인 범위: `app/api/ai.py:32` ~ `app/api/ai.py:81` (약 50줄)
- 데코레이터/엔드포인트: `POST /generate/story`
- 기존 docstring 요약: 사용자의 상황 입력을 바탕으로 드라마틱한 줄거리 생성
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `story_service.build_story_generation_system_prompt`, `story_service.build_story_generation_messages`, `story_service.call_story_llm_with_selection`, `story_service.parse_story_sections`, `logger.error`, `story_service.build_story_mock_plot`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `analyze_story_legacy` (`app/api/ai.py:84`)

- 시그니처: `async def analyze_story_legacy(request: StoryGenerationRequest)`
- 라인 범위: `app/api/ai.py:84` ~ `app/api/ai.py:86` (약 3줄)
- 데코레이터/엔드포인트: `POST /story/analyze`
- 기존 docstring 요약: 구형 엔드포인트 호환용 (/api/story/analyze)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `generate_story`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `generate_character_details` (`app/api/ai.py:102`)

- 시그니처: `async def generate_character_details(request: CharacterGenerationRequest, current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/ai.py:102` ~ `app/api/ai.py:223` (약 122줄)
- 데코레이터/엔드포인트: `POST /generate/character-details`
- 기존 docstring 요약: 이름과 기본 설명을 바탕으로 캐릭터의 상세 설정(페르소나, 성격, 말투, 세계관 등 16종)을 자동 생성
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `user_input_parts.append`, `'\n'.join`, `character_service.call_llm_json_with_gemini_fallback`, `logger.exception`, `HTTPException`, `str`, `_extract_json`, `character_service.normalize_string_list_fields`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 긴 함수(80줄+)
  - broad exception 사용
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인

### `app/api/auth.py`

#### 파일 역할

- FastAPI 인증/OAuth 라우터 (cookie/JWT/redirect 처리)

#### 파일 메타

- 총 라인 수: 481
- 클래스 수: 1
- 함수/메서드 수: 12
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 높음
- 전역 상태/캐시/환경변수 사용(상단 할당): `router`, `logger`, `sso`
- 외부 계약(엔드포인트/서비스/전역 singleton): `GET /google/login`, `POST /guest/login`, `GET /google/callback`, `GET /me`, `POST /logout`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `_sanitize_guest_display_name` | 40 | 5 | - | 하 |
| `_build_auth_principal_response` | 47 | 29 | - | 하 |
| `_set_guest_auth_cookie` | 78 | 12 | - | 하 |
| `_verify_google_user_info_or_raise` | 92 | 34 | - | 하 |
| `_get_or_create_google_user_or_raise` | 128 | 78 | - | 중 |
| `_create_access_token_or_raise` | 208 | 22 | - | 하 |
| `_build_auth_callback_redirect_response` | 232 | 38 | - | 하 |
| `google_login` | 272 | 5 | GET /google/login | 중 |
| `guest_login` | 280 | 69 | POST /guest/login | 상 |
| `google_callback` | 351 | 39 | GET /google/callback | 중 |
| `get_current_user_info` | 392 | 28 | GET /me | 중 |
| `logout` | 422 | 60 | POST /logout | 상 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `GuestLoginRequest` | 36 | `BaseModel` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - `google_callback` 단계 helper 경계 유지, 쿠키/OAuth 설정 [보안 민감] 주석 보강
- 2차 계획 후보:
  - 운영 환경 기본 보안값 강화(dev fallback/insecure 옵션 기본 비활성)
  - 실환경 회귀 테스트 자산(TestClient/pytest) 확충 후 예외 메시지 외부 노출 최소화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `_sanitize_guest_display_name` (`app/api/auth.py:40`)

- 시그니처: `def _sanitize_guest_display_name(raw_name: Optional[str]) -> str`
- 라인 범위: `app/api/auth.py:40` ~ `app/api/auth.py:44` (약 5줄)
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `(raw_name or '').strip`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_build_auth_principal_response` (`app/api/auth.py:47`)

- 시그니처: `def _build_auth_principal_response(principal: AuthPrincipal) -> dict`
- 라인 범위: `app/api/auth.py:47` ~ `app/api/auth.py:75` (약 29줄)
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `settings.is_admin`, `HTTPException`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_set_guest_auth_cookie` (`app/api/auth.py:78`)

- 시그니처: `def _set_guest_auth_cookie(response: JSONResponse, *, token: str) -> None`
- 라인 범위: `app/api/auth.py:78` ~ `app/api/auth.py:89` (약 12줄)
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `response.set_cookie`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 쿠키 설정/삭제
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_verify_google_user_info_or_raise` (`app/api/auth.py:92`)

- 시그니처: `async def _verify_google_user_info_or_raise(request: Request) -> Any`
- 라인 범위: `app/api/auth.py:92` ~ `app/api/auth.py:125` (약 34줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `asyncio.wait_for`, `sso.verify_and_process`, `logger.warning`, `HTTPException`, `logger.error`, `str`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_get_or_create_google_user_or_raise` (`app/api/auth.py:128`)

- 시그니처: `async def _get_or_create_google_user_or_raise(*, db: AsyncSession, user_info: Any) -> User`
- 라인 범위: `app/api/auth.py:128` ~ `app/api/auth.py:205` (약 78줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `db.execute`, `select(User).where`, `select`, `result.scalar_one_or_none`, `logger.error`, `str`, `HTTPException`, `User`, `uuid.uuid4`, `user_email.split`, `db.add`, `db.commit`, `db.refresh`, `logger.info`, `db.rollback`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_create_access_token_or_raise` (`app/api/auth.py:208`)

- 시그니처: `def _create_access_token_or_raise(*, user_id: str) -> str`
- 라인 범위: `app/api/auth.py:208` ~ `app/api/auth.py:229` (약 22줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `create_access_token`, `logger.error`, `str`, `HTTPException`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_build_auth_callback_redirect_response` (`app/api/auth.py:232`)

- 시그니처: `def _build_auth_callback_redirect_response(*, access_token: str) -> RedirectResponse`
- 라인 범위: `app/api/auth.py:232` ~ `app/api/auth.py:269` (약 38줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `RedirectResponse`, `response.set_cookie`, `response.delete_cookie`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 쿠키 설정/삭제
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `google_login` (`app/api/auth.py:272`)

- 시그니처: `async def google_login()`
- 라인 범위: `app/api/auth.py:272` ~ `app/api/auth.py:276` (약 5줄)
- 데코레이터/엔드포인트: `GET /google/login`
- 기존 docstring 요약: Redirect user to Google Login
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `sso.get_login_redirect`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `guest_login` (`app/api/auth.py:280`)

- 시그니처: `async def guest_login(body: GuestLoginRequest, request: Request, db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/auth.py:280` ~ `app/api/auth.py:348` (약 69줄)
- 데코레이터/엔드포인트: `POST /guest/login`
- 기존 docstring 요약: 프론트 게스트 시작 요청을 서버 guest 세션/쿠키로 확장한다.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `log_auth_event`, `HTTPException`, `_sanitize_guest_display_name`, `datetime.utcnow`, `GuestSession`, `str`, `uuid.uuid4`, `timedelta`, `request.headers.get('x-forwarded-for', '').split(',')[0].strip`, `request.headers.get('x-forwarded-for', '').split`, `request.headers.get`, `db.add`, `db.commit`, `db.refresh`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `google_callback` (`app/api/auth.py:351`)

- 시그니처: `async def google_callback(request: Request, db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/auth.py:351` ~ `app/api/auth.py:389` (약 39줄)
- 데코레이터/엔드포인트: `GET /google/callback`
- 기존 docstring 요약: Process login response from Google and return JWT
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `_verify_google_user_info_or_raise`, `HTTPException`, `_get_or_create_google_user_or_raise`, `_create_access_token_or_raise`, `log_auth_event`, `_build_auth_callback_redirect_response`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `get_current_user_info` (`app/api/auth.py:392`)

- 시그니처: `async def get_current_user_info(request: Request, db: AsyncSession=Depends(get_db), principal: Optional[AuthPrincipal]=Depends(get_current_principal_optional))`
- 라인 범위: `app/api/auth.py:392` ~ `app/api/auth.py:419` (약 28줄)
- 데코레이터/엔드포인트: `GET /me`
- 기존 docstring 요약: 현재 로그인한 사용자(google/guest) 정보 조회
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `HTTPException`, `datetime.utcnow`, `db.commit`, `db.rollback`, `logger.exception`, `_build_auth_principal_response`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `logout` (`app/api/auth.py:422`)

- 시그니처: `async def logout(request: Request, db: AsyncSession=Depends(get_db), principal: Optional[AuthPrincipal]=Depends(get_current_principal_optional))`
- 라인 범위: `app/api/auth.py:422` ~ `app/api/auth.py:481` (약 60줄)
- 데코레이터/엔드포인트: `POST /logout`
- 기존 docstring 요약: 로그아웃 - 쿠키에서 토큰 삭제
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `datetime.utcnow`, `db.commit`, `db.rollback`, `logger.exception`, `log_auth_event`, `RedirectResponse`, `response.delete_cookie`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 로그 출력
  - 쿠키 설정/삭제
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인

### `app/api/characters.py`

#### 파일 역할

- FastAPI 캐릭터 CRUD + preset/AI 생성 혼합 라우터

#### 파일 메타

- 총 라인 수: 735
- 클래스 수: 6
- 함수/메서드 수: 21
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 높음
- 전역 상태/캐시/환경변수 사용(상단 할당): `router`, `logger`
- 외부 계약(엔드포인트/서비스/전역 singleton): `GET /characters/presets`, `GET /characters`, `POST /characters`, `GET /characters/my`, `POST /characters/my`, `GET /characters/admin/all`, `PATCH /characters/admin/{character_id}/voice`, `GET /characters/{character_id}`, `PUT /characters/{character_id}`, `DELETE /characters/{character_id}`, `POST /generate`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `_resolve_preset_characters_dir` | 18 | 10 | - | 하 |
| `load_preset_characters` | 30 | 6 | - | 하 |
| `_serialize_character_response_data` | 38 | 10 | - | 하 |
| `_ensure_not_preset_character` | 50 | 16 | - | 하 |
| `_get_db_character_or_404` | 68 | 13 | - | 하 |
| `_get_owned_character_or_raise` | 83 | 18 | - | 하 |
| `_raise_bad_request` | 103 | 14 | - | 하 |
| `_raise_internal_server_error` | 119 | 13 | - | 하 |
| `_rollback_and_raise_internal_server_error` | 134 | 17 | - | 하 |
| `list_preset_characters` | 198 | 18 | GET /characters/presets | 중 |
| `list_user_characters` | 218 | 40 | GET /characters | 중 |
| `create_character` | 260 | 51 | POST /characters | 중 |
| `list_my_characters` | 318 | 28 | GET /characters/my | 중 |
| `create_my_character` | 349 | 44 | POST /characters/my | 중 |
| `list_admin_characters` | 401 | 33 | GET /characters/admin/all | 중 |
| `update_admin_character_voice` | 437 | 72 | PATCH /characters/admin/{character_id}/voice | 상 |
| `get_character` | 512 | 34 | GET /characters/{character_id} | 중 |
| `update_character` | 548 | 71 | PUT /characters/{character_id} | 상 |
| `delete_character` | 621 | 55 | DELETE /characters/{character_id} | 중 |
| `generate_character_details` | 683 | 53 | POST /generate | 중 |
| `parse_json_response` | 709 | 5 | - | 하 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `CharacterBase` | 153 | `BaseModel` | 0 |
| `CharacterCreate` | 162 | `CharacterBase` | 0 |
| `CharacterUpdate` | 165 | `BaseModel` | 0 |
| `GenerateRequest` | 174 | `BaseModel` | 0 |
| `AdminCharacterListItem` | 181 | `BaseModel` | 0 |
| `AdminCharacterVoiceUpdate` | 189 | `BaseModel` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 라우터 오케스트레이션 단계 주석/예외 정책 정리 + service helper 재사용 확대
- 2차 계획 후보:
  - 실환경 회귀 테스트 자산(TestClient/pytest) 확충 후 예외 메시지 외부 노출 최소화
  - 도메인 라우터 추가 분해(transport vs orchestration 완전 분리)

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `_resolve_preset_characters_dir` (`app/api/characters.py:18`)

- 시그니처: `def _resolve_preset_characters_dir()`
- 라인 범위: `app/api/characters.py:18` ~ `app/api/characters.py:27` (약 10줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `character_service.resolve_preset_characters_dir`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `load_preset_characters` (`app/api/characters.py:30`)

- 시그니처: `def load_preset_characters() -> List[Dict[str, Any]]`
- 라인 범위: `app/api/characters.py:30` ~ `app/api/characters.py:35` (약 6줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `character_service.load_preset_characters_from_disk`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_serialize_character_response_data` (`app/api/characters.py:38`)

- 시그니처: `def _serialize_character_response_data(character: Character) -> Dict[str, Any]`
- 라인 범위: `app/api/characters.py:38` ~ `app/api/characters.py:47` (약 10줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `character_service.serialize_character_model`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_ensure_not_preset_character` (`app/api/characters.py:50`)

- 시그니처: `def _ensure_not_preset_character(*, preset_chars: List[Dict[str, Any]], character_id: str, error_message: str) -> None`
- 라인 범위: `app/api/characters.py:50` ~ `app/api/characters.py:65` (약 16줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `character_service.find_preset_character_by_id`, `HTTPException`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_get_db_character_or_404` (`app/api/characters.py:68`)

- 시그니처: `async def _get_db_character_or_404(*, db: AsyncSession, character_id: str) -> Character`
- 라인 범위: `app/api/characters.py:68` ~ `app/api/characters.py:80` (약 13줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `db.execute`, `select(Character).where`, `select`, `result.scalar_one_or_none`, `HTTPException`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_get_owned_character_or_raise` (`app/api/characters.py:83`)

- 시그니처: `async def _get_owned_character_or_raise(*, db: AsyncSession, character_id: str, current_user: User, forbidden_message: str='캐릭터를 수정할 권한이 없습니다') -> Character`
- 라인 범위: `app/api/characters.py:83` ~ `app/api/characters.py:100` (약 18줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_get_db_character_or_404`, `HTTPException`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_raise_bad_request` (`app/api/characters.py:103`)

- 시그니처: `def _raise_bad_request(detail_prefix: str, exc: Exception, *, log_context: str) -> None`
- 라인 범위: `app/api/characters.py:103` ~ `app/api/characters.py:116` (약 14줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `logger.warning`, `HTTPException`, `str`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_raise_internal_server_error` (`app/api/characters.py:119`)

- 시그니처: `def _raise_internal_server_error(detail_prefix: str, exc: Exception, *, log_context: str) -> None`
- 라인 범위: `app/api/characters.py:119` ~ `app/api/characters.py:131` (약 13줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `logger.exception`, `HTTPException`, `str`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_rollback_and_raise_internal_server_error` (`app/api/characters.py:134`)

- 시그니처: `async def _rollback_and_raise_internal_server_error(*, db: AsyncSession, detail_prefix: str, exc: Exception, log_context: str) -> None`
- 라인 범위: `app/api/characters.py:134` ~ `app/api/characters.py:150` (약 17줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `db.rollback`, `logger.warning`, `_raise_internal_server_error`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `list_preset_characters` (`app/api/characters.py:198`)

- 시그니처: `async def list_preset_characters()`
- 라인 범위: `app/api/characters.py:198` ~ `app/api/characters.py:215` (약 18줄)
- 데코레이터/엔드포인트: `GET /characters/presets`
- 기존 docstring 요약: 사전설정 캐릭터 목록 조회
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `load_preset_characters`, `_raise_internal_server_error`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `list_user_characters` (`app/api/characters.py:218`)

- 시그니처: `async def list_user_characters(db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/characters.py:218` ~ `app/api/characters.py:257` (약 40줄)
- 데코레이터/엔드포인트: `GET /characters`
- 기존 docstring 요약: [DEVELOPMENT ONLY] 개발 환경: 인증 없이 캐릭터 목록 조회 (dev-user)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `db.execute`, `select(Character).where(Character.user_id == 'dev-user', Character.is_preset == False).order_by`, `select(Character).where`, `select`, `Character.created_at.desc`, `result.scalars().all`, `result.scalars`, `_serialize_character_response_data`, `_raise_internal_server_error`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `create_character` (`app/api/characters.py:260`)

- 시그니처: `async def create_character(character: CharacterCreate, db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/characters.py:260` ~ `app/api/characters.py:310` (약 51줄)
- 데코레이터/엔드포인트: `POST /characters`
- 기존 docstring 요약: [DEVELOPMENT ONLY] 개발 환경용: 인증 없이 캐릭터 생성 (임시 user_id="dev-user" 사용)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `character_service.build_character_model_for_create`, `db.add`, `db.commit`, `db.refresh`, `_serialize_character_response_data`, `db.rollback`, `_raise_bad_request`, `_rollback_and_raise_internal_server_error`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `list_my_characters` (`app/api/characters.py:318`)

- 시그니처: `async def list_my_characters(db: AsyncSession=Depends(get_db), current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/characters.py:318` ~ `app/api/characters.py:345` (약 28줄)
- 데코레이터/엔드포인트: `GET /characters/my`
- 기존 docstring 요약: 내 캐릭터 페르소나 목록 (user_id=me, DB only, is_preset=False)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `db.execute`, `select(Character).where(Character.user_id == current_user.id, Character.is_preset == False).order_by`, `select(Character).where`, `select`, `Character.created_at.desc`, `result.scalars().all`, `result.scalars`, `_serialize_character_response_data`, `_raise_internal_server_error`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `create_my_character` (`app/api/characters.py:349`)

- 시그니처: `async def create_my_character(character: CharacterCreate, db: AsyncSession=Depends(get_db), current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/characters.py:349` ~ `app/api/characters.py:392` (약 44줄)
- 데코레이터/엔드포인트: `POST /characters/my`
- 기존 docstring 요약: 내 캐릭터 생성 (user_id=current_user.id)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `character_service.build_character_model_for_create`, `db.add`, `db.commit`, `db.refresh`, `_serialize_character_response_data`, `db.rollback`, `_raise_bad_request`, `_rollback_and_raise_internal_server_error`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `list_admin_characters` (`app/api/characters.py:401`)

- 시그니처: `async def list_admin_characters(db: AsyncSession=Depends(get_db), current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/characters.py:401` ~ `app/api/characters.py:433` (약 33줄)
- 데코레이터/엔드포인트: `GET /characters/admin/all`
- 기존 docstring 요약: 관리자: DB + Preset 캐릭터 통합 목록 (voice 연결/교체용)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `load_preset_characters`, `db.execute`, `select(Character).order_by`, `select`, `Character.created_at.desc`, `result.scalars().all`, `result.scalars`, `character_service.build_admin_character_list_payload`, `AdminCharacterListItem(**item).model_dump`, `AdminCharacterListItem`, `_raise_internal_server_error`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `update_admin_character_voice` (`app/api/characters.py:437`)

- 시그니처: `async def update_admin_character_voice(character_id: str, body: AdminCharacterVoiceUpdate, db: AsyncSession=Depends(get_db), current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/characters.py:437` ~ `app/api/characters.py:508` (약 72줄)
- 데코레이터/엔드포인트: `PATCH /characters/admin/{character_id}/voice`
- 기존 docstring 요약: 관리자: 캐릭터 voice_id만 수정 (DB 또는 Preset JSON). null=연결 해제.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `character_service.load_preset_character_by_id_from_disk`, `character_service.update_preset_character_voice_id`, `str`, `HTTPException`, `detail.split(':', 1)[-1].strip`, `detail.split`, `_get_db_character_or_404`, `db.commit`, `db.refresh`, `db.rollback`, `_raise_bad_request`, `_rollback_and_raise_internal_server_error`, `router.patch`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `get_character` (`app/api/characters.py:512`)

- 시그니처: `async def get_character(character_id: str, db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/characters.py:512` ~ `app/api/characters.py:545` (약 34줄)
- 데코레이터/엔드포인트: `GET /characters/{character_id}`
- 기존 docstring 요약: 특정 캐릭터 상세 조회 (인증 없음)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `character_service.load_preset_character_by_id_from_disk`, `dict`, `_get_db_character_or_404`, `_serialize_character_response_data`, `_raise_internal_server_error`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `update_character` (`app/api/characters.py:548`)

- 시그니처: `async def update_character(character_id: str, character_update: CharacterUpdate, db: AsyncSession=Depends(get_db), current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/characters.py:548` ~ `app/api/characters.py:618` (약 71줄)
- 데코레이터/엔드포인트: `PUT /characters/{character_id}`
- 기존 docstring 요약: 캐릭터 수정 (본인 소유 확인)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `load_preset_characters`, `_ensure_not_preset_character`, `_get_owned_character_or_raise`, `character_update.model_dump`, `character_service.encode_tags_for_storage`, `update_data.items`, `setattr`, `db.commit`, `db.refresh`, `_serialize_character_response_data`, `db.rollback`, `_raise_bad_request`, `_rollback_and_raise_internal_server_error`, `router.put`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `delete_character` (`app/api/characters.py:621`)

- 시그니처: `async def delete_character(character_id: str, db: AsyncSession=Depends(get_db), current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/characters.py:621` ~ `app/api/characters.py:675` (약 55줄)
- 데코레이터/엔드포인트: `DELETE /characters/{character_id}`
- 기존 docstring 요약: 캐릭터 삭제 (본인 소유 확인)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `load_preset_characters`, `_ensure_not_preset_character`, `_get_owned_character_or_raise`, `db.execute`, `delete(Character).where`, `delete`, `db.commit`, `_rollback_and_raise_internal_server_error`, `router.delete`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `generate_character_details` (`app/api/characters.py:683`)

- 시그니처: `async def generate_character_details(request: GenerateRequest, current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/characters.py:683` ~ `app/api/characters.py:735` (약 53줄)
- 데코레이터/엔드포인트: `POST /generate`
- 기존 docstring 요약: AI를 사용하여 캐릭터의 상세 정보(페르소나, 말투 등)를 생성합니다.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `character_service.extract_json_object_from_llm_text`, `character_service.call_llm_json_with_gemini_fallback`, `parse_json_response`, `character_service.normalize_string_list_fields`, `logger.warning`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `parse_json_response` (`app/api/characters.py:709`)

- 시그니처: `def parse_json_response(text: str) -> Dict[str, Any]`
- 라인 범위: `app/api/characters.py:709` ~ `app/api/characters.py:713` (약 5줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 외부 함수 내부 로컬 helper 호출
- 호출 대상(정적 분석 추정, 상위 15개): `character_service.extract_json_object_from_llm_text`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - nested helper (문맥 의존성 점검 필요)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/api/chat.py`

#### 파일 역할

- FastAPI 채팅/스트리밍 오케스트레이션 라우터

#### 파일 메타

- 총 라인 수: 686
- 클래스 수: 2
- 함수/메서드 수: 11
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 높음
- 전역 상태/캐시/환경변수 사용(상단 할당): `router`, `logger`, `_BAN_POLYSEMY`
- 외부 계약(엔드포인트/서비스/전역 singleton): `POST /chat`, `POST /chat/stream`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `truncate_to_sentence` | 24 | 40 | - | 중 |
| `format_for_first_dialogue` | 66 | 36 | - | 하 |
| `format_persona_for_actor` | 104 | 46 | - | 중 |
| `get_character_by_id` | 152 | 20 | - | 하 |
| `_resolve_speaker_name_for_response` | 197 | 23 | - | 하 |
| `_run_chat_turn_llm_and_persist` | 222 | 43 | - | 중 |
| `_maybe_attach_tts_audio_url` | 267 | 49 | - | 중 |
| `_generate_chat_stream_sse_events` | 318 | 50 | - | 중 |
| `_prepare_chat_request_for_llm` | 370 | 143 | - | 상 |
| `chat` | 515 | 87 | POST /chat | 상 |
| `chat_stream` | 605 | 82 | POST /chat/stream | 상 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `Message` | 174 | `BaseModel` | 0 |
| `ChatRequest` | 178 | `BaseModel` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 라우터 오케스트레이션 단계 주석/예외 정책 정리 + service helper 재사용 확대
- 2차 계획 후보:
  - 실환경 회귀 테스트 자산(TestClient/pytest) 확충 후 예외 메시지 외부 노출 최소화
  - 도메인 라우터 추가 분해(transport vs orchestration 완전 분리)

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `truncate_to_sentence` (`app/api/chat.py:24`)

- 시그니처: `def truncate_to_sentence(text: str, max_len: int) -> str`
- 라인 범위: `app/api/chat.py:24` ~ `app/api/chat.py:63` (약 40줄)
- 기존 docstring 요약: 문장 종결형으로 깔끔하게 자르는 유틸 함수.
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `len`, `range`, `truncated.rfind`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `format_for_first_dialogue` (`app/api/chat.py:66`)

- 시그니처: `def format_for_first_dialogue(character_name: str, persona: str, opponent: str, situation: str, background: Optional[str]=None) -> tuple`
- 라인 범위: `app/api/chat.py:66` ~ `app/api/chat.py:101` (약 36줄)
- 기존 docstring 요약: 주연 모드 첫 대사 전용: (system_prompt, user_message) 반환.
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정): 구현 본문 기준 내부 로직/상위 호출 위주
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `format_persona_for_actor` (`app/api/chat.py:104`)

- 시그니처: `def format_persona_for_actor(character_name: str, persona: str, partner_name: str, situation: str, turn_count: int=0, director_note: Optional[str]=None, summary: Optional[str]=None, background: Optional[str]=None, is_director_mode: bool=False) -> str`
- 라인 범위: `app/api/chat.py:104` ~ `app/api/chat.py:149` (약 46줄)
- 기존 docstring 요약: 4k 컨텍스트 최적화: 압축된 시스템 프롬프트 (~1.2k 토큰 목표)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정): 구현 본문 기준 내부 로직/상위 호출 위주
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_character_by_id` (`app/api/chat.py:152`)

- 시그니처: `async def get_character_by_id(character_id: str, user_id: str, db: AsyncSession) -> Optional[Character]`
- 라인 범위: `app/api/chat.py:152` ~ `app/api/chat.py:171` (약 20줄)
- 기존 docstring 요약: 캐릭터 정보 조회 (사전설정 또는 사용자 소유)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `db.execute`, `select(Character).where`, `select`, `result.scalar_one_or_none`, `logger.warning`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_resolve_speaker_name_for_response` (`app/api/chat.py:197`)

- 시그니처: `def _resolve_speaker_name_for_response(*, speaker_name: Optional[str], resolved_character_name: Optional[str], opponent: str, is_director_mode: bool, current_speaker: Optional[str]) -> str`
- 라인 범위: `app/api/chat.py:197` ~ `app/api/chat.py:219` (약 23줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정): 구현 본문 기준 내부 로직/상위 호출 위주
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_run_chat_turn_llm_and_persist` (`app/api/chat.py:222`)

- 시그니처: `async def _run_chat_turn_llm_and_persist(*, db: AsyncSession, session_id: str, request: ChatRequest, chat_messages: List[Dict[str, str]], system_instruction: Optional[str], speaker_name: str) -> tuple[str, Dict[str, Any]]`
- 라인 범위: `app/api/chat.py:222` ~ `app/api/chat.py:264` (약 43줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `call_llm`, `chat_service.sanitize_assistant_reply_content`, `chat_service.persist_chat_turn_messages`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_maybe_attach_tts_audio_url` (`app/api/chat.py:267`)

- 시그니처: `async def _maybe_attach_tts_audio_url(*, response_data: Dict[str, Any], content: str, request: ChatRequest, current_user: Optional[User], db: AsyncSession) -> None`
- 라인 범위: `app/api/chat.py:267` ~ `app/api/chat.py:315` (약 49줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `(content or '').strip`, `character_service.resolve_character_voice_id_for_tts`, `chat_service.synthesize_chat_tts_audio_url`, `logger.warning`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_generate_chat_stream_sse_events` (`app/api/chat.py:318`)

- 시그니처: `async def _generate_chat_stream_sse_events(*, chat_messages: List[Dict[str, str]], request: ChatRequest, system_instruction: Optional[str], session_id: str)`
- 라인 범위: `app/api/chat.py:318` ~ `app/api/chat.py:367` (약 50줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `call_llm_stream`, `json.dumps`, `logger.info`, `len`, `logger.error`, `str`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_prepare_chat_request_for_llm` (`app/api/chat.py:370`)

- 시그니처: `async def _prepare_chat_request_for_llm(*, request: ChatRequest, db: AsyncSession, session_id: str) -> Dict[str, Any]`
- 라인 범위: `app/api/chat.py:370` ~ `app/api/chat.py:512` (약 143줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `context_manager.get_summary`, `chat_service.extract_scenario_fields`, `len`, `bool`, `chat_service.normalize_request_messages_for_context`, `context_manager.manage_context`, `getattr`, `truncate_to_sentence`, `chat_service.parse_director_turn_context`, `format_persona_for_actor`, `messages.append`, `logger.error`, `chat_service.resolve_character_identity`, `format_for_first_dialogue`, `chat_service.append_history_messages`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 긴 함수(80줄+)
  - broad exception 사용
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `chat` (`app/api/chat.py:515`)

- 시그니처: `async def chat(request: ChatRequest, db: AsyncSession=Depends(get_db), current_user: Optional[User]=Depends(get_current_user_optional), principal: Optional[AuthPrincipal]=Depends(get_current_principal_optional))`
- 라인 범위: `app/api/chat.py:515` ~ `app/api/chat.py:601` (약 87줄)
- 데코레이터/엔드포인트: `POST /chat`
- 기존 docstring 요약: 배우 모드: AI가 한 캐릭터로서 한 턴만 응답
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `str`, `uuid.uuid4`, `logger.info`, `_prepare_chat_request_for_llm`, `_resolve_speaker_name_for_response`, `_run_chat_turn_llm_and_persist`, `_maybe_attach_tts_audio_url`, `logger.error`, `HTTPException`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 긴 함수(80줄+)
  - broad exception 사용
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `chat_stream` (`app/api/chat.py:605`)

- 시그니처: `async def chat_stream(request: ChatRequest, db: AsyncSession=Depends(get_db), current_user: Optional[User]=Depends(get_current_user_optional), principal: Optional[AuthPrincipal]=Depends(get_current_principal_optional))`
- 라인 범위: `app/api/chat.py:605` ~ `app/api/chat.py:686` (약 82줄)
- 데코레이터/엔드포인트: `POST /chat/stream`
- 기존 docstring 요약: SSE 스트리밍 채팅 엔드포인트.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `str`, `uuid.uuid4`, `logger.info`, `chat_service.extract_scenario_fields`, `len`, `chat_service.resolve_character_identity`, `format_persona_for_actor`, `messages.append`, `chat_service.normalize_request_messages_for_context`, `chat_service.split_system_and_chat_messages`, `StreamingResponse`, `_generate_chat_stream_sse_events`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 긴 함수(80줄+)
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인

### `app/api/deps.py`

#### 파일 역할

- FastAPI 라우터 모듈

#### 파일 메타

- 총 라인 수: 255
- 클래스 수: 1
- 함수/메서드 수: 11
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `oauth2_scheme`, `logger`
- 외부 계약(엔드포인트/서비스/전역 singleton): 전역 singleton/상태: `logger`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `_decode_subject_from_token` | 29 | 7 | - | 하 |
| `_parse_subject` | 38 | 11 | - | 하 |
| `get_token_from_request` | 51 | 11 | - | 하 |
| `get_guest_token_from_request` | 64 | 5 | - | 하 |
| `get_any_auth_tokens_from_request` | 71 | 2 | - | 하 |
| `_get_user_from_token` | 75 | 9 | - | 하 |
| `_get_guest_session_from_token` | 86 | 34 | - | 하 |
| `get_current_principal_optional` | 122 | 15 | - | 하 |
| `get_current_user` | 139 | 67 | - | 중 |
| `get_current_user_optional` | 208 | 19 | - | 하 |
| `require_admin` | 229 | 26 | - | 하 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `AuthPrincipal` | 23 | - | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 운영 환경 기본 보안값 강화(dev fallback/insecure 옵션 기본 비활성)

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `_decode_subject_from_token` (`app/api/deps.py:29`)

- 시그니처: `def _decode_subject_from_token(token: str) -> Optional[str]`
- 라인 범위: `app/api/deps.py:29` ~ `app/api/deps.py:35` (약 7줄)
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `jwt.decode`, `payload.get`, `str`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_parse_subject` (`app/api/deps.py:38`)

- 시그니처: `def _parse_subject(subject: Optional[str]) -> tuple[Optional[str], Optional[str]]`
- 라인 범위: `app/api/deps.py:38` ~ `app/api/deps.py:48` (약 11줄)
- 기존 docstring 요약: Returns (kind, id).
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `subject.startswith`, `subject[len(GUEST_SUBJECT_PREFIX):].strip`, `len`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_token_from_request` (`app/api/deps.py:51`)

- 시그니처: `async def get_token_from_request(request: Request) -> Optional[str]`
- 라인 범위: `app/api/deps.py:51` ~ `app/api/deps.py:61` (약 11줄)
- 기존 docstring 요약: 쿠키(사용자 토큰) 또는 Authorization 헤더에서 토큰 추출.
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `request.cookies.get`, `request.headers.get`, `authorization.startswith`, `authorization.split`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_guest_token_from_request` (`app/api/deps.py:64`)

- 시그니처: `async def get_guest_token_from_request(request: Request) -> Optional[str]`
- 라인 범위: `app/api/deps.py:64` ~ `app/api/deps.py:68` (약 5줄)
- 기존 docstring 요약: 게스트 전용 쿠키에서 토큰 추출.
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `request.cookies.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_any_auth_tokens_from_request` (`app/api/deps.py:71`)

- 시그니처: `async def get_any_auth_tokens_from_request(request: Request) -> tuple[Optional[str], Optional[str]]`
- 라인 범위: `app/api/deps.py:71` ~ `app/api/deps.py:72` (약 2줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `get_token_from_request`, `get_guest_token_from_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_get_user_from_token` (`app/api/deps.py:75`)

- 시그니처: `async def _get_user_from_token(*, token: Optional[str], db: AsyncSession) -> Optional[User]`
- 라인 범위: `app/api/deps.py:75` ~ `app/api/deps.py:83` (약 9줄)
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_decode_subject_from_token`, `_parse_subject`, `db.execute`, `select(User).where`, `select`, `result.scalar_one_or_none`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_get_guest_session_from_token` (`app/api/deps.py:86`)

- 시그니처: `async def _get_guest_session_from_token(*, token: Optional[str], db: AsyncSession, mark_expired: bool=False) -> Optional[GuestSession]`
- 라인 범위: `app/api/deps.py:86` ~ `app/api/deps.py:119` (약 34줄)
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_decode_subject_from_token`, `_parse_subject`, `db.execute`, `select(GuestSession).where`, `select`, `result.scalar_one_or_none`, `expires_at.replace`, `datetime.utcnow`, `db.commit`, `db.rollback`, `logger.exception`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_current_principal_optional` (`app/api/deps.py:122`)

- 시그니처: `async def get_current_principal_optional(request: Request, db: AsyncSession=Depends(get_db)) -> Optional[AuthPrincipal]`
- 라인 범위: `app/api/deps.py:122` ~ `app/api/deps.py:136` (약 15줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `get_any_auth_tokens_from_request`, `_get_user_from_token`, `AuthPrincipal`, `_get_guest_session_from_token`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_current_user` (`app/api/deps.py:139`)

- 시그니처: `async def get_current_user(request: Request, db: AsyncSession=Depends(get_db)) -> User`
- 라인 범위: `app/api/deps.py:139` ~ `app/api/deps.py:205` (약 67줄)
- 기존 docstring 요약: 현재 사용자 조회 (쿠키 또는 Authorization 헤더에서 토큰 읽기)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `HTTPException`, `get_any_auth_tokens_from_request`, `_decode_subject_from_token`, `_parse_subject`, `db.execute`, `select(User).where`, `select`, `result.scalar_one_or_none`, `logger.warning`, `logger.info`, `User`, `db.add`, `db.commit`, `db.refresh`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 로그 출력
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_current_user_optional` (`app/api/deps.py:208`)

- 시그니처: `async def get_current_user_optional(request: Request, db: AsyncSession=Depends(get_db)) -> Optional[User]`
- 라인 범위: `app/api/deps.py:208` ~ `app/api/deps.py:226` (약 19줄)
- 기존 docstring 요약: 현재 사용자 조회. 토큰 없거나 만료/유효하지 않으면 None 반환 (HTTPException 미발생).
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `get_token_from_request`, `_decode_subject_from_token`, `_parse_subject`, `db.execute`, `select(User).where`, `select`, `result.scalar_one_or_none`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `require_admin` (`app/api/deps.py:229`)

- 시그니처: `async def require_admin(request: Request, current_user: User=Depends(get_current_user)) -> User`
- 라인 범위: `app/api/deps.py:229` ~ `app/api/deps.py:254` (약 26줄)
- 기존 docstring 요약: 관리자 권한 확인 의존성.
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `logger.warning`, `HTTPException`, `settings.is_admin`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/api/evaluation.py`

#### 파일 역할

- FastAPI 라우터 모듈

#### 파일 메타

- 총 라인 수: 122
- 클래스 수: 2
- 함수/메서드 수: 1
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `router`, `logger`
- 외부 계약(엔드포인트/서비스/전역 singleton): `POST /evaluate`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `evaluate_chat` | 26 | 97 | POST /evaluate | 상 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `Message` | 14 | `BaseModel` | 0 |
| `EvaluationRequest` | 18 | `BaseModel` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 실환경 회귀 테스트 자산(TestClient/pytest) 확충 후 예외 메시지 외부 노출 최소화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `evaluate_chat` (`app/api/evaluation.py:26`)

- 시그니처: `async def evaluate_chat(request: EvaluationRequest, db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/evaluation.py:26` ~ `app/api/evaluation.py:122` (약 97줄)
- 데코레이터/엔드포인트: `POST /evaluate`
- 기존 docstring 요약: [역할]
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `'\n'.join`, `call_llm`, `isinstance`, `result.get`, `content.split('```json')[1].split('```')[0].strip`, `content.split('```json')[1].split`, `content.split`, `content.split('```')[1].split('```')[0].strip`, `content.split('```')[1].split`, `json.loads`, `context_manager.get_summary`, `context_manager.summarize_dialogue`, `m.dict`, `context_manager.save_summary`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 긴 함수(80줄+)
  - broad exception 사용
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인

### `app/api/generate.py`

#### 파일 역할

- FastAPI 라우터 모듈

#### 파일 메타

- 총 라인 수: 139
- 클래스 수: 0
- 함수/메서드 수: 3
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `router`
- 외부 계약(엔드포인트/서비스/전역 singleton): `POST /`, `GET /status/{job_id}`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `process_generation_task` | 17 | 56 | - | 중 |
| `create_generation_job` | 76 | 41 | POST / | 중 |
| `get_job_status` | 119 | 21 | GET /status/{job_id} | 중 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 실환경 회귀 테스트 자산(TestClient/pytest) 확충 후 예외 메시지 외부 노출 최소화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `process_generation_task` (`app/api/generate.py:17`)

- 시그니처: `async def process_generation_task(job_id: str, input_path: str, db_session: AsyncSession)`
- 라인 범위: `app/api/generate.py:17` ~ `app/api/generate.py:72` (약 56줄)
- 기존 docstring 요약: Mock/Real background task to process 3D generation.
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `AsyncSessionLocal`, `db.execute`, `select(GenerationJob).where`, `select`, `result.scalar_one_or_none`, `db.commit`, `asyncio.sleep`, `os.path.join`, `os.makedirs`, `os.path.dirname`, `open`, `f.write`, `func.now`, `str`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 파일/디렉터리 I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `create_generation_job` (`app/api/generate.py:76`)

- 시그니처: `async def create_generation_job(background_tasks: BackgroundTasks, image: UploadFile=File(...), db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/generate.py:76` ~ `app/api/generate.py:116` (약 41줄)
- 데코레이터/엔드포인트: `POST /`
- 기존 docstring 요약: Start implicit async generation job (인증 없이 사용 가능)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `File`, `Depends`, `str`, `uuid.uuid4`, `os.makedirs`, `os.path.join`, `open`, `shutil.copyfileobj`, `GenerationJob`, `json.dumps`, `db.add`, `db.commit`, `db.refresh`, `background_tasks.add_task`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 파일/디렉터리 I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `get_job_status` (`app/api/generate.py:119`)

- 시그니처: `async def get_job_status(job_id: str, db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/generate.py:119` ~ `app/api/generate.py:139` (약 21줄)
- 데코레이터/엔드포인트: `GET /status/{job_id}`
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `db.execute`, `select(GenerationJob).where`, `select`, `result.scalar_one_or_none`, `HTTPException`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인

### `app/api/model_make.py`

#### 파일 역할

- FastAPI 라우터 모듈

#### 파일 메타

- 총 라인 수: 463
- 클래스 수: 3
- 함수/메서드 수: 10
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 높음
- 전역 상태/캐시/환경변수 사용(상단 할당): `router`, `logger`, `MAX_TOTAL_BYTES`, `MIN_DURATION_SEC`, `MODEL_VERSIONS_WHITELIST`
- 외부 계약(엔드포인트/서비스/전역 singleton): `POST /upload`, `POST /start`, `POST /abort`, `GET /status/{model_name}`, `GET /log/{model_name}`, `POST /register`, `GET /my`, `DELETE /my/{voice_id}`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `_get_duration_sec` | 59 | 10 | - | 하 |
| `_resolve_ref_audio_file_with_prepare_fallback` | 71 | 40 | - | 중 |
| `upload_training_files` | 116 | 91 | POST /upload | 상 |
| `start_training` | 215 | 30 | POST /start | 중 |
| `abort_training` | 250 | 34 | POST /abort | 중 |
| `get_training_status` | 289 | 12 | GET /status/{model_name} | 중 |
| `get_training_log` | 304 | 12 | GET /log/{model_name} | 중 |
| `register_voice` | 321 | 67 | POST /register | 상 |
| `list_my_model_voices` | 393 | 24 | GET /my | 중 |
| `delete_my_model_voice` | 422 | 42 | DELETE /my/{voice_id} | 중 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `ModelMakeStartRequest` | 33 | `BaseModel` | 0 |
| `ModelMakeAbortRequest` | 41 | `BaseModel` | 0 |
| `ModelMakeRegisterRequest` | 48 | `BaseModel` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - Server A/외부 HTTP 예외 매핑 공통화 유지 및 중복 분기 축소
- 2차 계획 후보:
  - 실환경 회귀 테스트 자산(TestClient/pytest) 확충 후 예외 메시지 외부 노출 최소화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `_get_duration_sec` (`app/api/model_make.py:59`)

- 시그니처: `def _get_duration_sec(data: bytes, filename: str) -> Optional[float]`
- 라인 범위: `app/api/model_make.py:59` ~ `app/api/model_make.py:68` (약 10줄)
- 기존 docstring 요약: mutagen으로 WAV 재생시간(초) 반환. 실패 시 None.
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `WAVE`, `io.BytesIO`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_resolve_ref_audio_file_with_prepare_fallback` (`app/api/model_make.py:71`)

- 시그니처: `async def _resolve_ref_audio_file_with_prepare_fallback(*, train_input_dir: str, original_ref_audio_file: str) -> str`
- 라인 범위: `app/api/model_make.py:71` ~ `app/api/model_make.py:110` (약 40줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `server_a_client.prepare_ref_audio`, `logger.warning`, `prepare_resp.json`, `prepare_data.get`, `logger.info`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `upload_training_files` (`app/api/model_make.py:116`)

- 시그니처: `async def upload_training_files(files: List[UploadFile]=File(..., description='WAV 파일 3개 이상 (files 또는 files[])'), current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/model_make.py:116` ~ `app/api/model_make.py:206` (약 91줄)
- 데코레이터/엔드포인트: `POST /upload`
- 기존 docstring 요약: 훈련용 WAV 업로드.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `File`, `Depends`, `len`, `HTTPException`, `(f.filename or '').lower().endswith`, `(f.filename or '').lower`, `', '.join`, `f.read`, `items.append`, `_get_duration_sec`, `unreadable.append`, `short.append`, `int`, `time.time_ns`, `getattr`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 긴 함수(80줄+)
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `start_training` (`app/api/model_make.py:215`)

- 시그니처: `async def start_training(body: ModelMakeStartRequest, current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/model_make.py:215` ~ `app/api/model_make.py:244` (약 30줄)
- 데코레이터/엔드포인트: `POST /start`
- 기존 docstring 요약: 학습 시작. upload_path = SERVER_A_TRAIN_VOICE_ROOT / train_input_dir 로 Server A /api/train/start 호출.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `HTTPException`, `', '.join`, `sorted`, `root.rstrip`, `body.train_input_dir.lstrip`, `server_a_client.start_training`, `server_a_client.ensure_success_response`, `r.json`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `abort_training` (`app/api/model_make.py:250`)

- 시그니처: `async def abort_training(body: ModelMakeAbortRequest, current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/model_make.py:250` ~ `app/api/model_make.py:283` (약 34줄)
- 데코레이터/엔드포인트: `POST /abort`
- 기존 docstring 요약: 모델 제작 중단(트랜잭션 롤백): 업로드 음성(train_input_dir), logs, TEMP 삭제.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `root_train.rstrip`, `body.train_input_dir.lstrip`, `server_a_client.delete_file`, `body.model_name.strip`, `base.rstrip`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `get_training_status` (`app/api/model_make.py:289`)

- 시그니처: `async def get_training_status(model_name: str, current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/model_make.py:289` ~ `app/api/model_make.py:300` (약 12줄)
- 데코레이터/엔드포인트: `GET /status/{model_name}`
- 기존 docstring 요약: 학습 상태 조회 (Server A /api/train/status/{model_name} 프록시).
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `server_a_client.get_training_status`, `server_a_client.ensure_success_response`, `r.json`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `get_training_log` (`app/api/model_make.py:304`)

- 시그니처: `async def get_training_log(model_name: str, current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/model_make.py:304` ~ `app/api/model_make.py:315` (약 12줄)
- 데코레이터/엔드포인트: `GET /log/{model_name}`
- 기존 docstring 요약: 학습 로그 조회 (Server A /api/train/log/{model_name} 프록시).
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `server_a_client.get_training_log`, `server_a_client.ensure_success_response`, `r.json`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `register_voice` (`app/api/model_make.py:321`)

- 시그니처: `async def register_voice(body: ModelMakeRegisterRequest, db: AsyncSession=Depends(get_db), current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/model_make.py:321` ~ `app/api/model_make.py:387` (약 67줄)
- 데코레이터/엔드포인트: `POST /register`
- 기존 docstring 요약: 학습 완료 후 Voice 등록. ref_audio_path = SERVER_A_TRAIN_VOICE_ROOT/train_input_dir/ref_audio_file. gpt/sovits 미지정 시 GET /api/files/logs에서 model_name 매칭으로 자동 추론.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `_resolve_ref_audio_file_with_prepare_fallback`, `root.rstrip`, `body.train_input_dir.lstrip`, `server_a_client.get_files_logs`, `r.json`, `data.get`, `(m.get('model_name') or '').strip`, `m.get`, `(body.model_name or '').strip`, `Voice`, `str`, `uuid.uuid4`, `db.add`, `db.commit`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `list_my_model_voices` (`app/api/model_make.py:393`)

- 시그니처: `async def list_my_model_voices(db: AsyncSession=Depends(get_db), current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/model_make.py:393` ~ `app/api/model_make.py:416` (약 24줄)
- 데코레이터/엔드포인트: `GET /my`
- 기존 docstring 요약: 내 모델 제작 음성 목록 (user_id=me, train_input_dir·training_model_name 포함).
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `select(Voice).where(Voice.user_id == current_user.id).order_by`, `select(Voice).where`, `select`, `Voice.created_at.desc`, `db.execute`, `res.scalars().all`, `res.scalars`, `getattr`, `v.created_at.isoformat`, `len`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `delete_my_model_voice` (`app/api/model_make.py:422`)

- 시그니처: `async def delete_my_model_voice(voice_id: str, db: AsyncSession=Depends(get_db), current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/model_make.py:422` ~ `app/api/model_make.py:463` (약 42줄)
- 데코레이터/엔드포인트: `DELETE /my/{voice_id}`
- 기존 docstring 요약: 내 모델 제작 음성 삭제.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `db.execute`, `select(Voice).where`, `select`, `res.scalar_one_or_none`, `HTTPException`, `Character.__table__.update().where(Character.voice_id == voice_id).values`, `Character.__table__.update().where`, `Character.__table__.update`, `getattr`, `root_train.rstrip`, `train_dir.lstrip`, `server_a_client.delete_file`, `root_logs.rstrip`, `db.delete`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인

### `app/api/story.py`

#### 파일 역할

- FastAPI 라우터 모듈

#### 파일 메타

- 총 라인 수: 115
- 클래스 수: 1
- 함수/메서드 수: 1
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `router`, `logger`
- 외부 계약(엔드포인트/서비스/전역 singleton): `POST /generate/story`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `generate_story` | 20 | 96 | POST /generate/story | 상 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `StoryGenerationRequest` | 14 | `BaseModel` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 실환경 회귀 테스트 자산(TestClient/pytest) 확충 후 예외 메시지 외부 노출 최소화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `generate_story` (`app/api/story.py:20`)

- 시그니처: `async def generate_story(request: StoryGenerationRequest, db: AsyncSession=Depends(get_db), current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/story.py:20` ~ `app/api/story.py:115` (약 96줄)
- 데코레이터/엔드포인트: `POST /generate/story`
- 기존 docstring 요약: [역할]
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `call_llm`, `isinstance`, `result.get`, `content.split('```json')[1].split('```')[0].strip`, `content.split('```json')[1].split`, `content.split`, `content.split('```')[1].split('```')[0].strip`, `content.split('```')[1].split`, `json.loads`, `data.get`, `Scenario`, `db.add`, `db.commit`, `db.refresh`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 긴 함수(80줄+)
  - broad exception 사용
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인

### `app/api/system.py`

#### 파일 역할

- FastAPI 라우터 모듈

#### 파일 메타

- 총 라인 수: 145
- 클래스 수: 0
- 함수/메서드 수: 3
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `router`
- 외부 계약(엔드포인트/서비스/전역 singleton): `GET /health/detailed`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `_health_message` | 23 | 17 | - | 하 |
| `check_http_service` | 42 | 29 | - | 하 |
| `check_system_health` | 73 | 73 | GET /health/detailed | 상 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 실환경 회귀 테스트 자산(TestClient/pytest) 확충 후 예외 메시지 외부 노출 최소화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `_health_message` (`app/api/system.py:23`)

- 시그니처: `def _health_message(status_code: int, is_ok: bool) -> str`
- 라인 범위: `app/api/system.py:23` ~ `app/api/system.py:39` (약 17줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정): 구현 본문 기준 내부 로직/상위 호출 위주
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `check_http_service` (`app/api/system.py:42`)

- 시그니처: `async def check_http_service(url: str, timeout: float=2.0) -> dict`
- 라인 범위: `app/api/system.py:42` ~ `app/api/system.py:70` (약 29줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `time.time`, `httpx.AsyncClient`, `client.get`, `round`, `_health_message`, `str`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `check_system_health` (`app/api/system.py:73`)

- 시그니처: `async def check_system_health(db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/system.py:73` ~ `app/api/system.py:145` (약 73줄)
- 데코레이터/엔드포인트: `GET /health/detailed`
- 기존 docstring 요약: [역할]
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `time.time`, `db.execute`, `text`, `round`, `str`, `settings.TTS_BASE_URL.rstrip`, `settings.TTS_API_PATH.lstrip`, `settings.SERVER_A_FILES_API_URL.rstrip`, `settings.SERVER_A_TRAINING_API_URL.rstrip`, `check_http_service`, `asyncio.gather`, `enumerate`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인

### `app/api/tts.py`

#### 파일 역할

- FastAPI TTS 라우터 (합성/voice 조회, 내부 service orchestration)

#### 파일 메타

- 총 라인 수: 325
- 클래스 수: 2
- 함수/메서드 수: 9
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 높음
- 전역 상태/캐시/환경변수 사용(상단 할당): `router`, `logger`, `_voices_config`
- 외부 계약(엔드포인트/서비스/전역 singleton): `POST /tts/prepare`, `POST /tts`, `GET /tts/voices`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `prepare_tts_weights` | 27 | 56 | POST /tts/prepare | 중 |
| `load_voices_config` | 87 | 23 | - | 하 |
| `get_ref_audio_path` | 111 | 21 | - | 하 |
| `get_voice_from_db` | 134 | 6 | - | 하 |
| `TTSRequest.validate_text` | 176 | 4 | - | 하 |
| `TTSRequest.model_dump_for_gpt_sovits` | 181 | 3 | - | 하 |
| `_synthesize_tts_internal` | 186 | 60 | - | 중 |
| `synthesize` | 249 | 47 | POST /tts | 중 |
| `list_voices` | 299 | 27 | GET /tts/voices | 중 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `TTSPrepareRequest` | 22 | `BaseModel` | 0 |
| `TTSRequest` | 142 | `BaseModel` | 2 |

#### 1차/2차 개선 계획

- 1차 계획:
  - Server A/외부 HTTP 예외 매핑 공통화 유지 및 중복 분기 축소
- 2차 계획 후보:
  - 실환경 회귀 테스트 자산(TestClient/pytest) 확충 후 예외 메시지 외부 노출 최소화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `prepare_tts_weights` (`app/api/tts.py:27`)

- 시그니처: `async def prepare_tts_weights(request: TTSPrepareRequest, db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/tts.py:27` ~ `app/api/tts.py:82` (약 56줄)
- 데코레이터/엔드포인트: `POST /tts/prepare`
- 기존 docstring 요약: 채팅 시작 전 TTS 모델 가중치를 미리 로드합니다.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `get_voice_from_db`, `get_ref_audio_path`, `logger.warning`, `voice_data.get`, `server_a_client.set_tts_gpt_weights`, `logger.info`, `logger.error`, `logger.exception`, `server_a_client.set_tts_sovits_weights`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `load_voices_config` (`app/api/tts.py:87`)

- 시그니처: `def load_voices_config()`
- 라인 범위: `app/api/tts.py:87` ~ `app/api/tts.py:109` (약 23줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `Path`, `open`, `json.load`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 파일/디렉터리 I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_ref_audio_path` (`app/api/tts.py:111`)

- 시그니처: `def get_ref_audio_path(voice_id: Optional[str]=None) -> Optional[str]`
- 라인 범위: `app/api/tts.py:111` ~ `app/api/tts.py:131` (약 21줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `load_voices_config`, `config.get`, `voice.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_voice_from_db` (`app/api/tts.py:134`)

- 시그니처: `async def get_voice_from_db(voice_id: str, db: AsyncSession) -> Optional[Dict[str, Any]]`
- 라인 범위: `app/api/tts.py:134` ~ `app/api/tts.py:139` (약 6줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `tts_service.get_voice_from_db_dict`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `validate_text` (`app/api/tts.py:176`)

- 시그니처: `def validate_text(cls, v)`
- 라인 범위: `app/api/tts.py:176` ~ `app/api/tts.py:179` (약 4줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `v.strip`, `ValueError`, `validator`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `model_dump_for_gpt_sovits` (`app/api/tts.py:181`)

- 시그니처: `def model_dump_for_gpt_sovits(self) -> Dict[str, Any]`
- 라인 범위: `app/api/tts.py:181` ~ `app/api/tts.py:183` (약 3줄)
- 기존 docstring 요약: Server A로 보낼 요청 바디 (가중치 경로 제외)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `self.model_dump`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_synthesize_tts_internal` (`app/api/tts.py:186`)

- 시그니처: `async def _synthesize_tts_internal(request: TTSRequest, current_user: Optional[User], db: AsyncSession) -> Dict[str, Any]`
- 라인 범위: `app/api/tts.py:186` ~ `app/api/tts.py:245` (약 60줄)
- 기존 docstring 요약: TTS 합성 내부 함수 (Chat API 등에서 사용).
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `tts_service.resolve_tts_request_defaults`, `tts_service.validate_tts_request_ready`, `tts_service.lookup_cached_tts_audio_response`, `tts_service.call_tts_binary_or_raise`, `tts_service.save_tts_audio_and_optionally_cache`, `HTTPException`, `str`, `logger.exception`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `synthesize` (`app/api/tts.py:249`)

- 시그니처: `async def synthesize(request: TTSRequest, db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/tts.py:249` ~ `app/api/tts.py:295` (약 47줄)
- 데코레이터/엔드포인트: `POST /tts`
- 기존 docstring 요약: 공개 TTS 엔드포인트.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `tts_service.resolve_tts_request_defaults`, `tts_service.validate_tts_request_ready`, `tts_service.call_tts_binary_or_raise`, `Response`, `tts_service.media_type_for_audio_format`, `tts_service.build_public_tts_json_response`, `HTTPException`, `str`, `logger.exception`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `list_voices` (`app/api/tts.py:299`)

- 시그니처: `async def list_voices(db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/tts.py:299` ~ `app/api/tts.py:325` (약 27줄)
- 데코레이터/엔드포인트: `GET /tts/voices`
- 기존 docstring 요약: 사용 가능한 음성 목록 조회
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `db.execute`, `select(Voice).where(Voice.is_active == True).order_by`, `select(Voice).where`, `select`, `Voice.is_default.desc`, `result.scalars().all`, `result.scalars`, `voices.append`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인

### `app/api/users.py`

#### 파일 역할

- FastAPI 라우터 모듈

#### 파일 메타

- 총 라인 수: 58
- 클래스 수: 0
- 함수/메서드 수: 2
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `router`, `ALLOWED_SETTINGS_KEYS`
- 외부 계약(엔드포인트/서비스/전역 singleton): `GET /me/settings`, `PUT /me/settings`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `get_my_settings` | 18 | 11 | GET /me/settings | 중 |
| `put_my_settings` | 32 | 27 | PUT /me/settings | 중 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 실환경 회귀 테스트 자산(TestClient/pytest) 확충 후 예외 메시지 외부 노출 최소화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `get_my_settings` (`app/api/users.py:18`)

- 시그니처: `async def get_my_settings(current_user: User=Depends(get_current_user), db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/users.py:18` ~ `app/api/users.py:28` (약 11줄)
- 데코레이터/엔드포인트: `GET /me/settings`
- 기존 docstring 요약: 로그인 사용자의 settings JSON 반환. 없으면 {}.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `db.execute`, `select(UserPreference).where`, `select`, `r.scalar_one_or_none`, `isinstance`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `put_my_settings` (`app/api/users.py:32`)

- 시그니처: `async def put_my_settings(body: Dict[str, Any], current_user: User=Depends(get_current_user), db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/users.py:32` ~ `app/api/users.py:58` (약 27줄)
- 데코레이터/엔드포인트: `PUT /me/settings`
- 기존 docstring 요약: body에 포함된 키만 기존 settings에 병합. 허용 키: tts_mode, tts_delay_ms, tts_streaming_mode, tts_enabled, tts_speed.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `body.items`, `db.execute`, `select(UserPreference).where`, `select`, `r.scalar_one_or_none`, `isinstance`, `UserPreference`, `dict`, `db.add`, `db.commit`, `db.refresh`, `cur.update`, `router.put`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인

### `app/api/voices.py`

#### 파일 역할

- FastAPI 라우터 모듈

#### 파일 메타

- 총 라인 수: 744
- 클래스 수: 8
- 함수/메서드 수: 16
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 높음
- 전역 상태/캐시/환경변수 사용(상단 할당): `router`, `logger`, `_ALLOWED_AUDIO_EXT`
- 외부 계약(엔드포인트/서비스/전역 singleton): `GET /voices/server-files`, `POST /voices/server-files/upload`, `DELETE /voices/server-files`, `POST /voices/server-files/mkdir`, `POST /voices/server-files/trim`, `GET /voices/link-options`, `GET /voices`, `GET /voices/{voice_id}`, `POST /voices`, `PUT /voices/{voice_id}`, `DELETE /voices/{voice_id}`, `POST /voices/{voice_id}/test`, `POST /voices/train/start`, `GET /voices/train/status/{model_name}`, `GET /voices/train/log/{model_name}`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `get_server_files` | 115 | 34 | GET /voices/server-files | 중 |
| `process_ref_audio` | 155 | 74 | - | 중 |
| `upload_train_voice_file` | 232 | 34 | POST /voices/server-files/upload | 중 |
| `delete_server_file` | 269 | 11 | DELETE /voices/server-files | 중 |
| `create_server_folder` | 283 | 11 | POST /voices/server-files/mkdir | 중 |
| `trim_server_audio` | 297 | 17 | POST /voices/server-files/trim | 중 |
| `get_voice_link_options` | 319 | 19 | GET /voices/link-options | 중 |
| `list_voices` | 343 | 42 | GET /voices | 중 |
| `get_voice` | 388 | 36 | GET /voices/{voice_id} | 중 |
| `create_voice` | 429 | 71 | POST /voices | 상 |
| `update_voice` | 503 | 68 | PUT /voices/{voice_id} | 상 |
| `delete_voice` | 574 | 33 | DELETE /voices/{voice_id} | 중 |
| `test_voice` | 610 | 61 | POST /voices/{voice_id}/test | 상 |
| `start_training_proxy` | 689 | 14 | POST /voices/train/start | 중 |
| `get_training_status_proxy` | 706 | 18 | GET /voices/train/status/{model_name} | 중 |
| `get_training_log_proxy` | 727 | 18 | GET /voices/train/log/{model_name} | 중 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `VoiceBase` | 26 | `BaseModel` | 0 |
| `VoiceCreateRequest` | 45 | `VoiceBase` | 0 |
| `VoiceUpdateRequest` | 51 | `BaseModel` | 0 |
| `VoiceResponse` | 69 | `BaseModel` | 0 |
| `VoiceLinkOption` | 93 | `BaseModel` | 0 |
| `VoiceListResponse` | 101 | `BaseModel` | 0 |
| `VoiceTestRequest` | 107 | `BaseModel` | 0 |
| `TrainStartRequest` | 675 | `BaseModel` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - Server A/외부 HTTP 예외 매핑 공통화 유지 및 중복 분기 축소
- 2차 계획 후보:
  - 실환경 회귀 테스트 자산(TestClient/pytest) 확충 후 예외 메시지 외부 노출 최소화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- 라우터 prefix/legacy alias 경로 호환성 유지

#### 함수별 상세 분석 (전수)

##### `get_server_files` (`app/api/voices.py:115`)

- 시그니처: `async def get_server_files(current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/voices.py:115` ~ `app/api/voices.py:148` (약 34줄)
- 데코레이터/엔드포인트: `GET /voices/server-files`
- 기존 docstring 요약: Server A의 GPT-SoVITS 파일 목록 조회 (관리자 전용)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `server_a_client.get_files_index`, `response.json`, `server_a_client.get_files_models`, `server_a_client.get_files_train_voices`, `server_a_client.get_files_logs`, `models_res.json`, `train_voices_res.json`, `logs_res.json`, `HTTPException`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `process_ref_audio` (`app/api/voices.py:155`)

- 시그니처: `async def process_ref_audio(path: str, train_input_dir: Optional[str]=None) -> str`
- 라인 범위: `app/api/voices.py:155` ~ `app/api/voices.py:228` (약 74줄)
- 기존 docstring 요약: Server A에 오디오 파일 검증 요청.
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `server_a_client.validate_ref_audio`, `HTTPException`, `resp.json`, `data.get`, `Path`, `ValueError`, `server_a_client.prepare_ref_audio`, `prepare_resp.json`, `prep_data.get`, `str`, `f'{parent_dir}/{new_filename}'.replace`, `isinstance`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 파일/디렉터리 I/O 가능성
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `upload_train_voice_file` (`app/api/voices.py:232`)

- 시그니처: `async def upload_train_voice_file(file: UploadFile=File(...), sub_path: str=Form(..., description='sample_train_voice 하위 폴더명(캐릭터명)'), current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/voices.py:232` ~ `app/api/voices.py:265` (약 34줄)
- 데코레이터/엔드포인트: `POST /voices/server-files/upload`
- 기존 docstring 요약: 훈련 데이터(train_voice) 전용 업로드. 오디오(.wav, .mp3, .flac, .ogg)만 허용.
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `File`, `Form`, `Depends`, `HTTPException`, `file.filename.rsplit('.', 1)[-1].lower`, `file.filename.rsplit`, `', '.join`, `server_a_client.upload_train_voice_file`, `server_a_client.ensure_success_response`, `response.json`, `str`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `delete_server_file` (`app/api/voices.py:269`)

- 시그니처: `async def delete_server_file(path: str, current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/voices.py:269` ~ `app/api/voices.py:279` (약 11줄)
- 데코레이터/엔드포인트: `DELETE /voices/server-files`
- 기존 docstring 요약: Server A 파일/폴더 삭제 (관리자 전용)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `server_a_client.delete_file`, `server_a_client.ensure_success_response`, `response.json`, `router.delete`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `create_server_folder` (`app/api/voices.py:283`)

- 시그니처: `async def create_server_folder(path: str=Form(...), current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/voices.py:283` ~ `app/api/voices.py:293` (약 11줄)
- 데코레이터/엔드포인트: `POST /voices/server-files/mkdir`
- 기존 docstring 요약: Server A 훈련 음성 폴더 생성 (관리자 전용)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Form`, `Depends`, `server_a_client.mkdir`, `server_a_client.ensure_success_response`, `response.json`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `trim_server_audio` (`app/api/voices.py:297`)

- 시그니처: `async def trim_server_audio(path: str=Form(..., description='원본 파일 경로 (Server A 기준)'), max_duration_sec: float=Form(9.0, description='목표 길이 (초)'), current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/voices.py:297` ~ `app/api/voices.py:313` (약 17줄)
- 데코레이터/엔드포인트: `POST /voices/server-files/trim`
- 기존 docstring 요약: Server A 오디오 자르기 (관리자 전용)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Form`, `Depends`, `server_a_client.trim_audio`, `server_a_client.ensure_success_response`, `response.json`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `get_voice_link_options` (`app/api/voices.py:319`)

- 시그니처: `async def get_voice_link_options(db: AsyncSession=Depends(get_db), current_user: User=Depends(get_current_user))`
- 라인 범위: `app/api/voices.py:319` ~ `app/api/voices.py:337` (약 19줄)
- 데코레이터/엔드포인트: `GET /voices/link-options`
- 기존 docstring 요약: 캐릭터 voice_id 선택용: DB Voice만. { id, name, gpt_weights_path?, sovits_weights_path? }
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `select(Voice).where((Voice.user_id == current_user.id) | Voice.user_id.is_(None)).where(Voice.is_active == True).order_by`, `select(Voice).where((Voice.user_id == current_user.id) | Voice.user_id.is_(None)).where`, `select(Voice).where`, `select`, `Voice.user_id.is_`, `db.execute`, `res.scalars().all`, `res.scalars`, `VoiceLinkOption`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `list_voices` (`app/api/voices.py:343`)

- 시그니처: `async def list_voices(active_only: bool=True, db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/voices.py:343` ~ `app/api/voices.py:384` (약 42줄)
- 데코레이터/엔드포인트: `GET /voices`
- 기존 docstring 요약: 활성화된 음성 목록 조회 (공개 API)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `select`, `query.where`, `query.order_by`, `Voice.is_default.desc`, `db.execute`, `result.scalars().all`, `result.scalars`, `voice_responses.append`, `VoiceResponse`, `getattr`, `voice.created_at.isoformat`, `voice.updated_at.isoformat`, `VoiceListResponse`, `len`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `get_voice` (`app/api/voices.py:388`)

- 시그니처: `async def get_voice(voice_id: str, db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/voices.py:388` ~ `app/api/voices.py:423` (약 36줄)
- 데코레이터/엔드포인트: `GET /voices/{voice_id}`
- 기존 docstring 요약: 특정 음성 조회
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `db.execute`, `select(Voice).where`, `select`, `result.scalar_one_or_none`, `HTTPException`, `VoiceResponse`, `getattr`, `voice.created_at.isoformat`, `voice.updated_at.isoformat`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `create_voice` (`app/api/voices.py:429`)

- 시그니처: `async def create_voice(request: VoiceCreateRequest, db: AsyncSession=Depends(get_db), current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/voices.py:429` ~ `app/api/voices.py:499` (약 71줄)
- 데코레이터/엔드포인트: `POST /voices`
- 기존 docstring 요약: 새 음성 등록 (관리자 전용)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `db.execute`, `Voice.__table__.update().where(Voice.is_default == True).values`, `Voice.__table__.update().where`, `Voice.__table__.update`, `process_ref_audio`, `Voice`, `str`, `uuid.uuid4`, `getattr`, `db.add`, `db.commit`, `db.refresh`, `VoiceResponse`, `voice.created_at.isoformat`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `update_voice` (`app/api/voices.py:503`)

- 시그니처: `async def update_voice(voice_id: str, request: VoiceUpdateRequest, db: AsyncSession=Depends(get_db), current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/voices.py:503` ~ `app/api/voices.py:570` (약 68줄)
- 데코레이터/엔드포인트: `PUT /voices/{voice_id}`
- 기존 docstring 요약: 음성 정보 수정 (관리자 전용)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `db.execute`, `select(Voice).where`, `select`, `result.scalar_one_or_none`, `HTTPException`, `Voice.__table__.update().where(Voice.is_default == True, Voice.id != voice_id).values`, `Voice.__table__.update().where`, `Voice.__table__.update`, `getattr`, `process_ref_audio`, `request.model_dump`, `update_data.items`, `setattr`, `db.commit`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `delete_voice` (`app/api/voices.py:574`)

- 시그니처: `async def delete_voice(voice_id: str, permanent: bool=False, db: AsyncSession=Depends(get_db), current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/voices.py:574` ~ `app/api/voices.py:606` (약 33줄)
- 데코레이터/엔드포인트: `DELETE /voices/{voice_id}`
- 기존 docstring 요약: 음성 삭제 (관리자 전용)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `db.execute`, `select(Voice).where`, `select`, `result.scalar_one_or_none`, `HTTPException`, `db.delete`, `db.commit`, `router.delete`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `test_voice` (`app/api/voices.py:610`)

- 시그니처: `async def test_voice(voice_id: str, request: VoiceTestRequest, db: AsyncSession=Depends(get_db))`
- 라인 범위: `app/api/voices.py:610` ~ `app/api/voices.py:670` (약 61줄)
- 데코레이터/엔드포인트: `POST /voices/{voice_id}/test`
- 기존 docstring 요약: 음성 테스트 (TTS 생성)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `db.execute`, `select(Voice).where`, `select`, `result.scalar_one_or_none`, `HTTPException`, `server_a_client.call_tts`, `server_a_client.ensure_success_response`, `base64.b64encode(audio_content).decode`, `base64.b64encode`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `start_training_proxy` (`app/api/voices.py:689`)

- 시그니처: `async def start_training_proxy(request: TrainStartRequest, current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/voices.py:689` ~ `app/api/voices.py:702` (약 14줄)
- 데코레이터/엔드포인트: `POST /voices/train/start`
- 기존 docstring 요약: 학습 시작 (파록시)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `server_a_client.start_training`, `request.dict`, `server_a_client.ensure_success_response`, `response.json`, `router.post`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `get_training_status_proxy` (`app/api/voices.py:706`)

- 시그니처: `async def get_training_status_proxy(model_name: str, current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/voices.py:706` ~ `app/api/voices.py:723` (약 18줄)
- 데코레이터/엔드포인트: `GET /voices/train/status/{model_name}`
- 기존 docstring 요약: 학습 상태 조회 (파록시)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `server_a_client.get_training_status`, `server_a_client.ensure_success_response`, `response.json`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인
##### `get_training_log_proxy` (`app/api/voices.py:727`)

- 시그니처: `async def get_training_log_proxy(model_name: str, current_user: User=Depends(require_admin))`
- 라인 범위: `app/api/voices.py:727` ~ `app/api/voices.py:744` (약 18줄)
- 데코레이터/엔드포인트: `GET /voices/train/log/{model_name}`
- 기존 docstring 요약: 학습 로그 조회 (파록시)
- 역할(1줄): FastAPI 엔드포인트 처리
- 호출 주체(누가 부름): FastAPI 라우터(HTTP 요청 진입점)
- 호출 대상(정적 분석 추정, 상위 15개): `Depends`, `server_a_client.get_training_log`, `server_a_client.ensure_success_response`, `response.json`, `router.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 입력 검증/권한 체크/오류 응답 일관성 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - route 함수는 transport 역할(입력 파싱/검증, auth dependency, service 호출, 응답 매핑) 중심으로 유지
  - 외부 계약(path/method/응답 주요 key) 고정 후 내부 helper/service 분리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 요청에서 상태코드/응답 key shape 유지 확인
  - 입력 오류/권한 오류/외부 의존성 실패 시 오류 응답 패턴 유지 확인

### `app/core/config.py`

#### 파일 역할

- 코어 인프라 모듈 (설정/보안/DB/LLM/Redis 등)

#### 파일 메타

- 총 라인 수: 227
- 클래스 수: 1
- 함수/메서드 수: 14
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): `settings`
- 외부 계약(엔드포인트/서비스/전역 singleton): 전역 singleton/상태: `settings`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `Settings.assemble_cors_origins` | 14 | 15 | - | 하 |
| `Settings.set_gemini_api_key` | 57 | 17 | - | 하 |
| `Settings.is_admin` | 135 | 6 | - | 하 |
| `Settings.normalize_auth_cookie_samesite` | 143 | 9 | - | 하 |
| `Settings._rstrip_slash` | 181 | 2 | - | 하 |
| `Settings.backend_cors_allow_origins` | 185 | 3 | - | 하 |
| `Settings.frontend_base_url` | 190 | 2 | - | 하 |
| `Settings.auth_cookie_name` | 194 | 2 | - | 하 |
| `Settings.guest_auth_cookie_name` | 198 | 2 | - | 하 |
| `Settings.auth_cookie_samesite_value` | 202 | 2 | - | 하 |
| `Settings.app_env_normalized` | 206 | 2 | - | 하 |
| `Settings.server_a_files_api_base_url` | 210 | 2 | - | 하 |
| `Settings.server_a_training_api_base_url` | 214 | 2 | - | 하 |
| `Settings.redis_url_value` | 218 | 2 | - | 하 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `Settings` | 5 | `BaseSettings` | 14 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `assemble_cors_origins` (`app/core/config.py:14`)

- 시그니처: `def assemble_cors_origins(cls, v: Union[str, List[str]]) -> List[str]`
- 라인 범위: `app/core/config.py:14` ~ `app/core/config.py:28` (약 15줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `isinstance`, `v.startswith`, `json.loads`, `i.strip().strip('"').strip`, `i.strip().strip`, `i.strip`, `v.strip('[]').split`, `v.strip`, `v.split`, `str`, `validator`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `set_gemini_api_key` (`app/core/config.py:57`)

- 시그니처: `def set_gemini_api_key(cls, v, values)`
- 라인 범위: `app/core/config.py:57` ~ `app/core/config.py:73` (약 17줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `os.environ.get`, `warnings.warn`, `os.getenv`, `validator`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `is_admin` (`app/core/config.py:135`)

- 시그니처: `def is_admin(self, email: str) -> bool`
- 라인 범위: `app/core/config.py:135` ~ `app/core/config.py:140` (약 6줄)
- 기존 docstring 요약: 이메일이 관리자 목록에 있는지 확인
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `e.strip().lower`, `e.strip`, `self.ADMIN_EMAILS.split`, `email.lower`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `normalize_auth_cookie_samesite` (`app/core/config.py:143`)

- 시그니처: `def normalize_auth_cookie_samesite(cls, v: Optional[str]) -> str`
- 라인 범위: `app/core/config.py:143` ~ `app/core/config.py:151` (약 9줄)
- 기존 docstring 요약: samesite 값을 FastAPI/Starlette가 기대하는 소문자 문자열로 정규화.
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `(v or 'lax').strip().lower`, `(v or 'lax').strip`, `validator`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_rstrip_slash` (`app/core/config.py:181`)

- 시그니처: `def _rstrip_slash(url: str) -> str`
- 라인 범위: `app/core/config.py:181` ~ `app/core/config.py:182` (약 2줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `(url or '').rstrip`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `backend_cors_allow_origins` (`app/core/config.py:185`)

- 시그니처: `def backend_cors_allow_origins(self) -> List[str]`
- 라인 범위: `app/core/config.py:185` ~ `app/core/config.py:187` (약 3줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `str(origin).rstrip`, `str`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `frontend_base_url` (`app/core/config.py:190`)

- 시그니처: `def frontend_base_url(self) -> str`
- 라인 범위: `app/core/config.py:190` ~ `app/core/config.py:191` (약 2줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `self._rstrip_slash`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `auth_cookie_name` (`app/core/config.py:194`)

- 시그니처: `def auth_cookie_name(self) -> str`
- 라인 범위: `app/core/config.py:194` ~ `app/core/config.py:195` (약 2줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `(self.AUTH_COOKIE_NAME or 'access_token').strip`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `guest_auth_cookie_name` (`app/core/config.py:198`)

- 시그니처: `def guest_auth_cookie_name(self) -> str`
- 라인 범위: `app/core/config.py:198` ~ `app/core/config.py:199` (약 2줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `(self.GUEST_AUTH_COOKIE_NAME or 'guest_access_token').strip`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `auth_cookie_samesite_value` (`app/core/config.py:202`)

- 시그니처: `def auth_cookie_samesite_value(self) -> str`
- 라인 범위: `app/core/config.py:202` ~ `app/core/config.py:203` (약 2줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `(self.AUTH_COOKIE_SAMESITE or 'lax').strip().lower`, `(self.AUTH_COOKIE_SAMESITE or 'lax').strip`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `app_env_normalized` (`app/core/config.py:206`)

- 시그니처: `def app_env_normalized(self) -> str`
- 라인 범위: `app/core/config.py:206` ~ `app/core/config.py:207` (약 2줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `(self.APP_ENV or 'development').strip().lower`, `(self.APP_ENV or 'development').strip`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `server_a_files_api_base_url` (`app/core/config.py:210`)

- 시그니처: `def server_a_files_api_base_url(self) -> str`
- 라인 범위: `app/core/config.py:210` ~ `app/core/config.py:211` (약 2줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `self._rstrip_slash`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `server_a_training_api_base_url` (`app/core/config.py:214`)

- 시그니처: `def server_a_training_api_base_url(self) -> str`
- 라인 범위: `app/core/config.py:214` ~ `app/core/config.py:215` (약 2줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `self._rstrip_slash`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `redis_url_value` (`app/core/config.py:218`)

- 시그니처: `def redis_url_value(self) -> str`
- 라인 범위: `app/core/config.py:218` ~ `app/core/config.py:219` (약 2줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `(self.REDIS_URL or 'redis://localhost:6379/0').strip`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/core/database.py`

#### 파일 역할

- 코어 인프라 모듈 (설정/보안/DB/LLM/Redis 등)

#### 파일 메타

- 총 라인 수: 30
- 클래스 수: 1
- 함수/메서드 수: 1
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): `engine`, `AsyncSessionLocal`
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `get_db` | 25 | 6 | - | 하 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `Base` | 22 | `DeclarativeBase` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `get_db` (`app/core/database.py:25`)

- 시그니처: `async def get_db()`
- 라인 범위: `app/core/database.py:25` ~ `app/core/database.py:30` (약 6줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `AsyncSessionLocal`, `session.close`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/core/llm.py`

#### 파일 역할

- 코어 인프라 모듈 (설정/보안/DB/LLM/Redis 등)

#### 파일 메타

- 총 라인 수: 416
- 클래스 수: 0
- 함수/메서드 수: 12
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `logger`
- 외부 계약(엔드포인트/서비스/전역 singleton): 전역 singleton/상태: `logger`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `_resolve_gemini_harm_block_threshold` | 10 | 15 | - | 하 |
| `_resolve_default_gemini_model` | 27 | 7 | - | 하 |
| `_build_gemini_safety_settings` | 36 | 8 | - | 하 |
| `_extract_system_instruction_from_messages` | 46 | 20 | - | 하 |
| `_build_gemini_chat_history` | 68 | 21 | - | 하 |
| `_normalize_llm_result` | 91 | 7 | - | 하 |
| `_call_gemini_non_stream` | 100 | 56 | - | 중 |
| `_merge_system_instruction_for_ollama_messages` | 158 | 27 | - | 하 |
| `_build_ollama_request_data` | 187 | 37 | - | 하 |
| `_call_ollama_non_stream` | 226 | 65 | - | 중 |
| `call_llm` | 292 | 54 | - | 중 |
| `call_llm_stream` | 348 | 68 | - | 중 |

#### 1차/2차 개선 계획

- 1차 계획:
  - provider helper 분리 유지 + call 경로 중복/예외 메시지 표준화
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `_resolve_gemini_harm_block_threshold` (`app/core/llm.py:10`)

- 시그니처: `def _resolve_gemini_harm_block_threshold()`
- 라인 범위: `app/core/llm.py:10` ~ `app/core/llm.py:24` (약 15줄)
- 기존 docstring 요약: Gemini safety threshold를 env(settings.GOOGLE_SAFETY_THRESHOLD) 문자열에서 enum으로 변환.
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `(getattr(settings, 'GOOGLE_SAFETY_THRESHOLD', '') or 'BLOCK_NONE').strip().upper`, `(getattr(settings, 'GOOGLE_SAFETY_THRESHOLD', '') or 'BLOCK_NONE').strip`, `getattr`, `logger.warning`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_resolve_default_gemini_model` (`app/core/llm.py:27`)

- 시그니처: `def _resolve_default_gemini_model() -> str`
- 라인 범위: `app/core/llm.py:27` ~ `app/core/llm.py:33` (약 7줄)
- 기존 docstring 요약: env(settings.GOOGLE_API_MODEL)에서 Gemini 기본 모델명을 가져온다.
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `(getattr(settings, 'GOOGLE_API_MODEL', '') or '').strip`, `getattr`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_build_gemini_safety_settings` (`app/core/llm.py:36`)

- 시그니처: `def _build_gemini_safety_settings() -> Dict[Any, Any]`
- 라인 범위: `app/core/llm.py:36` ~ `app/core/llm.py:43` (약 8줄)
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_resolve_gemini_harm_block_threshold`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_extract_system_instruction_from_messages` (`app/core/llm.py:46`)

- 시그니처: `def _extract_system_instruction_from_messages(messages: List[Dict[str, str]], explicit_system_instruction: Optional[str]) -> Optional[str]`
- 라인 범위: `app/core/llm.py:46` ~ `app/core/llm.py:65` (약 20줄)
- 기존 docstring 요약: system instruction이 명시되지 않았으면 messages의 system role을 합쳐서 사용한다.
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `msg.get`, `'\n'.join`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_build_gemini_chat_history` (`app/core/llm.py:68`)

- 시그니처: `def _build_gemini_chat_history(messages: List[Dict[str, str]]) -> List[Dict[str, Any]]`
- 라인 범위: `app/core/llm.py:68` ~ `app/core/llm.py:88` (약 21줄)
- 기존 docstring 요약: `call_llm`/`call_llm_stream`에서 공통으로 쓰는 Gemini contents payload를 생성한다.
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `msg.get`, `chat_history.append`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_normalize_llm_result` (`app/core/llm.py:91`)

- 시그니처: `def _normalize_llm_result(result: Any) -> Dict[str, Any]`
- 라인 범위: `app/core/llm.py:91` ~ `app/core/llm.py:97` (약 7줄)
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `isinstance`, `str(result or '').strip`, `str`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_call_gemini_non_stream` (`app/core/llm.py:100`)

- 시그니처: `async def _call_gemini_non_stream(*, messages: List[Dict[str, str]], model: str, temperature: float, max_tokens: int, json_mode: bool, system_instruction: Optional[str]) -> Dict[str, Any]`
- 라인 범위: `app/core/llm.py:100` ~ `app/core/llm.py:155` (약 56줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `getattr`, `ValueError`, `genai.configure`, `_extract_system_instruction_from_messages`, `_build_gemini_chat_history`, `_build_gemini_safety_settings`, `genai.GenerativeModel`, `gemini_model.generate_content_async`, `response.text.strip`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_merge_system_instruction_for_ollama_messages` (`app/core/llm.py:158`)

- 시그니처: `def _merge_system_instruction_for_ollama_messages(messages: List[Dict[str, str]], system_instruction: Optional[str]) -> List[Dict[str, str]]`
- 라인 범위: `app/core/llm.py:158` ~ `app/core/llm.py:184` (약 27줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `messages.copy`, `next`, `enumerate`, `final_messages.insert`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_build_ollama_request_data` (`app/core/llm.py:187`)

- 시그니처: `def _build_ollama_request_data(*, model: str, messages: List[Dict[str, str]], temperature: float, max_tokens: int, json_mode: bool, system_instruction: Optional[str]) -> Dict[str, Any]`
- 라인 범위: `app/core/llm.py:187` ~ `app/core/llm.py:223` (약 37줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_merge_system_instruction_for_ollama_messages`, `min`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_call_ollama_non_stream` (`app/core/llm.py:226`)

- 시그니처: `async def _call_ollama_non_stream(*, messages: List[Dict[str, str]], model: str, temperature: float, max_tokens: int, json_mode: bool, system_instruction: Optional[str]) -> Dict[str, Any]`
- 라인 범위: `app/core/llm.py:226` ~ `app/core/llm.py:290` (약 65줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `getattr`, `settings.OLLAMA_BASE_URL.rstrip`, `api_path.startswith`, `_build_ollama_request_data`, `httpx.AsyncClient`, `client.post`, `response.raise_for_status`, `response.json`, `KeyError`, `message.get`, `content.strip`, `result.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `call_llm` (`app/core/llm.py:292`)

- 시그니처: `async def call_llm(messages: List[Dict[str, str]], model: Optional[str]=None, temperature: float=0.7, max_tokens: int=512, json_mode: bool=False, system_instruction: Optional[str]=None) -> Dict[str, Any]`
- 라인 범위: `app/core/llm.py:292` ~ `app/core/llm.py:345` (약 54줄)
- 기존 docstring 요약: 공통 LLM 호출 함수 (Ollama 및 Gemini 지원)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_resolve_default_gemini_model`, `model.startswith`, `_call_gemini_non_stream`, `logger.error`, `_call_ollama_non_stream`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `call_llm_stream` (`app/core/llm.py:348`)

- 시그니처: `async def call_llm_stream(messages: List[Dict[str, str]], model: Optional[str]=None, temperature: float=0.7, max_tokens: int=512, system_instruction: Optional[str]=None) -> AsyncGenerator[str, None]`
- 라인 범위: `app/core/llm.py:348` ~ `app/core/llm.py:415` (약 68줄)
- 기존 docstring 요약: Gemini 스트리밍 LLM 호출 함수.
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_resolve_default_gemini_model`, `model.startswith`, `getattr`, `ValueError`, `genai.configure`, `_extract_system_instruction_from_messages`, `_build_gemini_chat_history`, `_build_gemini_safety_settings`, `genai.GenerativeModel`, `gemini_model.generate_content`, `logger.error`, `call_llm`, `_normalize_llm_result`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/core/redis.py`

#### 파일 역할

- 코어 인프라 모듈 (설정/보안/DB/LLM/Redis 등)

#### 파일 메타

- 총 라인 수: 39
- 클래스 수: 0
- 함수/메서드 수: 3
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): `redis_client`, `logger`
- 외부 계약(엔드포인트/서비스/전역 singleton): 전역 singleton/상태: `logger`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `init_redis_pool` | 10 | 15 | - | 하 |
| `close_redis_pool` | 26 | 6 | - | 하 |
| `get_redis_client` | 33 | 7 | - | 하 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `init_redis_pool` (`app/core/redis.py:10`)

- 시그니처: `async def init_redis_pool()`
- 라인 범위: `app/core/redis.py:10` ~ `app/core/redis.py:24` (약 15줄)
- 기존 docstring 요약: 애플리케이션 시작 시 Redis 연결 풀 초기화
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `redis.from_url`, `redis_client.ping`, `logger.info`, `logger.warning`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `close_redis_pool` (`app/core/redis.py:26`)

- 시그니처: `async def close_redis_pool()`
- 라인 범위: `app/core/redis.py:26` ~ `app/core/redis.py:31` (약 6줄)
- 기존 docstring 요약: 애플리케이션 종료 시 Redis 연결 해제
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `redis_client.close`, `logger.info`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_redis_client` (`app/core/redis.py:33`)

- 시그니처: `def get_redis_client() -> redis.Redis`
- 라인 범위: `app/core/redis.py:33` ~ `app/core/redis.py:39` (약 7줄)
- 기존 docstring 요약: Redis 클라이언트 반환
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `RuntimeError`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/core/security.py`

#### 파일 역할

- 코어 인프라 모듈 (설정/보안/DB/LLM/Redis 등)

#### 파일 메타

- 총 라인 수: 24
- 클래스 수: 0
- 함수/메서드 수: 2
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): `pwd_context`, `GUEST_SUBJECT_PREFIX`
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `create_access_token` | 10 | 9 | - | 하 |
| `create_guest_access_token` | 21 | 4 | - | 하 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `create_access_token` (`app/core/security.py:10`)

- 시그니처: `def create_access_token(subject: Union[str, Any], expires_delta: timedelta=None) -> str`
- 라인 범위: `app/core/security.py:10` ~ `app/core/security.py:18` (약 9줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `datetime.utcnow`, `timedelta`, `str`, `jwt.encode`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `create_guest_access_token` (`app/core/security.py:21`)

- 시그니처: `def create_guest_access_token(subject: Union[str, Any], expires_delta: timedelta=None) -> str`
- 라인 범위: `app/core/security.py:21` ~ `app/core/security.py:24` (약 4줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `timedelta`, `create_access_token`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/main.py`

#### 파일 역할

- 루트 스크립트/보조 실행 파일

#### 파일 메타

- 총 라인 수: 254
- 클래스 수: 0
- 함수/메서드 수: 11
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): `logger`, `app`
- 외부 계약(엔드포인트/서비스/전역 singleton): 전역 singleton/상태: `logger`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `_warn_deprecated_env_usage` | 16 | 14 | - | 하 |
| `_log_security_flag_status` | 32 | 15 | - | 하 |
| `_run_startup_validation_checks` | 49 | 31 | - | 하 |
| `_ensure_runtime_dirs` | 82 | 9 | - | 하 |
| `_apply_startup_schema_patches` | 93 | 41 | - | 중 |
| `_log_cors_configuration` | 136 | 3 | - | 하 |
| `lifespan` | 143 | 29 | - | 하 |
| `root` | 228 | 2 | - | 하 |
| `health` | 233 | 2 | - | 하 |
| `system_status` | 237 | 9 | - | 하 |
| `test_connection` | 248 | 7 | - | 하 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 운영 환경 기본 보안값 강화(dev fallback/insecure 옵션 기본 비활성)

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `_warn_deprecated_env_usage` (`app/main.py:16`)

- 시그니처: `def _warn_deprecated_env_usage() -> None`
- 라인 범위: `app/main.py:16` ~ `app/main.py:29` (약 14줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `os.environ.get`, `logger.warning`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_log_security_flag_status` (`app/main.py:32`)

- 시그니처: `def _log_security_flag_status() -> None`
- 라인 범위: `app/main.py:32` ~ `app/main.py:46` (약 15줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `logger.warning`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_run_startup_validation_checks` (`app/main.py:49`)

- 시그니처: `def _run_startup_validation_checks() -> None`
- 라인 범위: `app/main.py:49` ~ `app/main.py:79` (약 31줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `logger.warning`, `RuntimeError`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_ensure_runtime_dirs` (`app/main.py:82`)

- 시그니처: `def _ensure_runtime_dirs() -> None`
- 라인 범위: `app/main.py:82` ~ `app/main.py:90` (약 9줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `os.makedirs`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_apply_startup_schema_patches` (`app/main.py:93`)

- 시그니처: `async def _apply_startup_schema_patches(conn) -> None`
- 라인 범위: `app/main.py:93` ~ `app/main.py:133` (약 41줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `logger.warning`, `conn.execute`, `text`, `str(e).lower`, `str`, `logger.info`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_log_cors_configuration` (`app/main.py:136`)

- 시그니처: `def _log_cors_configuration() -> None`
- 라인 범위: `app/main.py:136` ~ `app/main.py:138` (약 3줄)
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `logger.info`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `lifespan` (`app/main.py:143`)

- 시그니처: `async def lifespan(app: FastAPI)`
- 라인 범위: `app/main.py:143` ~ `app/main.py:171` (약 29줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `init_redis_pool`, `_warn_deprecated_env_usage`, `_log_security_flag_status`, `_run_startup_validation_checks`, `_ensure_runtime_dirs`, `engine.begin`, `conn.run_sync`, `_apply_startup_schema_patches`, `close_redis_pool`, `engine.dispose`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `root` (`app/main.py:228`)

- 시그니처: `async def root()`
- 라인 범위: `app/main.py:228` ~ `app/main.py:229` (약 2줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `app.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `health` (`app/main.py:233`)

- 시그니처: `async def health()`
- 라인 범위: `app/main.py:233` ~ `app/main.py:234` (약 2줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `app.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `system_status` (`app/main.py:237`)

- 시그니처: `async def system_status()`
- 라인 범위: `app/main.py:237` ~ `app/main.py:245` (약 9줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `app.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `test_connection` (`app/main.py:248`)

- 시그니처: `async def test_connection()`
- 라인 범위: `app/main.py:248` ~ `app/main.py:254` (약 7줄)
- 기존 docstring 요약: 연결 테스트 엔드포인트
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `str`, `app.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/models/audio.py`

#### 파일 역할

- SQLAlchemy ORM 모델 정의

#### 파일 메타

- 총 라인 수: 35
- 클래스 수: 1
- 함수/메서드 수: 0
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

- 함수/메서드 없음

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `AudioFile` | 16 | `Base` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 스키마 변경 없이 관계/직렬화/호환성 주석 보강 유지
- 2차 계획 후보:
  - 마이그레이션 도구 기준 스키마/제약 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- DB 테이블/컬럼 의미 및 직렬화 관례 변경 금지

#### 함수별 상세 분석 (전수)

### `app/models/auth_event.py`

#### 파일 역할

- SQLAlchemy ORM 모델 정의

#### 파일 메타

- 총 라인 수: 30
- 클래스 수: 1
- 함수/메서드 수: 0
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

- 함수/메서드 없음

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `AuthEvent` | 13 | `Base` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 스키마 변경 없이 관계/직렬화/호환성 주석 보강 유지
- 2차 계획 후보:
  - 마이그레이션 도구 기준 스키마/제약 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- DB 테이블/컬럼 의미 및 직렬화 관례 변경 금지

#### 함수별 상세 분석 (전수)

### `app/models/character.py`

#### 파일 역할

- SQLAlchemy ORM 모델 정의

#### 파일 메타

- 총 라인 수: 54
- 클래스 수: 1
- 함수/메서드 수: 0
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

- 함수/메서드 없음

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `Character` | 17 | `Base` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 스키마 변경 없이 관계/직렬화/호환성 주석 보강 유지
- 2차 계획 후보:
  - 마이그레이션 도구 기준 스키마/제약 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- DB 테이블/컬럼 의미 및 직렬화 관례 변경 금지

#### 함수별 상세 분석 (전수)

### `app/models/chat_message.py`

#### 파일 역할

- SQLAlchemy ORM 모델 정의

#### 파일 메타

- 총 라인 수: 29
- 클래스 수: 1
- 함수/메서드 수: 0
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

- 함수/메서드 없음

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `ChatMessage` | 16 | `Base` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 스키마 변경 없이 관계/직렬화/호환성 주석 보강 유지
- 2차 계획 후보:
  - 마이그레이션 도구 기준 스키마/제약 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- DB 테이블/컬럼 의미 및 직렬화 관례 변경 금지

#### 함수별 상세 분석 (전수)

### `app/models/generation.py`

#### 파일 역할

- SQLAlchemy ORM 모델 정의

#### 파일 메타

- 총 라인 수: 34
- 클래스 수: 1
- 함수/메서드 수: 0
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

- 함수/메서드 없음

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `GenerationJob` | 16 | `Base` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 스키마 변경 없이 관계/직렬화/호환성 주석 보강 유지
- 2차 계획 후보:
  - 마이그레이션 도구 기준 스키마/제약 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- DB 테이블/컬럼 의미 및 직렬화 관례 변경 금지

#### 함수별 상세 분석 (전수)

### `app/models/guest_session.py`

#### 파일 역할

- SQLAlchemy ORM 모델 정의

#### 파일 메타

- 총 라인 수: 33
- 클래스 수: 1
- 함수/메서드 수: 0
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

- 함수/메서드 없음

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `GuestSession` | 17 | `Base` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 스키마 변경 없이 관계/직렬화/호환성 주석 보강 유지
- 2차 계획 후보:
  - 마이그레이션 도구 기준 스키마/제약 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- DB 테이블/컬럼 의미 및 직렬화 관례 변경 금지

#### 함수별 상세 분석 (전수)

### `app/models/scenario.py`

#### 파일 역할

- SQLAlchemy ORM 모델 정의

#### 파일 메타

- 총 라인 수: 36
- 클래스 수: 1
- 함수/메서드 수: 0
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

- 함수/메서드 없음

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `Scenario` | 15 | `Base` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 스키마 변경 없이 관계/직렬화/호환성 주석 보강 유지
- 2차 계획 후보:
  - 마이그레이션 도구 기준 스키마/제약 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- DB 테이블/컬럼 의미 및 직렬화 관례 변경 금지

#### 함수별 상세 분석 (전수)

### `app/models/summary.py`

#### 파일 역할

- SQLAlchemy ORM 모델 정의

#### 파일 메타

- 총 라인 수: 20
- 클래스 수: 1
- 함수/메서드 수: 0
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

- 함수/메서드 없음

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `ChatSummary` | 14 | `Base` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 스키마 변경 없이 관계/직렬화/호환성 주석 보강 유지
- 2차 계획 후보:
  - 마이그레이션 도구 기준 스키마/제약 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- DB 테이블/컬럼 의미 및 직렬화 관례 변경 금지

#### 함수별 상세 분석 (전수)

### `app/models/user.py`

#### 파일 역할

- SQLAlchemy ORM 모델 정의

#### 파일 메타

- 총 라인 수: 37
- 클래스 수: 1
- 함수/메서드 수: 0
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

- 함수/메서드 없음

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `User` | 17 | `Base` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 스키마 변경 없이 관계/직렬화/호환성 주석 보강 유지
- 2차 계획 후보:
  - 마이그레이션 도구 기준 스키마/제약 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- DB 테이블/컬럼 의미 및 직렬화 관례 변경 금지

#### 함수별 상세 분석 (전수)

### `app/models/user_preference.py`

#### 파일 역할

- SQLAlchemy ORM 모델 정의

#### 파일 메타

- 총 라인 수: 20
- 클래스 수: 1
- 함수/메서드 수: 0
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

- 함수/메서드 없음

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `UserPreference` | 15 | `Base` | 0 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 스키마 변경 없이 관계/직렬화/호환성 주석 보강 유지
- 2차 계획 후보:
  - 마이그레이션 도구 기준 스키마/제약 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- DB 테이블/컬럼 의미 및 직렬화 관례 변경 금지

#### 함수별 상세 분석 (전수)

### `app/models/voice.py`

#### 파일 역할

- SQLAlchemy ORM 모델 정의

#### 파일 메타

- 총 라인 수: 65
- 클래스 수: 1
- 함수/메서드 수: 1
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `Voice.__repr__` | 64 | 2 | - | 하 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `Voice` | 12 | `Base` | 1 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 스키마 변경 없이 관계/직렬화/호환성 주석 보강 유지
- 2차 계획 후보:
  - 마이그레이션 도구 기준 스키마/제약 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- DB 테이블/컬럼 의미 및 직렬화 관례 변경 금지

#### 함수별 상세 분석 (전수)

##### `__repr__` (`app/models/voice.py:64`)

- 시그니처: `def __repr__(self)`
- 라인 범위: `app/models/voice.py:64` ~ `app/models/voice.py:65` (약 2줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정): 구현 본문 기준 내부 로직/상위 호출 위주
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/services/audio_analyzer.py`

#### 파일 역할

- 서비스 계층/보조 로직 모듈

#### 파일 메타

- 총 라인 수: 101
- 클래스 수: 1
- 함수/메서드 수: 1
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `AudioAnalyzer.analyze_audio` | 18 | 84 | - | 상 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `AudioAnalyzer` | 14 | - | 1 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `analyze_audio` (`app/services/audio_analyzer.py:18`)

- 시그니처: `def analyze_audio(file_path: str) -> Dict[str, Any]`
- 라인 범위: `app/services/audio_analyzer.py:18` ~ `app/services/audio_analyzer.py:101` (약 84줄)
- 기존 docstring 요약: 오디오 파일을 분석하여 메타데이터를 반환합니다.
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `os.path.exists`, `FileNotFoundError`, `os.path.getsize`, `MutagenFile`, `os.path.splitext(file_path)[1][1:].lower`, `os.path.splitext`, `hasattr`, `float`, `isinstance`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 파일/디렉터리 I/O 가능성
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 긴 함수(80줄+)
  - broad exception 사용
- 분석/개선 우선순위: **상**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/services/auth_audit_service.py`

#### 파일 역할

- 서비스 계층/보조 로직 모듈

#### 파일 메타

- 총 라인 수: 71
- 클래스 수: 0
- 함수/메서드 수: 2
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `logger`
- 외부 계약(엔드포인트/서비스/전역 singleton): 전역 singleton/상태: `logger`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `_extract_client_ip` | 22 | 9 | - | 하 |
| `log_auth_event` | 33 | 39 | - | 하 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `_extract_client_ip` (`app/services/auth_audit_service.py:22`)

- 시그니처: `def _extract_client_ip(request: Optional[Request]) -> Optional[str]`
- 라인 범위: `app/services/auth_audit_service.py:22` ~ `app/services/auth_audit_service.py:30` (약 9줄)
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `request.headers.get`, `xff.split(',')[0].strip`, `xff.split`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `log_auth_event` (`app/services/auth_audit_service.py:33`)

- 시그니처: `async def log_auth_event(db: AsyncSession, *, request: Optional[Request], actor_type: str, actor_id: Optional[str], provider: str, event_type: str, success: bool, reason: Optional[str]=None, commit: bool=False) -> None`
- 라인 범위: `app/services/auth_audit_service.py:33` ~ `app/services/auth_audit_service.py:71` (약 39줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `db.add`, `AuthEvent`, `_extract_client_ip`, `request.headers.get`, `db.commit`, `db.rollback`, `logger.exception`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/services/character_service.py`

#### 파일 역할

- 서비스 계층/보조 로직 모듈

#### 파일 메타

- 총 라인 수: 626
- 클래스 수: 0
- 함수/메서드 수: 14
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `logger`, `_preset_characters_cache`, `_preset_cache_key`
- 외부 계약(엔드포인트/서비스/전역 singleton): 전역 singleton/상태: `logger`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `resolve_preset_characters_dir` | 30 | 40 | - | 중 |
| `invalidate_preset_character_cache` | 72 | 24 | - | 하 |
| `load_preset_characters_from_disk` | 98 | 60 | - | 중 |
| `update_preset_character_voice_id` | 160 | 49 | - | 중 |
| `find_preset_character_by_id` | 211 | 26 | - | 하 |
| `load_preset_character_by_id_from_disk` | 239 | 23 | - | 하 |
| `resolve_character_voice_id_for_tts` | 264 | 47 | - | 중 |
| `encode_tags_for_storage` | 313 | 25 | - | 하 |
| `serialize_character_model` | 340 | 36 | - | 하 |
| `build_character_model_for_create` | 378 | 39 | - | 하 |
| `build_admin_character_list_payload` | 419 | 49 | - | 중 |
| `extract_json_object_from_llm_text` | 470 | 50 | - | 중 |
| `normalize_string_list_fields` | 522 | 27 | - | 하 |
| `call_llm_json_with_gemini_fallback` | 551 | 76 | - | 중 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `resolve_preset_characters_dir` (`app/services/character_service.py:30`)

- 시그니처: `def resolve_preset_characters_dir() -> Optional[Path]`
- 라인 범위: `app/services/character_service.py:30` ~ `app/services/character_service.py:69` (약 40줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `os.environ.get`, `Path`, `candidate.is_dir`, `Path(__file__).resolve`, `monorepo.is_dir`, `fallback.is_dir`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 파일/디렉터리 I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `invalidate_preset_character_cache` (`app/services/character_service.py:72`)

- 시그니처: `def invalidate_preset_character_cache() -> None`
- 라인 범위: `app/services/character_service.py:72` ~ `app/services/character_service.py:95` (약 24줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정): 구현 본문 기준 내부 로직/상위 호출 위주
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `load_preset_characters_from_disk` (`app/services/character_service.py:98`)

- 시그니처: `def load_preset_characters_from_disk() -> List[Dict[str, Any]]`
- 라인 범위: `app/services/character_service.py:98` ~ `app/services/character_service.py:157` (약 60줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `resolve_preset_characters_dir`, `base.is_dir`, `invalidate_preset_character_cache`, `base.stat`, `str`, `sorted`, `base.glob`, `open`, `json.load`, `isinstance`, `out.append`, `logger.warning`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 파일/디렉터리 I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `update_preset_character_voice_id` (`app/services/character_service.py:160`)

- 시그니처: `def update_preset_character_voice_id(*, preset_char: Dict[str, Any], voice_id: Optional[str]) -> Dict[str, Any]`
- 라인 범위: `app/services/character_service.py:160` ~ `app/services/character_service.py:208` (약 49줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `resolve_preset_characters_dir`, `base.is_dir`, `FileNotFoundError`, `preset_char.get`, `path.exists`, `open`, `json.load`, `json.dump`, `invalidate_preset_character_cache`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 파일/디렉터리 I/O 가능성
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `find_preset_character_by_id` (`app/services/character_service.py:211`)

- 시그니처: `def find_preset_character_by_id(preset_characters: List[Dict[str, Any]], character_id: str) -> Optional[Dict[str, Any]]`
- 라인 범위: `app/services/character_service.py:211` ~ `app/services/character_service.py:236` (약 26줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `next`, `c.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `load_preset_character_by_id_from_disk` (`app/services/character_service.py:239`)

- 시그니처: `def load_preset_character_by_id_from_disk(character_id: str) -> Optional[Dict[str, Any]]`
- 라인 범위: `app/services/character_service.py:239` ~ `app/services/character_service.py:261` (약 23줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `find_preset_character_by_id`, `load_preset_characters_from_disk`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `resolve_character_voice_id_for_tts` (`app/services/character_service.py:264`)

- 시그니처: `async def resolve_character_voice_id_for_tts(*, character_id: Optional[str], db: AsyncSession, current_user_id: Optional[str], default_voice_id: str='default') -> str`
- 라인 범위: `app/services/character_service.py:264` ~ `app/services/character_service.py:310` (약 47줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `db.execute`, `select(Character).where`, `select`, `result.scalar_one_or_none`, `find_preset_character_by_id`, `load_preset_characters_from_disk`, `preset.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `encode_tags_for_storage` (`app/services/character_service.py:313`)

- 시그니처: `def encode_tags_for_storage(tags: Optional[Iterable[str]]) -> Optional[str]`
- 라인 범위: `app/services/character_service.py:313` ~ `app/services/character_service.py:337` (약 25줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `json.dumps`, `list`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `serialize_character_model` (`app/services/character_service.py:340`)

- 시그니처: `def serialize_character_model(character: Character) -> Dict[str, Any]`
- 라인 범위: `app/services/character_service.py:340` ~ `app/services/character_service.py:375` (약 36줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `json.loads`, `character.created_at.isoformat`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `build_character_model_for_create` (`app/services/character_service.py:378`)

- 시그니처: `def build_character_model_for_create(*, user_id: str, payload: Any) -> Character`
- 라인 범위: `app/services/character_service.py:378` ~ `app/services/character_service.py:416` (약 39줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `encode_tags_for_storage`, `getattr`, `Character`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `build_admin_character_list_payload` (`app/services/character_service.py:419`)

- 시그니처: `def build_admin_character_list_payload(*, preset_characters: List[Dict[str, Any]], db_characters: List[Character]) -> List[Dict[str, Any]]`
- 라인 범위: `app/services/character_service.py:419` ~ `app/services/character_service.py:467` (약 49줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `c.get`, `bool`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `extract_json_object_from_llm_text` (`app/services/character_service.py:470`)

- 시그니처: `def extract_json_object_from_llm_text(content: str) -> Dict[str, Any]`
- 라인 범위: `app/services/character_service.py:470` ~ `app/services/character_service.py:519` (약 50줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `(content or '').strip`, `content.split('```json', 1)[1].split('```', 1)[0].strip`, `content.split('```json', 1)[1].split`, `content.split`, `content.split('```', 1)[1].split('```', 1)[0].strip`, `content.split('```', 1)[1].split`, `json.loads`, `isinstance`, `content.find`, `content.rfind`, `ValueError`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `normalize_string_list_fields` (`app/services/character_service.py:522`)

- 시그니처: `def normalize_string_list_fields(payload: Dict[str, Any], fields: Iterable[str]) -> Dict[str, Any]`
- 라인 범위: `app/services/character_service.py:522` ~ `app/services/character_service.py:548` (약 27줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `payload.get`, `isinstance`, `x.strip`, `value.split`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `call_llm_json_with_gemini_fallback` (`app/services/character_service.py:551`)

- 시그니처: `async def call_llm_json_with_gemini_fallback(*, messages: List[Dict[str, str]], system_instruction: Optional[str], fallback_model: str, temperature: float, max_tokens: int, logger_hint: str) -> Tuple[str, bool]`
- 라인 범위: `app/services/character_service.py:551` ~ `app/services/character_service.py:626` (약 76줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `getattr`, `call_llm`, `isinstance`, `result.get`, `logger.warning`, `RuntimeError`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/services/chat_service.py`

#### 파일 역할

- 서비스 계층/보조 로직 모듈

#### 파일 메타

- 총 라인 수: 397
- 클래스 수: 0
- 함수/메서드 수: 9
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `extract_scenario_fields` | 22 | 33 | - | 하 |
| `resolve_character_identity` | 57 | 49 | - | 중 |
| `split_system_and_chat_messages` | 108 | 37 | - | 하 |
| `sanitize_assistant_reply_content` | 147 | 27 | - | 하 |
| `parse_director_turn_context` | 176 | 60 | - | 중 |
| `normalize_request_messages_for_context` | 238 | 29 | - | 하 |
| `append_history_messages` | 269 | 33 | - | 하 |
| `persist_chat_turn_messages` | 304 | 52 | - | 중 |
| `synthesize_chat_tts_audio_url` | 358 | 40 | - | 중 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `extract_scenario_fields` (`app/services/chat_service.py:22`)

- 시그니처: `def extract_scenario_fields(scenario: Optional[Dict[str, str]], *, default_user_name: str) -> Dict[str, Optional[str]]`
- 라인 범위: `app/services/chat_service.py:22` ~ `app/services/chat_service.py:54` (약 33줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `scenario.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `resolve_character_identity` (`app/services/chat_service.py:57`)

- 시그니처: `async def resolve_character_identity(*, character_id: Optional[str], fallback_name: str, db: AsyncSession, preset_loader: Callable[[], List[Dict[str, Any]]]) -> Tuple[str, Optional[str]]`
- 라인 범위: `app/services/chat_service.py:57` ~ `app/services/chat_service.py:105` (약 49줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `preset_loader`, `next`, `x.get`, `preset.get`, `db.execute`, `select(Character).where`, `select`, `result.scalar_one_or_none`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `split_system_and_chat_messages` (`app/services/chat_service.py:108`)

- 시그니처: `def split_system_and_chat_messages(messages: List[Dict[str, str]]) -> Tuple[Optional[str], List[Dict[str, str]]]`
- 라인 범위: `app/services/chat_service.py:108` ~ `app/services/chat_service.py:144` (약 37줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `chat_messages.append`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `sanitize_assistant_reply_content` (`app/services/chat_service.py:147`)

- 시그니처: `def sanitize_assistant_reply_content(content: str, speaker_name: Optional[str]) -> str`
- 라인 범위: `app/services/chat_service.py:147` ~ `app/services/chat_service.py:173` (약 27줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `re.sub`, `re.escape`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `parse_director_turn_context` (`app/services/chat_service.py:176`)

- 시그니처: `def parse_director_turn_context(*, persona: str, messages: List[Any], current_speaker_override: Optional[str]) -> Dict[str, str]`
- 라인 범위: `app/services/chat_service.py:176` ~ `app/services/chat_service.py:235` (약 60줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `persona.split`, `len`, `ValueError`, `char1_section.split('[배우 1: ', 1)[1].split`, `char1_section.split`, `char2_section.split('[배우 2: ', 1)[1].split`, `char2_section.split`, `'\n'.join`, `getattr`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `normalize_request_messages_for_context` (`app/services/chat_service.py:238`)

- 시그니처: `def normalize_request_messages_for_context(messages: List[Any]) -> List[Dict[str, str]]`
- 라인 범위: `app/services/chat_service.py:238` ~ `app/services/chat_service.py:266` (약 29줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `getattr`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `append_history_messages` (`app/services/chat_service.py:269`)

- 시그니처: `def append_history_messages(*, messages_acc: List[Dict[str, str]], request_messages: List[Dict[str, str]], default_user_message: str='대화를 시작해주세요.') -> None`
- 라인 범위: `app/services/chat_service.py:269` ~ `app/services/chat_service.py:301` (약 33줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `request_messages.append`, `messages_acc.append`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `persist_chat_turn_messages` (`app/services/chat_service.py:304`)

- 시그니처: `async def persist_chat_turn_messages(*, db: AsyncSession, session_id: str, raw_request_messages: List[Any], assistant_content: str, speaker_name: Optional[str]) -> None`
- 라인 범위: `app/services/chat_service.py:304` ~ `app/services/chat_service.py:355` (약 52줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `getattr`, `db.add`, `ChatMessage`, `db.commit`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `synthesize_chat_tts_audio_url` (`app/services/chat_service.py:358`)

- 시그니처: `async def synthesize_chat_tts_audio_url(*, text: str, voice_id: Optional[str], streaming_mode: int, speed_factor: float, current_user: Any, db: AsyncSession) -> Optional[str]`
- 라인 범위: `app/services/chat_service.py:358` ~ `app/services/chat_service.py:397` (약 40줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `(text or '').strip`, `TTSRequest`, `text.strip`, `_synthesize_tts_internal`, `tts_resp.get`, `tts_resp.get('data', {}).get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/services/context_manager.py`

#### 파일 역할

- 서비스 계층/보조 로직 모듈

#### 파일 메타

- 총 라인 수: 335
- 클래스 수: 1
- 함수/메서드 수: 14
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `logger`, `context_manager`
- 외부 계약(엔드포인트/서비스/전역 singleton): 전역 singleton/상태: `logger`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `ContextManager.__init__` | 20 | 17 | - | 하 |
| `ContextManager._summary_key` | 38 | 23 | - | 하 |
| `ContextManager._try_get_summary_from_redis` | 62 | 15 | - | 하 |
| `ContextManager._try_set_summary_to_redis` | 78 | 14 | - | 하 |
| `ContextManager._try_get_summary_from_db` | 93 | 19 | - | 하 |
| `ContextManager._try_save_summary_to_db` | 113 | 23 | - | 하 |
| `ContextManager._normalize_chat_messages` | 137 | 16 | - | 하 |
| `ContextManager._estimate_total_context_tokens` | 154 | 17 | - | 하 |
| `ContextManager._split_window_and_summary_candidates` | 172 | 31 | - | 하 |
| `ContextManager._build_dialogue_text` | 204 | 9 | - | 하 |
| `ContextManager.get_summary` | 214 | 24 | - | 하 |
| `ContextManager.save_summary` | 239 | 16 | - | 하 |
| `ContextManager.manage_context` | 256 | 41 | - | 중 |
| `ContextManager.summarize_dialogue` | 298 | 36 | - | 하 |

#### 주요 클래스 목록

| 클래스 | 라인 | Base | 메서드 수 |
|---|---|---|---|
| `ContextManager` | 19 | - | 14 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `__init__` (`app/services/context_manager.py:20`)

- 시그니처: `def __init__(self)`
- 라인 범위: `app/services/context_manager.py:20` ~ `app/services/context_manager.py:36` (약 17줄)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `redis.from_url`, `self.redis.ping`, `logger.info`, `logger.debug`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_summary_key` (`app/services/context_manager.py:38`)

- 시그니처: `def _summary_key(self, session_id: str) -> str`
- 라인 범위: `app/services/context_manager.py:38` ~ `app/services/context_manager.py:60` (약 23줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정): 구현 본문 기준 내부 로직/상위 호출 위주
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_try_get_summary_from_redis` (`app/services/context_manager.py:62`)

- 시그니처: `def _try_get_summary_from_redis(self, session_id: str) -> Optional[str]`
- 라인 범위: `app/services/context_manager.py:62` ~ `app/services/context_manager.py:76` (약 15줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `self.redis.get`, `self._summary_key`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_try_set_summary_to_redis` (`app/services/context_manager.py:78`)

- 시그니처: `def _try_set_summary_to_redis(self, session_id: str, summary: str) -> None`
- 라인 범위: `app/services/context_manager.py:78` ~ `app/services/context_manager.py:91` (약 14줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `self.redis.set`, `self._summary_key`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_try_get_summary_from_db` (`app/services/context_manager.py:93`)

- 시그니처: `async def _try_get_summary_from_db(self, session_id: str, db: AsyncSession) -> Optional[str]`
- 라인 범위: `app/services/context_manager.py:93` ~ `app/services/context_manager.py:111` (약 19줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `select(ChatSummary).where`, `select`, `db.execute`, `result.scalar_one_or_none`, `logger.warning`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_try_save_summary_to_db` (`app/services/context_manager.py:113`)

- 시그니처: `async def _try_save_summary_to_db(self, session_id: str, summary: str, db: AsyncSession) -> None`
- 라인 범위: `app/services/context_manager.py:113` ~ `app/services/context_manager.py:135` (약 23줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `select(ChatSummary).where`, `select`, `db.execute`, `result.scalar_one_or_none`, `db.add`, `ChatSummary`, `db.commit`, `logger.warning`, `db.rollback`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_normalize_chat_messages` (`app/services/context_manager.py:137`)

- 시그니처: `def _normalize_chat_messages(self, messages: List[Dict[str, Any]]) -> List[Dict[str, str]]`
- 라인 범위: `app/services/context_manager.py:137` ~ `app/services/context_manager.py:152` (약 16줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `m.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_estimate_total_context_tokens` (`app/services/context_manager.py:154`)

- 시그니처: `def _estimate_total_context_tokens(self, *, enc: Any, messages: List[Dict[str, str]], persona: Optional[str], summary: Optional[str]) -> int`
- 라인 범위: `app/services/context_manager.py:154` ~ `app/services/context_manager.py:170` (약 17줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `len`, `enc.encode`, `sum`, `m.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_split_window_and_summary_candidates` (`app/services/context_manager.py:172`)

- 시그니처: `def _split_window_and_summary_candidates(self, *, normalized: List[Dict[str, str]], total_tokens: int, max_turns: int, max_context: int, ratio: float) -> Tuple[List[Dict[str, str]], List[Dict[str, str]], bool]`
- 라인 범위: `app/services/context_manager.py:172` ~ `app/services/context_manager.py:202` (약 31줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `max`, `len`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_build_dialogue_text` (`app/services/context_manager.py:204`)

- 시그니처: `def _build_dialogue_text(self, messages: List[Dict[str, str]]) -> str`
- 라인 범위: `app/services/context_manager.py:204` ~ `app/services/context_manager.py:212` (약 9줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `'\n'.join`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_summary` (`app/services/context_manager.py:214`)

- 시그니처: `async def get_summary(self, session_id: str, db: Optional[AsyncSession]=None) -> str`
- 라인 범위: `app/services/context_manager.py:214` ~ `app/services/context_manager.py:237` (약 24줄)
- 기존 docstring 요약: 요약 조회 우선순위:
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `self._try_get_summary_from_redis`, `self._try_get_summary_from_db`, `self._try_set_summary_to_redis`, `self._memory_store.get`, `self._summary_key`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `save_summary` (`app/services/context_manager.py:239`)

- 시그니처: `async def save_summary(self, session_id: str, summary: str, db: Optional[AsyncSession]=None)`
- 라인 범위: `app/services/context_manager.py:239` ~ `app/services/context_manager.py:254` (약 16줄)
- 기존 docstring 요약: 요약 저장:
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `self._try_save_summary_to_db`, `self._try_set_summary_to_redis`, `self._summary_key`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `manage_context` (`app/services/context_manager.py:256`)

- 시그니처: `async def manage_context(self, messages: List[Dict], summary: str, session_id: str, db: Optional[AsyncSession], persona: Optional[str], max_turns: int, max_context: int, ratio: float) -> Tuple[List[Dict], str, bool]`
- 라인 범위: `app/services/context_manager.py:256` ~ `app/services/context_manager.py:296` (약 41줄)
- 기존 docstring 요약: 턴 기반 슬라이딩 윈도우 + tiktoken 80% 트리거.
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `tiktoken.get_encoding`, `self._normalize_chat_messages`, `self._estimate_total_context_tokens`, `self._split_window_and_summary_candidates`, `self.summarize_dialogue`, `self.save_summary`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `summarize_dialogue` (`app/services/context_manager.py:298`)

- 시그니처: `async def summarize_dialogue(self, messages: List[Dict[str, str]], previous_summary: str='') -> str`
- 라인 범위: `app/services/context_manager.py:298` ~ `app/services/context_manager.py:333` (약 36줄)
- 기존 docstring 요약: LLM을 사용한 요약 생성 (기존 로직 유지)
- 역할(1줄): 클래스 메서드 처리
- 호출 주체(누가 부름): 같은 클래스 인스턴스/클래스 메서드 호출자
- 호출 대상(정적 분석 추정, 상위 15개): `self._build_dialogue_text`, `call_llm`, `isinstance`, `result.get`, `summary.strip`, `logger.warning`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/services/server_a_client.py`

#### 파일 역할

- 서비스 계층/보조 로직 모듈

#### 파일 메타

- 총 라인 수: 327
- 클래스 수: 0
- 함수/메서드 수: 22
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `logger`
- 외부 계약(엔드포인트/서비스/전역 singleton): 전역 singleton/상태: `logger`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `_join_url` | 25 | 12 | - | 하 |
| `_service_base` | 39 | 19 | - | 하 |
| `_service_label` | 60 | 16 | - | 하 |
| `_request` | 78 | 62 | - | 중 |
| `ensure_success_response` | 142 | 30 | - | 하 |
| `get_files_index` | 176 | 8 | - | 하 |
| `get_files_models` | 186 | 2 | - | 하 |
| `get_files_train_voices` | 190 | 2 | - | 하 |
| `get_files_logs` | 194 | 2 | - | 하 |
| `upload_train_voice_file` | 198 | 9 | - | 하 |
| `upload_training_item` | 209 | 9 | - | 하 |
| `delete_file` | 220 | 8 | - | 하 |
| `mkdir` | 230 | 8 | - | 하 |
| `trim_audio` | 240 | 12 | - | 하 |
| `validate_ref_audio` | 254 | 8 | - | 하 |
| `prepare_ref_audio` | 264 | 8 | - | 하 |
| `start_training` | 276 | 2 | - | 하 |
| `get_training_status` | 280 | 2 | - | 하 |
| `get_training_log` | 284 | 2 | - | 하 |
| `call_tts` | 290 | 18 | - | 하 |
| `set_tts_gpt_weights` | 310 | 8 | - | 하 |
| `set_tts_sovits_weights` | 320 | 8 | - | 하 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `_join_url` (`app/services/server_a_client.py:25`)

- 시그니처: `def _join_url(base: str, path: str) -> str`
- 라인 범위: `app/services/server_a_client.py:25` ~ `app/services/server_a_client.py:36` (약 12줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `(base or '').rstrip`, `(path or '').lstrip`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_service_base` (`app/services/server_a_client.py:39`)

- 시그니처: `def _service_base(service: str) -> str`
- 라인 범위: `app/services/server_a_client.py:39` ~ `app/services/server_a_client.py:57` (약 19줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `(service or '').strip().lower`, `(service or '').strip`, `settings.TTS_BASE_URL.rstrip`, `ValueError`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_service_label` (`app/services/server_a_client.py:60`)

- 시그니처: `def _service_label(service: str) -> str`
- 라인 범위: `app/services/server_a_client.py:60` ~ `app/services/server_a_client.py:75` (약 16줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `(service or '').strip().lower`, `(service or '').strip`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_request` (`app/services/server_a_client.py:78`)

- 시그니처: `async def _request(*, service: str, method: str, path: str, timeout: float, params: Optional[Dict[str, Any]]=None, data: Optional[Dict[str, Any]]=None, json_body: Optional[Dict[str, Any]]=None, files: Any=None, verify: Optional[bool]=None, request_error_detail: Optional[str]=None) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:78` ~ `app/services/server_a_client.py:139` (약 62줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_service_base`, `_join_url`, `(method or 'GET').upper`, `httpx.AsyncClient`, `client.request`, `logger.warning`, `_service_label`, `str`, `HTTPException`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `ensure_success_response` (`app/services/server_a_client.py:142`)

- 시그니처: `def ensure_success_response(response: httpx.Response, *, error_prefix: str, allow_404_detail: Optional[str]=None) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:142` ~ `app/services/server_a_client.py:171` (약 30줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `HTTPException`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_files_index` (`app/services/server_a_client.py:176`)

- 시그니처: `async def get_files_index(*, timeout: float=10.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:176` ~ `app/services/server_a_client.py:183` (약 8줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_files_models` (`app/services/server_a_client.py:186`)

- 시그니처: `async def get_files_models(*, timeout: float=10.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:186` ~ `app/services/server_a_client.py:187` (약 2줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_files_train_voices` (`app/services/server_a_client.py:190`)

- 시그니처: `async def get_files_train_voices(*, timeout: float=10.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:190` ~ `app/services/server_a_client.py:191` (약 2줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_files_logs` (`app/services/server_a_client.py:194`)

- 시그니처: `async def get_files_logs(*, timeout: float=10.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:194` ~ `app/services/server_a_client.py:195` (약 2줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `upload_train_voice_file` (`app/services/server_a_client.py:198`)

- 시그니처: `async def upload_train_voice_file(*, file_tuple: Any, sub_path: str, timeout: float=300.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:198` ~ `app/services/server_a_client.py:206` (약 9줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `upload_training_item` (`app/services/server_a_client.py:209`)

- 시그니처: `async def upload_training_item(*, files: Any, sub_path: str, model_version: str='v2', timeout: float=120.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:209` ~ `app/services/server_a_client.py:217` (약 9줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `delete_file` (`app/services/server_a_client.py:220`)

- 시그니처: `async def delete_file(*, path: str, timeout: float=10.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:220` ~ `app/services/server_a_client.py:227` (약 8줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `mkdir` (`app/services/server_a_client.py:230`)

- 시그니처: `async def mkdir(*, path: str, timeout: float=10.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:230` ~ `app/services/server_a_client.py:237` (약 8줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `trim_audio` (`app/services/server_a_client.py:240`)

- 시그니처: `async def trim_audio(*, source_path: str, max_duration_sec: float, output_suffix: str='_trimmed', timeout: float=30.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:240` ~ `app/services/server_a_client.py:251` (약 12줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `validate_ref_audio` (`app/services/server_a_client.py:254`)

- 시그니처: `async def validate_ref_audio(*, path: str, timeout: float=10.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:254` ~ `app/services/server_a_client.py:261` (약 8줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `prepare_ref_audio` (`app/services/server_a_client.py:264`)

- 시그니처: `async def prepare_ref_audio(*, data: Dict[str, Any], timeout: float=30.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:264` ~ `app/services/server_a_client.py:271` (약 8줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `start_training` (`app/services/server_a_client.py:276`)

- 시그니처: `async def start_training(*, payload: Dict[str, Any], timeout: float=30.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:276` ~ `app/services/server_a_client.py:277` (약 2줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_training_status` (`app/services/server_a_client.py:280`)

- 시그니처: `async def get_training_status(*, model_name: str, timeout: float=10.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:280` ~ `app/services/server_a_client.py:281` (약 2줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_training_log` (`app/services/server_a_client.py:284`)

- 시그니처: `async def get_training_log(*, model_name: str, timeout: float=10.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:284` ~ `app/services/server_a_client.py:285` (약 2줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `call_tts` (`app/services/server_a_client.py:290`)

- 시그니처: `async def call_tts(*, json_body: Dict[str, Any], timeout: Optional[float]=None) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:290` ~ `app/services/server_a_client.py:307` (약 18줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `set_tts_gpt_weights` (`app/services/server_a_client.py:310`)

- 시그니처: `async def set_tts_gpt_weights(*, weights_path: str, timeout: float=10.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:310` ~ `app/services/server_a_client.py:317` (약 8줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `set_tts_sovits_weights` (`app/services/server_a_client.py:320`)

- 시그니처: `async def set_tts_sovits_weights(*, weights_path: str, timeout: float=10.0) -> httpx.Response`
- 라인 범위: `app/services/server_a_client.py:320` ~ `app/services/server_a_client.py:327` (약 8줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `_request`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/services/story_service.py`

#### 파일 역할

- 서비스 계층/보조 로직 모듈

#### 파일 메타

- 총 라인 수: 266
- 클래스 수: 0
- 함수/메서드 수: 6
- 보안 민감도: 높음
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `logger`
- 외부 계약(엔드포인트/서비스/전역 singleton): 전역 singleton/상태: `logger`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `build_story_generation_system_prompt` | 21 | 37 | - | 하 |
| `build_story_generation_messages` | 60 | 26 | - | 하 |
| `ensure_story_auto_mode_gemini_key` | 88 | 29 | - | 하 |
| `call_story_llm_with_selection` | 119 | 67 | - | 중 |
| `parse_story_sections` | 188 | 50 | - | 중 |
| `build_story_mock_plot` | 240 | 27 | - | 하 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `build_story_generation_system_prompt` (`app/services/story_service.py:21`)

- 시그니처: `def build_story_generation_system_prompt(*, user_name: str, opponent_name: str) -> str`
- 라인 범위: `app/services/story_service.py:21` ~ `app/services/story_service.py:57` (약 37줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정): 구현 본문 기준 내부 로직/상위 호출 위주
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `build_story_generation_messages` (`app/services/story_service.py:60`)

- 시그니처: `def build_story_generation_messages(*, system_prompt: str, situation: str) -> List[Dict[str, str]]`
- 라인 범위: `app/services/story_service.py:60` ~ `app/services/story_service.py:85` (약 26줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정): 구현 본문 기준 내부 로직/상위 호출 위주
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `ensure_story_auto_mode_gemini_key` (`app/services/story_service.py:88`)

- 시그니처: `def ensure_story_auto_mode_gemini_key() -> None`
- 라인 범위: `app/services/story_service.py:88` ~ `app/services/story_service.py:116` (약 29줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `getattr`, `HTTPException`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `call_story_llm_with_selection` (`app/services/story_service.py:119`)

- 시그니처: `async def call_story_llm_with_selection(*, messages: List[Dict[str, str]], user_selected_model: Optional[str], temperature: float, max_tokens: int, logger_hint: str='Story generation') -> str`
- 라인 범위: `app/services/story_service.py:119` ~ `app/services/story_service.py:185` (약 67줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `logger.info`, `call_llm`, `isinstance`, `result.get`, `ensure_story_auto_mode_gemini_key`, `getattr`, `logger.warning`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - [보안 민감] cookie/JWT/OAuth 관련 설정/로그 노출 범위 점검
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `parse_story_sections` (`app/services/story_service.py:188`)

- 시그니처: `def parse_story_sections(content: str) -> Tuple[str, str]`
- 라인 범위: `app/services/story_service.py:188` ~ `app/services/story_service.py:237` (약 50줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `text.find`, `text[plot_idx + 5:bg_idx].strip`, `text[bg_idx + 4:].strip`, `text[bg_idx + 4:plot_idx].strip`, `text[plot_idx + 5:].strip`, `text[:bg_idx].strip`, `text.strip`, `logger.warning`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `build_story_mock_plot` (`app/services/story_service.py:240`)

- 시그니처: `def build_story_mock_plot(situation: str) -> str`
- 라인 범위: `app/services/story_service.py:240` ~ `app/services/story_service.py:266` (약 27줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정): 구현 본문 기준 내부 로직/상위 호출 위주
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/services/tts_queue.py`

#### 파일 역할

- 서비스 계층/보조 로직 모듈

#### 파일 메타

- 총 라인 수: 26
- 클래스 수: 0
- 함수/메서드 수: 1
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `DEPRECATED_TTS_QUEUE_MESSAGE`
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `raise_deprecated_tts_queue_usage` | 21 | 6 | - | 하 |

#### 1차/2차 개선 계획

- 1차 계획:
  - deprecated 모듈로 유지, 실제 호출 경로 없음 명시
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)
- deprecated 상태이지만 import 경로 존재 가능성 고려

#### 함수별 상세 분석 (전수)

##### `raise_deprecated_tts_queue_usage` (`app/services/tts_queue.py:21`)

- 시그니처: `def raise_deprecated_tts_queue_usage() -> None`
- 라인 범위: `app/services/tts_queue.py:21` ~ `app/services/tts_queue.py:26` (약 6줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `RuntimeError`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/services/tts_service.py`

#### 파일 역할

- 서비스 계층/보조 로직 모듈

#### 파일 메타

- 총 라인 수: 274
- 클래스 수: 0
- 함수/메서드 수: 11
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `logger`
- 외부 계약(엔드포인트/서비스/전역 singleton): 전역 singleton/상태: `logger`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `_voice_to_dict` | 31 | 11 | - | 하 |
| `_text_hash` | 44 | 2 | - | 하 |
| `get_voice_from_db_dict` | 48 | 12 | - | 하 |
| `_apply_voice_defaults_to_request` | 62 | 12 | - | 하 |
| `resolve_tts_request_defaults` | 76 | 29 | - | 하 |
| `validate_tts_request_ready` | 107 | 7 | - | 하 |
| `lookup_cached_tts_audio_response` | 116 | 31 | - | 하 |
| `call_tts_binary_or_raise` | 149 | 27 | - | 하 |
| `media_type_for_audio_format` | 178 | 8 | - | 하 |
| `build_public_tts_json_response` | 188 | 16 | - | 하 |
| `save_tts_audio_and_optionally_cache` | 206 | 69 | - | 중 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `_voice_to_dict` (`app/services/tts_service.py:31`)

- 시그니처: `def _voice_to_dict(voice: Voice) -> Dict[str, Any]`
- 라인 범위: `app/services/tts_service.py:31` ~ `app/services/tts_service.py:41` (약 11줄)
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정): 구현 본문 기준 내부 로직/상위 호출 위주
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_text_hash` (`app/services/tts_service.py:44`)

- 시그니처: `def _text_hash(text: str) -> str`
- 라인 범위: `app/services/tts_service.py:44` ~ `app/services/tts_service.py:45` (약 2줄)
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `hashlib.sha256((text or '').encode('utf-8')).hexdigest`, `hashlib.sha256`, `(text or '').encode`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `get_voice_from_db_dict` (`app/services/tts_service.py:48`)

- 시그니처: `async def get_voice_from_db_dict(voice_id: str, db: AsyncSession) -> Optional[Dict[str, Any]]`
- 라인 범위: `app/services/tts_service.py:48` ~ `app/services/tts_service.py:59` (약 12줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `db.execute`, `select(Voice).where`, `select`, `result.scalar_one_or_none`, `_voice_to_dict`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_apply_voice_defaults_to_request` (`app/services/tts_service.py:62`)

- 시그니처: `def _apply_voice_defaults_to_request(request: Any, voice_data: Dict[str, Any]) -> None`
- 라인 범위: `app/services/tts_service.py:62` ~ `app/services/tts_service.py:73` (약 12줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `voice_data.get`, `getattr`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `resolve_tts_request_defaults` (`app/services/tts_service.py:76`)

- 시그니처: `async def resolve_tts_request_defaults(*, request: Any, db: AsyncSession, legacy_ref_audio_resolver: Callable[[Optional[str]], Optional[str]]) -> Optional[Dict[str, Any]]`
- 라인 범위: `app/services/tts_service.py:76` ~ `app/services/tts_service.py:104` (약 29줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `getattr`, `get_voice_from_db_dict`, `_apply_voice_defaults_to_request`, `legacy_ref_audio_resolver`, `ValueError`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `validate_tts_request_ready` (`app/services/tts_service.py:107`)

- 시그니처: `def validate_tts_request_ready(request: Any) -> None`
- 라인 범위: `app/services/tts_service.py:107` ~ `app/services/tts_service.py:113` (약 7줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `getattr`, `ValueError`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 일반 예외 raise 또는 상위 전파 가능성 있음 (라우터/호출자 매핑 정책 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `lookup_cached_tts_audio_response` (`app/services/tts_service.py:116`)

- 시그니처: `async def lookup_cached_tts_audio_response(*, request: Any, current_user: Optional[User], db: AsyncSession) -> Optional[Dict[str, Any]]`
- 라인 범위: `app/services/tts_service.py:116` ~ `app/services/tts_service.py:146` (약 31줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `getattr`, `_text_hash`, `db.execute`, `select(AudioFile).where`, `select`, `result.scalar_one_or_none`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `call_tts_binary_or_raise` (`app/services/tts_service.py:149`)

- 시그니처: `async def call_tts_binary_or_raise(*, request: Any, error_prefix: str='TTS Server Error') -> bytes`
- 라인 범위: `app/services/tts_service.py:149` ~ `app/services/tts_service.py:175` (약 27줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `request.model_dump_for_gpt_sovits`, `logger.debug`, `getattr`, `len`, `server_a_client.call_tts`, `server_a_client.ensure_success_response`, `HTTPException`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `media_type_for_audio_format` (`app/services/tts_service.py:178`)

- 시그니처: `def media_type_for_audio_format(audio_format: str) -> str`
- 라인 범위: `app/services/tts_service.py:178` ~ `app/services/tts_service.py:185` (약 8줄)
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `media_type_map.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `build_public_tts_json_response` (`app/services/tts_service.py:188`)

- 시그니처: `def build_public_tts_json_response(*, audio_content: bytes, media_type: str, voice_id: Optional[str]) -> Dict[str, Any]`
- 라인 범위: `app/services/tts_service.py:188` ~ `app/services/tts_service.py:203` (약 16줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `base64.b64encode(audio_content).decode`, `base64.b64encode`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `save_tts_audio_and_optionally_cache` (`app/services/tts_service.py:206`)

- 시그니처: `async def save_tts_audio_and_optionally_cache(*, audio_content: bytes, request: Any, current_user: Optional[User], db: AsyncSession) -> Dict[str, Any]`
- 라인 범위: `app/services/tts_service.py:206` ~ `app/services/tts_service.py:274` (약 69줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `str`, `uuid.uuid4`, `len`, `HTTPException`, `Path`, `audio_dir.mkdir`, `open`, `f.write`, `AudioAnalyzer().analyze_audio`, `AudioAnalyzer`, `AudioFile`, `_text_hash`, `db.add`, `db.commit`, `db.refresh`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - DB 조회/쓰기 가능성
  - 파일/디렉터리 I/O 가능성
- 예외/실패 경로:
  - `HTTPException` 직접 발생/전파 경로가 있음 (상태코드/메시지 형식 유지 여부 확인)
- 보안 포인트:
  - 파일 경로/업로드 입력 검증 및 경로 조작 위험 점검
- 성능 포인트:
  - DB round-trip 횟수/N+1 가능성 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `app/workers/tts_worker.py`

#### 파일 역할

- 비동기/큐 워커 모듈

#### 파일 메타

- 총 라인 수: 234
- 클래스 수: 0
- 함수/메서드 수: 6
- 보안 민감도: 중간
- 변경 위험도(구조/계약): 중간
- 전역 상태/캐시/환경변수 사용(상단 할당): `logger`, `_current_gpt_path`, `_current_sovits_path`, `QUEUE_KEYS`, `STREAM_TTL_SECONDS`
- 외부 계약(엔드포인트/서비스/전역 singleton): 전역 singleton/상태: `logger`

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `_decode_job_payload` | 28 | 8 | - | 하 |
| `_xadd_error` | 38 | 9 | - | 하 |
| `_set_weight_if_needed` | 49 | 26 | - | 하 |
| `_relay_tts_stream` | 77 | 39 | - | 하 |
| `_process_single_job` | 118 | 59 | - | 중 |
| `run_worker` | 178 | 49 | - | 중 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `_decode_job_payload` (`app/workers/tts_worker.py:28`)

- 시그니처: `def _decode_job_payload(job_data)`
- 라인 범위: `app/workers/tts_worker.py:28` ~ `app/workers/tts_worker.py:35` (약 8줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `isinstance`, `job_data.decode`, `json.loads`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 명시적 부작용 낮음/정적 추정 어려움
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_xadd_error` (`app/workers/tts_worker.py:38`)

- 시그니처: `async def _xadd_error(redis_client, stream_key: str, message: str)`
- 라인 범위: `app/workers/tts_worker.py:38` ~ `app/workers/tts_worker.py:46` (약 9줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `redis_client.xadd`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - Redis/Stream I/O 가능성
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - 특이 냄새 낮음(정적 기준)
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_set_weight_if_needed` (`app/workers/tts_worker.py:49`)

- 시그니처: `async def _set_weight_if_needed(client: httpx.AsyncClient, *, tts_base: str, endpoint: str, label: str, weights_path: str | None, current_path: str | None) -> str | None`
- 라인 범위: `app/workers/tts_worker.py:49` ~ `app/workers/tts_worker.py:74` (약 26줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `logger.info`, `client.get`, `logger.warning`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_relay_tts_stream` (`app/workers/tts_worker.py:77`)

- 시그니처: `async def _relay_tts_stream(client: httpx.AsyncClient, redis_client, *, stream_key: str, tts_url: str, request_body: dict) -> int`
- 라인 범위: `app/workers/tts_worker.py:77` ~ `app/workers/tts_worker.py:115` (약 39줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `client.stream`, `response.read`, `error_text.decode`, `logger.warning`, `_xadd_error`, `response.aiter_bytes`, `redis_client.xadd`, `str`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `_process_single_job` (`app/workers/tts_worker.py:118`)

- 시그니처: `async def _process_single_job(redis_client, client: httpx.AsyncClient, *, queue_key, job: dict)`
- 라인 범위: `app/workers/tts_worker.py:118` ~ `app/workers/tts_worker.py:176` (약 59줄)
- 기존 docstring 요약: [역할]
- 역할(1줄): 내부 helper/오케스트레이션 보조 로직
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `job.get`, `logger.info`, `settings.TTS_BASE_URL.rstrip`, `_set_weight_if_needed`, `request_body.pop`, `settings.TTS_API_PATH.lstrip`, `_relay_tts_stream`, `redis_client.expire`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - 중간 이상 길이 함수(50줄+)
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `run_worker` (`app/workers/tts_worker.py:178`)

- 시그니처: `async def run_worker()`
- 라인 범위: `app/workers/tts_worker.py:178` ~ `app/workers/tts_worker.py:226` (약 49줄)
- 기존 docstring 요약: TTS Worker 메인 루프
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `logger.info`, `redis.from_url`, `httpx.AsyncClient`, `redis_client.brpop`, `_decode_job_payload`, `_process_single_job`, `logger.warning`, `asyncio.sleep`, `logger.exception`, `client.aclose`, `redis_client.close`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - broad exception 사용
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

### `run.py`

#### 파일 역할

- 루트 스크립트/보조 실행 파일

#### 파일 메타

- 총 라인 수: 26
- 클래스 수: 0
- 함수/메서드 수: 0
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): 특이 사항 적음
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

- 함수/메서드 없음

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

### `test_ollama_connection.py`

#### 파일 역할

- 루트 스크립트/보조 실행 파일

#### 파일 메타

- 총 라인 수: 142
- 클래스 수: 0
- 함수/메서드 수: 4
- 보안 민감도: 낮음
- 변경 위험도(구조/계약): 낮음
- 전역 상태/캐시/환경변수 사용(상단 할당): `OLLAMA_BASE_URL`
- 외부 계약(엔드포인트/서비스/전역 singleton): 명시적 외부 계약은 라우터 엔드포인트보다 내부 helper/모듈 함수 중심

#### 주요 클래스/함수 목록 요약

| 함수 | 라인 | 길이 | 엔드포인트 | 우선순위 |
|---|---|---|---|---|
| `test_ollama_version` | 23 | 16 | - | 하 |
| `test_ollama_models` | 41 | 15 | - | 하 |
| `test_ollama_chat` | 58 | 43 | - | 중 |
| `main` | 103 | 36 | - | 하 |

#### 1차/2차 개선 계획

- 1차 계획:
  - 외부 계약 불변 상태에서 긴 함수/예외 정책/로깅 일관성 중심 보수적 정리
- 2차 계획 후보:
  - 테스트/모니터링/운영 정책 문서화 강화

#### 호환성 주의사항

- HTTP path/method/응답 주요 key 변경 금지 (1차 개선 원칙)

#### 함수별 상세 분석 (전수)

##### `test_ollama_version` (`test_ollama_connection.py:23`)

- 시그니처: `async def test_ollama_version() -> bool`
- 라인 범위: `test_ollama_connection.py:23` ~ `test_ollama_connection.py:38` (약 16줄)
- 기존 docstring 요약: Ollama 버전 정보 확인
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `httpx.AsyncClient`, `client.get`, `response.raise_for_status`, `response.json`, `print`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - broad exception 사용
  - print 기반 로그
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `test_ollama_models` (`test_ollama_connection.py:41`)

- 시그니처: `async def test_ollama_models() -> Optional[list]`
- 라인 범위: `test_ollama_connection.py:41` ~ `test_ollama_connection.py:55` (약 15줄)
- 기존 docstring 요약: 사용 가능한 모델 목록 조회
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `httpx.AsyncClient`, `client.get`, `response.raise_for_status`, `response.json`, `models_data.get`, `print`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - broad exception 사용
  - print 기반 로그
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `test_ollama_chat` (`test_ollama_connection.py:58`)

- 시그니처: `async def test_ollama_chat(model: str='gemma-3-27b-it') -> bool`
- 라인 범위: `test_ollama_connection.py:58` ~ `test_ollama_connection.py:100` (약 43줄)
- 기존 docstring 요약: Ollama 채팅 API 테스트
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `print`, `httpx.AsyncClient`, `client.post`, `response.raise_for_status`, `response.json`, `result.get('message', {}).get`, `result.get`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 외부 HTTP 호출 가능성
  - Redis/Stream I/O 가능성
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
  - `except Exception` 사용: 유지 사유 주석/에러 매핑 정책 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 외부 HTTP 호출 timeout/재시도/에러 변환 중복 여부 확인
- 중복/냄새(code smell):
  - broad exception 사용
  - print 기반 로그
- 분석/개선 우선순위: **중**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인
##### `main` (`test_ollama_connection.py:103`)

- 시그니처: `async def main()`
- 라인 범위: `test_ollama_connection.py:103` ~ `test_ollama_connection.py:138` (약 36줄)
- 기존 docstring 요약: 메인 테스트 함수
- 역할(1줄): 내부 로직/유틸 처리
- 호출 주체(누가 부름): 같은 파일 내부 helper / 서비스 / 외부 호출자(정적 분석 기준)
- 호출 대상(정적 분석 추정, 상위 15개): `print`, `test_ollama_version`, `sys.exit`, `test_ollama_models`, `test_ollama_chat`
- 입력/출력 의미:
  - 입력: 함수 시그니처 기준 파라미터 (실제 의미는 호출 문맥/라우터·서비스 문맥과 함께 확인)
  - 출력: 반환값 또는 `HTTPException`/예외 전파 형태
- 부작용(DB, 파일, Redis, 외부 HTTP, 쿠키, JWT, 로그):
  - 로그 출력
- 예외/실패 경로:
  - 명시적 raise 적음; 호출 대상(DB/HTTP/파일) 예외 전파 여부 확인 필요
- 보안 포인트:
  - 특이 보안 포인트 낮음 (예외/로그/입력 검증 중심 점검)
- 성능 포인트:
  - 순수 가공 로직 비중이 크면 병목 가능성 낮음 (호출 빈도 기준 재확인)
- 중복/냄새(code smell):
  - print 기반 로그
- 분석/개선 우선순위: **하**
- 보수적 개선 방법 (구체적):
  - 호출 순서/반환 계약 유지 상태에서 helper 경계/예외 정책/로그 메시지 정리
- 테스트 포인트 (정상/실패/경계값):
  - 정상 입력 반환값 유지 확인
  - 경계값(None/빈 문자열/빈 목록/잘못된 타입) 처리 정책 확인
  - 예외 경로에서 상위 호출자 계약(raise/폴백/로그) 유지 확인

## 보안 점검 결과 (1차: 경고+플래그화)

### 현재 상태 요약 (정적 기준)

- 보안 민감 기본값은 최대한 유지하면서 설정 플래그화/경고 중심 접근을 유지한다.
- `auth.py`, `deps.py`, `main.py`, `config.py` 축은 1차 목표(경고 + 설정화) 반영 상태를 전제로 한다.
- 실환경 검증은 본 라운드에서 생략했고, 문법/논리/문서 정합성 기준으로만 확인했다.

### 정적 점검 스냅샷 (코드 기준)

- broad exception 사용 함수 수(정적 탐지): **58개**
- bare except 사용 함수 수(정적 탐지): **0개**
- `print(` 포함 함수 수(정적 탐지): **4개**
- `os.getenv(` 설정 외부 직접 사용 파일 수(정적 탐지): **1개**

### 1차 체크리스트 (계획 포함)

- `dev-user fallback` 가시화 및 설정화 유지
- Google OAuth insecure HTTP 옵션 설정화 유지
- auth cookie secure/samesite 설정화 유지
- startup schema patch 위험성 문서화 + 설정화 유지
- 민감 데이터 로그 출력 여부 점검 (`print`, exception message) 지속
- 외부 요청 timeout/SSL verify 설정 일관성 점검 지속
- 파일 경로/업로드 검증 재확인 (`voices.py`, `model_make.py`, `tts.py`)

### 2차 후보 (이번 문서에만 명시, 구현 별도)

- prod에서 dev fallback 기본 비활성
- insecure Google SSO 기본 비활성
- cookie secure 기본 true (HTTPS 전제)
- startup schema patch 제거 및 마이그레이션 도구로 전환
- 세부 예외 메시지 외부 노출 최소화

## 함수 분석 기반 개선 실행 순서

### 실행 원칙 (고정)

- 외부 계약 유지(path/method/응답 주요 key/DB 의미/Redis 계약/Server A 계약)
- 문서 canonical -> 코드 보수적 개선 -> 문서 재동기화 순서 유지
- 라우터는 transport 역할 중심, 복잡한 오케스트레이션은 service/helper로 단계 분리
- broad exception 유지가 필요한 곳은 이유 주석 필수

### Phase별 요약 (현 상태 반영)

1. Phase R0/1: 완료 (초대형 문서 canonical + 코드 기준 재생성 + 함수 헤딩 정합성 체계 확립)
2. Phase B: 완료 (TTS/Server A 공통화 축 + worker/queue 보수적 정리 반영)
3. Phase C: 완료 (chat/characters/story 오케스트레이션 보수적 분해 및 섹션 경계 정리 반영)
4. Phase 6: 완료 (LLM helper 분리 + runtime logging 정리 + startup validation warn-only 연결 반영)
5. Phase 7: 완료 (정적/문서 정합성 기준, 실환경 B/C 회귀는 후속 환경/CI에서 누적 검증)

## 회귀 테스트/검증 시나리오

### A. 정적/구조 검증 (이번 라운드 기본)

1. `python3 -m compileall -q app run.py test_ollama_connection.py`
2. AST vs 문서 함수 헤딩 정합성 (파일 수/정의 수/헤딩 수/누락 수)
3. 함수 헤딩 라인번호 드리프트 점검 (허용 기준 ±5줄, 가능하면 0)
4. `rg` 품질 체크 (`print(`, `except Exception`, `httpx.AsyncClient(`, `os.getenv(`)

### B. 기능 회귀 (후속 환경에서 실행 예정)

- 현재 라운드에서 실환경 의존성/환경 제약이 있는 경우 **실환경 회귀를 생략**하고, Phase 7 결과는 하단 Canonical 정적 검증 섹션으로 관리한다.
- `/auth/*`, `/chat`, `/chat/stream`, `/tts`, `/voices`, `/model_make`, `/characters`, `/system`
- 상태코드/응답 key/오류 응답 패턴/외부 서비스 다운 시 graceful degradation 확인

### C. 보안/설정 검증 (후속 환경에서 실행 예정)

- 현재 라운드에서 환경 토글 검증이 어려운 경우 **실환경/환경값 토글 검증을 생략**하고, 문서/코드 정합성만 먼저 확인한다.
- `DEV_AUTH_FALLBACK_ENABLED`, `GOOGLE_SSO_ALLOW_INSECURE_HTTP`, `AUTH_COOKIE_SECURE`, `AUTH_COOKIE_SAMESITE`, `STARTUP_SCHEMA_PATCH_ENABLED`, `STRICT_STARTUP_VALIDATION`

### D. 문서 UX 검증

- 문서 상단 요약과 Canonical 수치 일치
- 함수 헤딩 라인번호로 실제 함수 점프 가능
- Phase 상태표가 현재 코드 구조와 모순되지 않음

## Phase 7 정합성 재검증 결과 (현재 Canonical, 2026-02-24)

- 코드 파일 수(검증 대상): **42** (`app/**/*.py` + `run.py` + `test_ollama_connection.py`)
- 코드 함수/메서드 정의 수(AST, nested 포함 / class 헤딩 제외 기준): **238**
- 문서 함수 헤딩 수(`#####`): **238**
- 누락 함수/메서드 정의 수: **0**
- 문서에만 존재하는 추가 함수/메서드 정의 수: **0**
- 함수 헤딩 라인번호 드리프트 수(재생성 기준): **0**
- 함수 헤딩 라인번호 최대 드리프트: **0줄**
- 라우터 엔드포인트 데코레이터 수(`app/api/*`): **54**
- 참고: 본 문서는 재생성 베이스라인이므로 함수 헤딩 라인번호는 생성 시점 코드와 일치한다.
- 연결 규칙: 상단 `전체 구조 요약`의 정량 수치와 `현재 Phase 진행 상태`의 Phase 7 상태 판단은 본 Canonical 재검증 섹션 수치를 기준으로 해석한다.
- 실행 범위 정책(현재 라운드): A(정적/구조 검증)는 실행했으며, B/C(기능/보안 실환경 회귀)는 환경/의존성 준비 상태에 따라 후속 실행한다.
- B/C(기능/보안 실환경 회귀) 결과는 **본 Canonical 섹션 바로 아래**에 날짜와 함께 누적 기록한다.

### Phase 7 B/C 실환경 검증 결과 누적 (정책 변경 시)

- 현재 라운드: 미실행 (사용자 정책에 따라 제외).
- 정책 변경 후 실행 시, B(기능 회귀)/C(보안 토글 검증) 결과를 이 섹션에 날짜와 함께 append한다.

## 변경 이력 / TODO / 후속 과제

### 이번 라운드 재구축 메모

- 손상된 초대형 문서를 코드 기준으로 재생성(베이스라인 재구축)했다.
- 수동 누적 Appendix/메모 일부는 복원되지 않을 수 있다.
- 후속 라운드에서 도메인별 수동 설명(운영 맥락/주의사항/디버깅 팁)을 다시 보강한다.

### 후속 과제 (우선순위)

1. 수동 Appendix 운영 체크리스트 템플릿(정확 로그/관련 env/확인 순서)을 신규 도메인 메모에도 동일 형식으로 유지
2. broad exception 잔여 구간은 2차 품질개선 단계에서 테스트 자산과 함께 추가 축소 재평가
3. 실환경 가능한 셸/CI에서 Phase 7 B/C(기능/보안) 검증 결과를 Canonical 섹션 아래 누적 기록

<!-- MANUAL_APPENDIX_START -->
## 수동 Appendix (도메인별 운영/디버깅 메모, 재생성 시 보존)

### 사용 규칙

- 이 구간은 코드 자동 재생성 대상이 아니며, 스크립트가 마커 기준으로 그대로 보존한다.
- 도메인별 운영 메모/디버깅 포인트/호환성 주의사항을 누적한다.

### `auth.py` / OAuth 콜백 도메인 메모

- `google_callback`는 현재 "검증 -> DB 조회/생성 -> JWT 발급 -> 리다이렉트/쿠키" 단계 helper 순서가 핵심 계약이다.
- 이 순서를 바꾸면 `user_info` 미검증 상태에서 DB/JWT 단계로 넘어가는 회귀가 생길 수 있다.
- 1차 정책상 cookie 옵션(`secure`, `samesite`) 기본값은 기존 동작 호환을 유지하고, 실제 운영 강제화는 2차 후보로 남긴다.
- `fastapi-sso` 예외 타입은 버전/환경 편차가 있어 `_verify_google_user_info_or_raise`의 broad except -> `400` 매핑을 일부 유지한다.

### `characters.py` 도메인 메모 (preset vs DB, dev/user/admin 혼합)

- 이 파일은 의도적으로 경계가 섞여 있다: `dev` 편의 CRUD + 사용자 CRUD + 관리자 preset/voice patch + AI generate.
- 1차 리팩토링에서 path 분리보다 "섹션 주석 + helper 재사용 + 예외 정책 고정"을 우선한 이유는 프론트/운영 호환성 때문이다.
- preset 상세/관리자 patch는 `character_service.load_preset_character_by_id_from_disk()`를 통해 "preset 우선 -> DB fallback" 가독성을 높인 상태다.
- broad `except Exception`는 현재 6개가 남아 있으며, 주로 변경 라우트(create/update/delete/admin patch)와 `/generate` mock fallback 계약 때문에 유지된다.
- 조회/목록 경로는 가능한 범위에서 `SQLAlchemyError`, `(TypeError, ValueError)` 등 typed except로 먼저 줄였다.

### `ai.py` 도메인 메모 (LLM 생성/파싱)

- `generate_character_details`는 단계 주석(1~5) 기준으로 읽으면 된다:
- 1) 시스템 프롬프트 제약 고정
- 2) 사용자 입력 정규화
- 3) 메시지 구성
- 4) Gemini -> Ollama fallback 호출
- 5) JSON 파싱 + 리스트 필드 보정
- `current_user` dependency는 본문에서 직접 쓰지 않아도 "인증 필요 생성 엔드포인트" 정책을 고정하기 위한 장치다.
- 1차에서는 스키마 엄격 검증보다 응답 shape 유지(`success/is_fallback/data`)가 우선이며, 2차에서 schema validation 계층 추가 후보.

### `chat.py` / `context_manager.py` / `llm.py` 메모

- `chat.py`는 핵심 오케스트레이션이 service/helper로 많이 빠졌지만, 여전히 응답 shape와 SSE 이벤트 필드(`content`, `done`, `full_content`, `session_id`)가 최우선 계약이다.
- `chat_stream`은 저장 정책/로그 정책이 일반 chat과 다를 수 있으므로(특히 DB 저장 여부) 변경 시 프론트 사용 흐름과 함께 확인한다.
- `llm.py`는 provider helper 분리로 길이는 줄었지만, provider별 예외 메시지/타임아웃/모델명 fallback 정책은 cross-cutting이라 문서와 같이 관리해야 한다.

### `tts.py` / `voices.py` / `model_make.py` / `tts_worker.py` 메모

- Server A 연동 축은 `server_a_client.py` 공통화 이후에도 라우터별 사용자-facing 에러 detail 차이가 일부 남아 있다.
- 1차에서는 공통화 범위를 "연결 실패 + non-200 변환" 중심으로 제한하고, 업무 규칙/메시지는 라우터에 남기는 정책을 유지한다.
- `tts_worker.py`는 현재 단계 함수 분해 수준을 유지하고, 추가 대규모 구조 변경보다 큐/스트림 계약 안정성(이벤트 포맷/TTL)을 우선한다.
- `tts_queue.py`는 deprecated 모듈로 유지되므로, import 존재 여부만 확인하고 실제 동작 경로로 재사용하지 않는다.

### 문서/재생성 워크플로 메모

- `scripts/rebuild_backend_function_role_doc.py`는 베이스라인 재구축용이며, 함수 헤딩/핫스팟/인벤토리/정합성 섹션을 코드 기준으로 재생성한다.
- 이 수동 Appendix 구간은 시작/끝 마커 전용 라인 사이만 수동 편집한다.
- 재생성 전후 diff가 크더라도, 먼저 "마커 보존 + 수동 메모 보존" 여부를 확인한다.

### 실제 장애 대응 포인트 (도메인별 운영 메모 누적)

#### `characters.py`

- 증상: 관리자 preset voice patch 후 재조회 시 `voice_id` 반영이 안 보임
- 1차 확인:
  - `character_service.update_preset_character_voice_id()` 호출 여부
  - `invalidate_preset_character_cache()` 호출 여부
  - `_filename` 메타 기준 대상 파일이 맞는지 (`id` fallback 오인 여부)
- 주의:
  - 이름(name) 중복 가능성이 있으므로 운영 확인은 `id` 기준으로 한다.

#### `auth.py` / OAuth callback

- 증상: 로그인 성공처럼 보이지만 프론트에서 세션 유지가 안 됨
- 1차 확인:
  - `AUTH_COOKIE_SECURE`, `AUTH_COOKIE_SAMESITE` 값
  - 프론트/백엔드 스킴(HTTP/HTTPS) 조합
  - `_build_auth_callback_redirect_response`에서 쿠키 옵션 적용 여부
- 주의:
  - 로컬 HTTP 환경 호환 정책(`secure=false`)과 운영 HTTPS 강제 정책을 혼동하지 않기.

#### `ai.py` / character generate

- 증상: `/generate` 응답은 성공인데 품질이 급격히 나빠짐
- 1차 확인:
  - Gemini -> local fallback -> mock 중 실제 사용 단계 로그
  - `Character generate fallback chain failed` 로그 존재 여부
  - JSON 파싱 후 `tags`/필수 필드 보정에서 비정형 데이터가 들어왔는지
- 주의:
  - 1차 정책에서는 성공 shape 유지가 우선이라 품질 저하가 `success=true`로 보일 수 있다.

#### `chat.py` / `llm.py`

- 증상: `/chat`는 되는데 `/chat/stream` 프론트 렌더링이 깨짐
- 1차 확인:
  - SSE 최종 프레임에 `done`, `full_content`, `session_id` 키 유지 여부
  - `call_llm_stream` provider 분기(`gemini-` prefix 기반) 확인
  - 에러가 HTTP 응답으로 바뀐 게 아니라 SSE 에러 프레임으로 내려오는지 확인
- 주의:
  - Ollama 경로는 현재 non-stream fallback이라 한 번에 yield될 수 있다.

#### `tts.py` / `tts_worker.py` / Server A 연동

- 증상: 채팅 텍스트는 정상인데 `audio_url`만 누락됨
- 1차 확인:
  - `_maybe_attach_tts_audio_url` 경고 로그
  - `resolve_character_voice_id_for_tts` 결과값
  - `_synthesize_tts_internal` 반환 shape(`success/data/audio_url`) 유지 여부
- 주의:
  - `/chat`에서 TTS 실패는 의도적으로 전체 실패로 전파하지 않는다(텍스트 응답 계약 우선).

### 운영 체크리스트 (로그 키 / 환경 변수 / 우선 확인 순서)

#### `characters.py` (preset/DB 혼합)

- 우선 확인 순서:
  1. 요청이 `dev/user/admin/generate` 중 어떤 섹션 라우트인지 확인
  2. preset 우선 경로인지(DB fallback 포함) 확인
  3. `HTTPException`인지 내부 500 매핑인지 확인
  4. DB commit/rollback 경로 여부 확인
- 실제 로그 문자열(코드 기준):
  - `logger.warning("%s (bad request): %s", log_context, exc)` (`_raise_bad_request`)
  - `logger.exception("%s", log_context)` (`_raise_internal_server_error`, `_rollback_and_raise_internal_server_error`)
  - `logger.warning("DB rollback failed while handling %s: %s", log_context, rollback_error)`
  - `Character generate fallback chain failed, using mock data: %s`
  - 주요 `log_context` 값(코드 기준): `Character create failed (/characters dev unexpected)`, `Character create failed (/characters/my unexpected)`, `Admin character voice update failed (/characters/admin/{id}/voice unexpected)`, `Character update failed (/characters/{id} unexpected)`, `Character delete failed (/characters/{id} unexpected)`
- 관련 env/설정:
  - preset 경로: `PRESET_CHARACTERS_DIR` (간접)
  - 인증 fallback 정책과 간접 연동: `DEV_AUTH_FALLBACK_ENABLED`
- 운영용 우선 확인 순서(도메인별):
  1. 요청 경로가 `dev/user/admin/generate` 중 어느 섹션인지 먼저 분류한다.
  2. `HTTPException` 응답인지 helper 기반 내부 500 매핑인지 분리한다.
  3. 내부 500이면 `logger.exception("%s", log_context)`의 `log_context` 문자열로 라우트/단계를 특정한다.
  4. 생성/수정/삭제 경로는 DB rollback 로그(`DB rollback failed while handling ...`) 존재 여부로 rollback 단계 이상을 함께 확인한다.
  5. `/generate`는 `Character generate fallback chain failed, using mock data: %s` 로그 여부와 `success=true` fallback 응답을 함께 확인한다.
- 추가 확인 포인트:
  - 잔여 broad except 6개는 추가 typed pre-catch 확대보다 `log_context` 경로 식별자 + 사유 주석 유지 전략으로 마감한다.
  - 이유: `/generate` mock fallback 계약 + 변경 라우트의 기존 500 shape 유지가 우선이며, 과도한 분기는 유지보수 이득이 작다.

#### `auth.py` (OAuth callback / 쿠키)

- 우선 확인 순서:
  1. Google user info 검증 단계 실패인지 확인
  2. DB user upsert 단계 실패인지 확인
  3. JWT 생성 단계 실패인지 확인
  4. redirect/cookie 설정 단계 실패인지 확인
- 실제 로그 문자열(코드 기준):
  - `Google 인증 타임아웃 (30초 초과)`
  - `Google 인증 실패: %s`
  - `사용자 조회 실패: %s`
  - `사용자 조회 실패(비정형 예외): %s`
  - `새 사용자 생성: %s`
  - `사용자 생성 실패: %s`
  - `사용자 생성 실패(비정형 예외): %s`
  - `JWT 생성 실패: %s`
- 관련 env/설정:
  - `GOOGLE_SSO_ALLOW_INSECURE_HTTP`
  - `AUTH_COOKIE_SECURE`
  - `AUTH_COOKIE_SAMESITE`
  - `FRONTEND_URL`
- 운영용 우선 확인 순서(도메인별):
  1. `GOOGLE_SSO_ALLOW_INSECURE_HTTP`와 현재 스킴(HTTP/HTTPS) 조합 확인
  2. `Google 인증 타임아웃 (30초 초과)` 또는 `Google 인증 실패: %s` 로그 여부 확인
  3. 사용자 조회/생성 단계 로그(`사용자 조회 실패...`, `사용자 생성 실패...`) 확인
  4. `JWT 생성 실패: %s` 여부 확인
  5. 쿠키 옵션(`AUTH_COOKIE_SECURE`, `AUTH_COOKIE_SAMESITE`)과 프론트 리다이렉트(`FRONTEND_URL`) 조합 확인
- 추가 확인 포인트:
  - `fastapi-sso` 버전/환경 차이로 verify 예외 타입이 달라질 수 있어 `_verify_google_user_info_or_raise`의 broad except -> `400` 매핑을 유지한다.
  - 브라우저 측 cookie 정책 이슈는 서버 응답 성공과 별개로 체감 로그인 실패를 만들 수 있다.

#### `ai.py` (character generate / fallback chain)

- 우선 확인 순서:
  1. Gemini 호출 실패 여부
  2. Local LLM fallback 실패 여부
  3. JSON 파싱 실패 여부
  4. Mock fallback 응답 사용 여부(`success=true`라도 품질 저하 가능)
- 실제 로그 문자열(코드 기준):
  - `Character generate fallback chain failed, using mock data: %s`
  - `Character generate (/api/characters/generate)` (logger hint / service 호출 라벨)
- 관련 env/설정:
  - `GEMINI_API_KEY`
  - `GOOGLE_API_MODEL`
  - `GOOGLE_SAFETY_THRESHOLD`
  - `OLLAMA_BASE_URL`
- 운영용 우선 확인 순서(도메인별):
  1. Gemini 실패인지 Local LLM fallback 실패인지 먼저 분리
  2. `Character generate fallback chain failed, using mock data: %s` 로그 여부 확인
  3. fallback 응답(`success=true`)을 성공으로만 집계하지 말고 품질 저하 여부를 함께 확인
  4. 최근 프롬프트/파싱 규칙 변경 시 JSON 형식 회귀를 우선 의심
- 추가 확인 포인트:
  - `current_user` dependency는 인증 정책 고정용이라 미사용처럼 보여도 제거 시 경로 정책 회귀가 생길 수 있다.
  - `tags`는 문자열/배열 혼합 응답이 들어올 수 있어 normalize 단계 이후 최종 응답을 기준으로 확인한다.

#### `chat.py` / `llm.py` (일반/스트리밍)

- 우선 확인 순서:
  1. `/chat` vs `/chat/stream` 경로 구분
  2. model prefix 기준 provider 분기(`gemini-` vs 기타) 확인
  3. SSE 최종 프레임 shape 유지 여부 확인(`done/full_content/session_id`)
  4. TTS 부가 기능 실패가 본문 응답까지 전파됐는지 여부 확인
- 실제 로그 문자열(코드 기준):
  - `캐릭터 조회 실패: {e}`
  - `TTS 합성 실패(audio_url 미포함): %s`
  - `스트리밍 완료: session=%s, length=%s`
  - `스트리밍 오류: %s`
  - `감독 모드 파싱 실패: %s`
  - `LLM service error: {status_code}`
  - `Chat API error: {e}`
  - `Invalid GOOGLE_SAFETY_THRESHOLD=%r. Falling back to BLOCK_NONE. Allowed: ...`
  - `Gemini 호출 실패 (%s): %s`
  - `Gemini 스트리밍 호출 실패 (%s): %s`
  - `LLM 호출 실패: %s`
- 관련 env/설정:
  - `GEMINI_API_KEY`, `GOOGLE_API_MODEL`, `GOOGLE_SAFETY_THRESHOLD`
  - `OLLAMA_BASE_URL`, `OLLAMA_API_PATH`, `OLLAMA_SSL_VERIFY`
- 운영용 우선 확인 순서(도메인별):
  1. `/chat`(일반 응답)인지 `/chat/stream`(SSE)인지 먼저 구분
  2. `model` 접두사로 Gemini/Ollama provider 분기 확인
  3. `/chat` 실패는 `LLM service error...`(HTTP 상태) vs `Chat API error...`(mock fallback)로 분리
  4. `/chat/stream`은 `스트리밍 오류: %s` 로그와 최종 SSE 프레임 shape를 함께 확인
  5. Gemini 계열이면 `Gemini 호출 실패...` / `Gemini 스트리밍 호출 실패...` 로그를 우선 확인
- 추가 확인 포인트:
  - `/chat`의 TTS 부가 기능 실패는 본문 텍스트 응답 계약상 전파되지 않아 `audio_url`만 빠질 수 있다.
  - `GOOGLE_SAFETY_THRESHOLD` 오탈자는 호출 실패가 아니라 경고 후 `BLOCK_NONE` 폴백으로 이어질 수 있다.

#### `tts.py` / `server_a_client.py` (TTS API/프록시)

- 우선 확인 순서:
  1. `/chat` 부가 TTS인지 `/tts` 직접 호출인지 구분
  2. voice_id 해석(DB -> legacy JSON fallback) 결과 확인
  3. Server A 연결 실패(RequestError)인지 non-200 상태코드인지 구분
  4. 파일 저장/캐시 저장 단계 실패인지 확인
- 실제 로그 문자열(코드 기준):
  - `TTS Prepare: Voice not found for {voice_id}`
  - `Loaded GPT weights for %s`
  - `Failed to set GPT weights: status=%s body=%s`
  - `Failed to set GPT weights: {e}`
  - `Loaded SoVITS weights for %s`
  - `Failed to set SoVITS weights: status=%s body=%s`
  - `Failed to set SoVITS weights: {e}`
  - `TTS internal synthesis failed: %s`
  - `Public TTS synthesis failed: %s`
  - `%s request failed: method=%s url=%s err=%s` (`server_a_client._request`)
- 관련 env/설정:
  - `TTS_BASE_URL`, `TTS_TIMEOUT`, `TTS_SSL_VERIFY`, `TTS_API_PATH`
  - `SERVER_A_FILES_API_URL`, `SERVER_A_FILES_API_BASE_URL`
  - `SERVER_A_TRAINING_API_URL`, `SERVER_A_TRAINING_API_BASE_URL`
- 운영용 우선 확인 순서(도메인별):
  1. `/chat` 경유면 본문 성공 + `audio_url` 누락인지, `/tts` 직접 호출이면 API 실패인지 구분
  2. `TTS Prepare...`/weight load 로그로 voice 준비 단계 실패 여부 확인
  3. `... request failed: method=... url=...` 로그로 Server A/TTS 연결 실패 여부 확인
  4. `TTS internal synthesis failed...` vs `Public TTS synthesis failed...`로 내부 wrapper/공개 라우터 경계 구분
  5. non-200 응답이면 `ensure_success_response()` 변환 detail에 상태코드/본문 일부가 붙었는지 확인
- 추가 확인 포인트:
  - `server_a_client` 로그의 `%s` 서비스 라벨은 호출 서비스 종류(예: Server A TTS API)에 따라 달라진다.
  - `/chat` TTS 실패는 텍스트 응답 계약상 전파되지 않을 수 있으므로 `audio_url` 존재 여부를 별도로 확인한다.

#### `tts_worker.py` (Redis queue worker)

- 우선 확인 순서:
  1. 프로세스 기동/Redis 연결 여부 확인
  2. 큐 소비(BRPOP) 여부 확인
  3. weight 전환 단계 실패인지 TTS 요청 단계 실패인지 구분
  4. stream relay 실패인지 Redis 재연결 루프인지 구분
- 실제 로그 문자열(코드 기준):
  - `Connecting to Redis: %s`
  - `TTS Worker started. Waiting for jobs...`
  - `Processing job %s from %s`
  - `Setting %s weights: %s`
  - `Failed to set %s weights: status=%s body=%s`
  - `Error setting %s weights: %s`
  - `TTS Request failed: %s`
  - `Job %s completed. %s chunks relayed.`
  - `Redis connection lost. Retrying in 1s...`
  - `Worker loop error: %s`
  - Redis Stream error 이벤트 payload: `{type: \"error\", message: ...}` (`message`는 길이 제한됨)
- 관련 env/설정:
  - `REDIS_URL`
  - `TTS_BASE_URL`
  - `TTS_API_PATH`
  - (간접 영향) `TTS_TIMEOUT`, `TTS_SSL_VERIFY`는 API 라우터 경로와 혼동하지 않도록 분리해서 확인
- 운영용 우선 확인 순서(도메인별):
  1. `Connecting to Redis: %s` 직후 `TTS Worker started...` 로그가 찍히는지 확인 (프로세스 기동 여부)
  2. 작업이 안 빠지면 `Processing job %s from %s` 로그 존재 여부로 BRPOP/큐 소비 여부 확인
  3. 모델 전환 이슈면 `Setting ... weights` / `Failed to set ...` / `Error setting ...` 순으로 확인
  4. 스트리밍 relay 실패면 `TTS Request failed: %s` 또는 Stream `error` 이벤트 payload 확인
  5. 반복 장애면 `Redis connection lost...` / `Worker loop error: %s` 빈도로 Redis/네트워크 이슈 먼저 분류
- 추가 확인 포인트:
  - Redis Stream `error` payload의 `message`는 길이 제한되므로 원인 상세는 서버 로그와 함께 확인한다.
  - worker 장애는 `/tts` 직접 호출 실패와 별개일 수 있으므로 API 라우터 로그와 혼동하지 않게 분리 추적한다.

#### `voices.py` (관리자 파일 매니저 / Voice CRUD / 학습 프록시)

- 우선 확인 순서:
  1. 실패 구간이 `server-files`(파일관리) / `voices CRUD` / `voices/train/*`(학습 프록시) 중 어디인지 먼저 분류
  2. `server_a_client.ensure_success_response()`에서 난 non-200 변환인지, 라우터 로컬 검증(확장자/ref_audio 정책) 실패인지 구분
  3. `process_ref_audio()` 경유 여부 확인 (`create_voice`, `update_voice`에서 ref_audio 변경 시)
  4. 404 허용 경로(status/log polling)인지 실제 장애인지 확인
- 실제 로그 문자열(코드 기준):
  - `Upload Failed`
  - `Delete Failed`
  - `Mkdir Failed`
  - `Trim Failed`
  - `Training Start Failed`
  - `Status Check Failed`
  - `Log Retrieval Failed`
  - `TTS 서비스 오류`
  - `Server A connection failed` (ref_audio 검증/자동 트리밍 경로)
- `voices/train/*` 세부 확인 (정확 문구 기준):
  - `/voices/train/start` 실패 시: `Training Start Failed`
  - `/voices/train/status/{model_name}` 실패 시: `Status Check Failed`
  - `/voices/train/log/{model_name}` 실패 시: `Log Retrieval Failed`
  - 위 3개는 `server_a_client.ensure_success_response()`에서 상태코드/본문 일부를 붙여 `HTTPException.detail`로 변환될 수 있음
- 관련 env/설정:
  - `SERVER_A_FILES_API_URL`, `SERVER_A_FILES_API_BASE_URL`
  - `SERVER_A_TRAINING_API_URL`, `SERVER_A_TRAINING_API_BASE_URL`
  - `TTS_BASE_URL`, `TTS_TIMEOUT`
  - `SERVER_A_REQUEST_TIMEOUT` 계열(있다면 우선 적용 지점 확인)
- 운영용 우선 확인 순서(도메인별):
  1. 요청 경로가 start/status/log 중 무엇인지 확인 (polling인지 단발 요청인지)
  2. `SERVER_A_TRAINING_API_URL` 값/스킴(HTTP/HTTPS) 확인
  3. `allow_404_detail` 허용 경로(status/log)인지 확인 후 404를 장애로 분류할지 판단
  4. non-200이면 Server A 응답 본문이 프록시 detail에 포함됐는지 확인 (`...Failed: ...`)
  5. 모델명 오탈자/공백(`model_name`) 여부 확인
- 추가 확인 포인트:
  - `get_server_files()`가 통합 인덱스 실패 후 개별 API 폴백으로 내려갔는지
  - `allow_404_detail`가 설정된 학습 status/log 경로에서 404를 과잉 장애로 해석하지 않았는지
  - `test_voice()` 응답 shape(`success/data/audio_base64/format`)가 유지되는지

#### `model_make.py` (업로드/학습/등록/정리)

- 우선 확인 순서:
  1. `/upload` 검증 단계(개수/확장자/용량/길이) 실패인지 Server A 업로드 실패인지 구분
  2. `/start`에서 `train_input_dir -> upload_path` 조합이 올바른지 확인
  3. `/register`에서 `prepare-ref-audio` 실패 흡수 후 원본 사용으로 계속 진행됐는지 확인
  4. `/register`의 가중치 자동 추론(`get_files_logs`) 실패가 Voice 등록 실패로 오해되지 않았는지 확인
  5. `/abort`, `/my/{voice_id}` 정리 경로에서 Server A 삭제 실패가 의도적으로 흡수되는 정책인지 확인
- 실제 로그 문자열(코드 기준):
  - `Server A 업로드 실패(<filename>)`
  - `학습 시작 실패`
  - `학습 상태 조회 실패`
  - `학습 로그 조회 실패`
  - `prepare-ref-audio failed: ...`
  - `prepare-ref-audio error: ...`
  - `ref_audio prepared: ...`
- `/model_make/register` 세부 확인 (정확 문구 기준):
  - `prepare-ref-audio failed: ...` (Server A 응답은 왔지만 non-200)
  - `prepare-ref-audio error: ...` (요청/파싱/기타 예외)
  - `ref_audio prepared: ...` (보정 성공)
  - 가중치 자동 추론(`get_files_logs`) 실패는 현재 의도적으로 로그 없이 흡수될 수 있음(등록 자체는 계속 진행)
- 관련 env/설정:
  - `SERVER_A_TRAIN_VOICE_ROOT`
  - `SERVER_A_LOGS_ROOT`
  - `SERVER_A_TEMP_ROOT`
  - `SERVER_A_FILES_API_URL` / `SERVER_A_TRAINING_API_URL`
- 운영용 우선 확인 순서(도메인별):
  1. `train_input_dir`, `model_name`, `ref_audio_file` 요청값이 프론트 업로드 결과와 일치하는지 확인
  2. `prepare-ref-audio` 로그가 `prepared`/`failed`/`error` 중 어떤 경로로 찍혔는지 확인
  3. 최종 `ref_audio_path` 조합 기준(`SERVER_A_TRAIN_VOICE_ROOT/train_input_dir/ref_audio_file`)이 맞는지 확인
  4. `gpt_weights_path`/`sovits_weights_path`가 요청에 없으면 logs 인덱스 자동 추론 결과를 확인
  5. Voice DB 등록은 성공했는데 경로만 비어있다면 logs 인덱스 구조/`model_name` 매칭 실패 가능성 우선 확인
- 추가 확인 포인트:
  - `upload_training_files()` 반환 `train_input_dir`를 프론트가 그대로 `/start`, `/register`에 전달했는지
  - `register_voice()`에서 `gpt_weights_path`, `sovits_weights_path`가 미지정이면 logs 인덱스 자동 추론 결과를 확인했는지
  - `delete_my_model_voice()`에서 Character `voice_id=NULL` 해제 후 DB Voice 삭제 순서를 유지하는지

#### `system.py` (상세 헬스 체크 대시보드)

- 우선 확인 순서:
  1. `database`가 offline인지 외부 서비스만 offline인지 먼저 분류
  2. 외부 서비스 offline이면 `ollama/tts/server_a_files/server_a_train` 중 어떤 key인지 확인
  3. `message`가 연결 가능(4xx) 안내인지 실제 네트워크 예외 문자열인지 구분
  4. `url` 표시값이 base URL인지 health endpoint 경로인지 구분해서 운영자 혼동 방지
- 실제 로그 문자열(코드 기준):
  - 현재 라우터/헬스 helper에서 `logger.*` 로그를 직접 남기지 않음 (응답 payload 중심)
- 참고 응답 메시지 문자열(코드 기준, `_health_message`):
  - `연결됨 (5xx 서버 오류)`
  - `연결됨 (해당 경로 GET 미지원)`
  - `연결됨 (POST 전용 엔드포인트)`
  - `연결됨 (파라미터 필요)`
  - `Status: <status_code>`
- 관련 env/설정:
  - `OLLAMA_BASE_URL`
  - `TTS_BASE_URL`
  - `TTS_API_PATH`
  - `SERVER_A_FILES_API_URL`
  - `SERVER_A_TRAINING_API_URL`
- 운영용 우선 확인 순서(도메인별):
  1. 응답 최상위 `success=true` 여부보다 `services` 내부 key별 상태를 먼저 확인
  2. `database.status` 확인 후 DB 이슈면 외부 서비스 상태와 분리해서 처리
  3. `tts/server_a_train`은 timeout이 길게 잡혀 있으므로(상대적 5초) 지연을 네트워크 장애와 구분
  4. 404/405/422가 `online`으로 나와도 message를 보고 "경로/메서드 차이"인지 판단
  5. `message`가 예외 문자열이면 DNS/SSL/연결거부 여부를 URL/스킴과 함께 확인
- 추가 확인 포인트:
  - `system.py`는 응답 payload 중심이라 별도 `logger.*` 로그가 없음을 운영 체크리스트에 명시적으로 유지한다.
  - 4xx를 `online`으로 보는 정책은 연결성 확인 목적이므로, 기능 실패 판단과 혼동하지 않게 분리해서 해석한다.

#### `evaluation.py` (대화 평가 + 요약 저장)

- 우선 확인 순서:
  1. 평가 품질 이슈인지(내용 이상) fallback 사용 이슈인지(`success=true` 유지) 먼저 구분
  2. `Evaluation fallback used: %s` 로그 여부로 LLM/파싱/요약 저장 실패 경로 진입 확인
  3. LLM 응답 파싱 실패인지 `context_manager` 저장 경로 실패인지 request payload 기반으로 분류
  4. `summary`가 fallback 텍스트인지, 실제 통합 요약(`context_manager`) 결과인지 확인
- 실제 로그 문자열(코드 기준):
  - `Evaluation fallback used: %s`
- 관련 env/설정:
  - `GEMINI_API_KEY`
  - `GOOGLE_API_MODEL`
  - `GOOGLE_SAFETY_THRESHOLD`
  - `OLLAMA_BASE_URL`
  - `REDIS_URL` (간접: `context_manager` 저장소 fallback 경로)
- 운영용 우선 확인 순서(도메인별):
  1. 요청 `messages`가 비었거나 role/content shape가 비정상인지 확인 (프롬프트 품질 저하 원인)
  2. `Evaluation fallback used: %s` 로그 존재 여부 확인
  3. fallback이면 LLM 응답/JSON 파싱/요약 저장 중 어느 단계인지 최근 변경점 기준으로 분류
  4. Redis/DB 이슈가 있으면 `context_manager` fallback 정책으로 요약 저장 경로가 바뀌었는지 확인
  5. 프론트는 `success=true`여도 fallback 결과를 정상으로 보일 수 있으므로 내용 품질까지 함께 점검
- 추가 확인 포인트:
  - fallback 응답도 `success=true`이므로 API 성공률 지표만으로 품질 회귀를 감지하기 어렵다.
  - `context_manager` 저장소 fallback(DB/Redis) 경로는 평가 품질 이슈와 분리해서 추적해야 한다.

#### `story.py` (스토리 생성 + Scenario 저장)

- 우선 확인 순서:
  1. 응답이 `success=true`라도 fallback 텍스트인지 실제 생성 결과인지 먼저 구분
  2. `Story generation endpoint fallback used: %s` 로그 존재 여부 확인
  3. 실패 원인이 LLM 호출/JSON 파싱/DB 저장(`Scenario`) 중 어디인지 최근 변경/배포 기준으로 분류
  4. DB 저장 실패였는지 확인할 때 `scenario_id` 반환 유무로 실제 저장 성공 여부를 함께 확인
- 실제 로그 문자열(코드 기준):
  - `Story generation endpoint fallback used: %s`
- 관련 env/설정:
  - `GEMINI_API_KEY`
  - `GOOGLE_API_MODEL`
  - `GOOGLE_SAFETY_THRESHOLD`
  - `OLLAMA_BASE_URL`
  - (간접) DB 연결 설정 (`DATABASE_URL` 계열 / `app.core.database`)
- 운영용 우선 확인 순서(도메인별):
  1. 프론트가 `summary/background`는 받았더라도 fallback 문구 패턴인지 먼저 확인
  2. `Story generation endpoint fallback used: %s` 로그 확인
  3. 최근 LLM/provider 변경이 있으면 LLM 응답 포맷(JSON 코드블록 포함 여부) 회귀를 우선 의심
  4. DB 이슈가 의심되면 `scenario_id` 누락 여부와 DB 연결 상태를 함께 확인
  5. 현재 계약상 fallback도 `success=true`이므로 모니터링은 응답 성공률만 보지 말고 fallback 로그 비율도 함께 본다
- 추가 확인 포인트:
  - `scenario_id`가 없으면 fallback 가능성이 높지만, 프론트는 `summary/background`만으로 성공처럼 보일 수 있다.
  - JSON 코드블록 제거 로직(````json`/``` ) 회귀는 LLM 모델 변경 직후 자주 발생하는 이슈 포인트다.

<!-- MANUAL_APPENDIX_END -->
